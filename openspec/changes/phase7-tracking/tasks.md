# Tasks: Phase 7 (Deepen — Plasticity & Cross-Species Transfer) Shipment Tracker

This is the living checklist for Phase 7. The plan is authoritative in `docs/roadmap.md`
§ Phase 7 (v4.2, PR #300): the pre-registered 2×2 central hypothesis (rule × wiring),
design decisions D1–D13, exit criteria, and risk table. Phase 7 ships in three cuts —
**7a-i** (platform freeze + minimal rule + 2×2 panel), **7a-ii** (receptor-grounded
neuromodulator stack), **7b** (cross-species comparative learning) — plus a synthesis.
Every Phase 7 milestone PR updates this checklist as part of its diff.

**Status legend**: `[ ]` not started, `[x]` closed — done, or (for unexercised SHOULD scope) dropped/deferred with a dated note, per the house dropped-scope style (`phase6-tracking` precedent), so `openspec archive` is never blocked by an honestly-unexercised SHOULD.

**Preconditions (both hold):**

1. ✅ **Gate 3 GO** — Phase 6a closed ([Logbook 037](../../../docs/experiments/logbooks/037-phase6a-synthesis.md), 2026-07-07). Phase 7 never gates on Phase 6b (`phase6b-tracking`, decoupled per D13).
2. ✅ **Plan finalised** — roadmap v4.2 with D1–D13 ratified and adversarial-review amendments merged (PR #300, merged 2026-08-27).

> **Not in Phase 7.** Co-evolution (deferred, no destination — roadmap RQ4). Phase 6b NEAT (own tracker; opportunistic pending a dated GPU/cloud decision — D13/D6). Neuropeptide layer as a rule substrate (Phase-7+ candidate, roadmap § signalling-layer scope). Full `∂C/∂t = D∇²C` PDE. *C. briggsae* (no connectome exists, re-verified 2026-08).

<!-- -->

> **Execution-protocol standards (Decision B in design.md) apply to every panel/sweep task below** — n ≥ 8 paired-seed with BH-FDR within-pass; uniform budget set by the slowest converger with a convergence audit before ranking (level-agnostic metric); metric audit before every panel; byte-identical-when-off; load-time config validation; substrate freeze before the L4 panel; no wiring claim off a single seed.

<!-- -->

> **Execution**: the n ≥ 8 paired-seed arms below launch through `scripts/run_campaign.py` (`add-parallel-campaign-runner`), which runs the config × seed cross product as isolated subprocesses of the standard single-run entry point — each run byte-for-byte the command it replaces, so results are unchanged and only wall-clock moves.

<!-- -->

> **Coarse-grained by design.** These sub-tasks are the load-bearing shape; per-milestone OpenSpec changes elaborate them (first: `add-l4-trace-substrate`).

## Shipment 7a-i — Platform Freeze + Minimal Rule + the 2×2 Panel

**OpenSpec changes**: `add-l4-trace-substrate` (merged 2026-09-05, archived `2026-09-04-add-l4-trace-substrate` — A.1/A.2 done), `add-state-dependent-action-std` (merged 2026-09-05, archived `2026-09-05-add-state-dependent-action-std` — the P.1–P.5 platform tranche, closed under Amendment A), then a panel change (placeholder; created per milestone)
**Status**: 🟡 in progress (A.1–A.8 done 2026-09-06 — the 7a-i panel resolved to `sanity_floor_fail`, Logbook 040; P.1–P.5 closed 2026-09-05 under Amendment A — substrate FROZEN mode-off; A.9 panel 2 and A.10 panel 3 both resolved `inconclusive` 2026-09-07, Logbooks 041 and 042 written; S.2 resolved `sanity_floor_fail` + `rule_destroys_clone` 2026-09-08, Logbook 043 written; clone-destruction diagnostic done and 7a-ii restructured 2026-09-08; **B.1 atlas done 2026-09-09, verdict `degree_statistics` — grounded signs leave the prior alone and make Hebbian learning worse, so the substrate was not the limit**, [Logbook 044](../../../docs/experiments/logbooks/044-l4-atlas-signs.md) written; B.4 consolidation screened 2026-09-09 with no mechanism passing the clone assay, [Logbook 045](../../../docs/experiments/logbooks/045-l4-consolidation.md); the decorrelating term ran 2026-09-09 and resolved `no_recovery`, [Logbook 046](../../../docs/experiments/logbooks/046-l4-decorrelation.md); B.4b structured instruction resolved 2026-09-10 `no_routing_effect`, [Logbook 047](../../../docs/experiments/logbooks/047-l4-structured-instruction.md); **reordered 2026-09-09 around the instrument**: block I runs before any further substrate rung, B.4b completes as registered, and B.8 is taken after block I. **I.0 ran 2026-09-10 and failed with the control valid** ([Logbook 048](../../../docs/experiments/logbooks/048-l4-rule-positive-control.md)) — the rule does not learn a task its analytic reference solves to 99.9%, and its updates are near-orthogonal to the policy gradient — so I.1, an eligibility with the noise inside it, became the critical path — and **I.1 passed the control 2026-09-10**, the first mechanism in the sequence that works; the clone assay ran 2026-09-10 and the variant failed it, though a frozen control shows perturbation dominates that loss rather than the rule; **σ-annealing (I.1b) then failed the positive control 2026-09-10** — the registered decay spends 70% of its budget below a scale already shown not to clear the bar, so the assay did not run; **I.1c step 0 ran 2026-09-11 and failed** (endpoints evaluated perturbation-off: mean 20.6 vs 38.7) but found the effect **bimodal** — six seeds degraded, two improved, seed 2 to 73.4, the best this substrate has recorded — which licenses the low-σ programme and gave I.2 a demonstrated case; **I.2 was then built 2026-09-11** and its re-read promotes no contrast — 14 `no_effect`, 3 `degrades` — so the committed negative readings survive a statistic matched to the shape and the closed wiring hypothesis is not reopened; **I.3 ran 2026-09-11 and found the eligibility horizon limiting and recoverable** — at the pinned decay the rule falls below the cue-blind floor by 20 steps of delay, and one setting change recovers 45% of the gap there, the first mechanism that predicts the one-step-success / multi-step-failure pattern rather than describing it; **I.3b then tested that horizon on a multi-step task 2026-09-11 and it did not transfer** — the yardstick's learning arm is worse than its own frozen control at every horizon and worse still as the horizon lengthens, with weights moving further than their own norm, so the failure is destructive rather than starved; I.4 then re-read the ladder 2026-09-12 and **no committed verdict changed** — of 32 registered contrasts across 040–047 only ten are instrument findings, sixteen are substrate findings block I does not reach (four under no rule, twelve under the reward-free Hebbian rule I.0 never tested), five are about neither because no optimiser has ever found a wild-type advantage (034 indistinguishable under PPO, 043's PPO arms with the null ahead by 12.6 on 0/8), and one is about PPO and carries a withdrawn reading — [Logbook 056](../../../docs/experiments/logbooks/056-l4-ladder-reread.md); **block I is complete and B.8 is unblocked**, with GO unreachable as written because its clause names the never-built B.3; the panel stays gated; **V.1 then tested the premise on a cell matched to a behaviour the animal performs, 2026-09-12, and it holds** — under PPO the wild-type wiring reaches competence ~35% sooner than its degree-preserving rewired null on a foraging cell under thermal pressure, 396 episodes against 613 over 64 paired seeds at q ≤ 0.001, replicated on an independent 32-seed panel, with both learning gates 64/64 and the untrained prior indistinguishable; it is **speed rather than performance** and **PPO rather than a local rule**, so 7b's gate as written is still unmet, [Logbook 057](../../../docs/experiments/logbooks/057-wiring-premise-contrast.md); **V.3 then removed temperature 2026-09-13 and the advantage survives** — +23.5% off time-to-competence on a hard food-only cell over 32 paired seeds, so difficulty is sufficient and the thermosensory projection is not necessary, [Logbook 058](../../../docs/experiments/logbooks/058-wiring-premise-difficulty.md); **V.4 then closed block V's shared-nulls caveat 2026-09-16 and the advantage replicates on fresh rewirings** — seeds 65–96, fresh to both prior panels, 256/256 runs, **+55.3%** on the thermal cell and **+40.1%** on `hard350`, both above their own comparators and all four efficiency metrics at q = 0.000, through both committed harnesses unmodified; it does **not** separate rewiring from initialisation, which stays unregistered, [Logbook 065](../../../docs/experiments/logbooks/065-wiring-fresh-rewiring.md))
**Roadmap layer**: L4 (minimal)
**Approx effort**: ~3-5 active weeks (roadmap § estimate restatement)
**Roadmap reference**: `docs/roadmap.md` § Phase 7 § Required deliverables 1, D1/D2/D5/D7/D8/D10/D13

### Pre-panel platform tranche (D5/D7 — land, validate, freeze)

- [x] P.1 **D5** (verified already-done, 2026-09-05): the #254 dead-key removal shipped during the Phase 6 bit-memory pre-work — zero `normalize_advantages` keys and zero lstmppo renamed-key residue in `configs/` (grep-verified; no code change). Advantage normalization stays unimplemented (closed-issue constraint, 029 raw-GAE lineage).
- [x] P.2 **D7** (`add-state-dependent-action-std`, 2026-09-05): state-dependent std heads on all five continuous brains, byte-identical-when-off and step-0 bit-equal on/off as a run property (RNG-free zero-Parameter heads; no `_policy.py` changes — the helpers were already shape-generic); ships **dormant** per the Amendment A gate outcome.
- [x] P.3 **D7 validation gate — FAILED** ([Logbook 038](../../../docs/experiments/logbooks/038-state-dependent-std-gate.md), 2026-09-05): attempt 1 (ent 0.05) hit the clamp-ceiling trap (monitor caught it live); the one pre-registered entropy-only pass (ent 0.01) left klinokinesis EQUIVOCAL with weathervane regressed — capability without pressure. Dated Amendment A applied; the Leifer/Chen validation target stays unmeasurable; klinokinesis retry is recorded future work via its own pre-registered change.
- [x] P.4 **DESCOPED under Amendment A** (2026-09-05): the substrate froze mode-off, so nothing changed under Logbook 029 — **029 remains the panel's descriptive reference frame** and there is nothing to re-measure. (House dropped-scope tick.)
- [x] P.5 (2026-09-05): plasticity-key half verified already-done (the trace fields are pydantic-validated since #303); the new `continuous_std_mode` field validates via `model_fields` + a model validator. **Substrate declared FROZEN for the L4 panel in `continuous_std_mode: state_independent` (mode-off, Amendment A)** — the panel runs the unchanged 029-lineage substrate; any mode-on retry is a new pre-registered change.

### L4 minimal rule + panel

- [x] A.1 Rule-seam decision documented (`add-l4-trace-substrate`, 2026-08-28: Protocols reconciled to the seam a rule needs — mask projector + learnable parameters; the inlined PPO update extracted verbatim into `learning_rules.ConnectomePPORule` under the M1 frozen-reference byte-equivalence bar; conformance now genuinely `isinstance`-passes).
- [x] A.2 Persistent activity traces on `ConnectomeTopology` (`add-l4-trace-substrate`, 2026-08-28: conditionally-allocated eligibility buffer, v1 formula `E ← λE + M∘(h hᵀ)` under load-bearing `no_grad`, reset at `prepare_episode`; byte-identical when off AND training bit-identical with traces on until a rule consumes them).
- [x] A.3 Minimal rate-based three-factor rule (`add-l4-three-factor-rule`, 2026-09-05): `ConnectomeThreeFactorRule` in `learning_rules/` — `dw = eta * delta * E` with an EMA-baseline prediction error, per-step cadence, no gradients or critic, chemical synapses only, bounded by decay + clamp with saturation telemetry. Two ratified design choices: the eligibility trace became **temporally causal** (`h_prev (x) h`, exercising the A.2 amendment clause — the symmetric form could not distinguish reciprocal edge directions), and the frozen motor readout is set to the **anatomical** dorsal/ventral and forward/backward contrasts rather than a random orthogonal draw. Dale's law **withdrawn at spec review**: synapse signs here are arbitrary draws, not neurotransmitter identity, so the constraint would freeze noise — enforceable once 7a-ii grounds signs in the atlas. Default `ppo` path byte-identical.
- [x] A.4 Baseline arms (`add-l4-baseline-arms`, 2026-09-05): **frozen-weights** and **unmodulated-Hebbian** sanity floors, both configured under the plasticity rule rather than PPO so they share its anatomical readout — a floor decoding differently from the arm it bounds would confound decoding with learning. "Vanilla rule" was undefined in D2/D10 and is resolved here as unmodulated Hebbian (`Δw = η·E`): beating the frozen floor shows only that *something* was learned, beating this one shows something was learned **from reward**. The unmodulated arm still reports the prediction error it discards, so the ablation is visible in telemetry. **Finding (a) resolved**: D2's "Cook-2019 synapse-count-derived initial weights" corrected in wording, not code — the connectome gives edge existence, weights are `N(0, 1/√(chemical in-degree))`, and changing the initialisation would move the substrate Amendment A froze. **Finding (b) was my over-claim and is withdrawn**: the frozen arm needs no weight persistence (topology + seed + freeze flag suffice). The real persistence gap — `save_weights` is a no-op for this brain, so plastic runs discard the synapses they modified — is filed as issue #308 and blocks the imitation-warm-start arm (S.2), not this one. Also added the first connectome smoke entries: the plastic path had never run end to end through the entry point.
- [x] A.5 Matched-rule MLP arm (`add-l4-matched-rule-mlp`, 2026-09-06): D10 called the rule substrate-generic and the code disagreed — it named five connectome attributes. It now reads a `PlasticTopology` seam and `mlpppo` runs it as the ranking yardstick: same rule class, same arithmetic (proved bit-identical on the connectome against a pre-rewrite frozen reference), same hyperparameters from one shared mixin. `MLPTopology` wraps the actor by reference, so the MLP PPO path is byte-identical to a pre-refactor frozen reference too. **Ratified with Chris:** every Linear weight plastic including the output layer — the asymmetry is in the MLP's favour and makes it a *conservative* yardstick (freezing a random output layer would have matched the OLD random decoder, not the anatomical one the connectome runs). Feedforward eligibility is same-step post⊗pre (a layer already orders pre before post; the connectome needed h_prev only because h⊗h is symmetric). Review caught that the discrete path forwards the actor twice per step — only the action-selecting forward accrues eligibility. Next: A.6 rewired-null plastic arm, then the panel.
- [x] A.6 Plastic degree-preserving rewired-null arm (`add-l4-plastic-rewired-null`, 2026-09-06): one config, one key (`wiring: rewired_degree_preserving`) off the plastic wild-type arm — no mechanism, since 034's rewiring precedes topology construction so traces and updates sit on the null wiring by construction. The value is the parity claim, established by probe on the **real C3 config** (predator + thermotaxis projections on) before being specified: identical readout, all five gains, `log_std`, trace config and hyperparameters at one seed; every chemical in/out and gap degree preserved; mask, `w_chem` placement and `g_gap` differ; pairing by run seed. Probe caught a false claim — per-neuron initial weight *energy* is NOT preserved (same draws, different pairs); the spec states scale and declines energy, with a negative-space test. **Ratified with Chris:** primary arm only; rewired versions of the floors are one-key configs deferred to A.7's pre-registration. Next: A.7 the 2×2 panel.
- [x] A.7 **The 2×2 panel**: plastic wild-type vs plastic rewired-null (primary, paired n ≥ 8) + matched-rule-MLP ranking test, under the D2 success tests; PPO cells enter as qualitative Phase 6 context only. *(Complete 2026-09-06 — `add-l4-panel`, PRs #312 and #318, verdict `sanity_floor_fail`: pre-registration, rewired floors, harness and pilot runner landed. The registered pilot pinned nothing — the grid was two orders of magnitude too hot and the MLP yardstick is dead at 0.01 and frozen at ≤3e-3, its per-weight trace ~1000× smaller than the connectome's, so a matched rate is not a matched rule. **Ratified with Chris:** a substrate-invariant rule-scaling change is inserted before the panel; the grid is then re-registered and the pilot re-run. The connectome learns at 1e-4 in a diagnostic probe. Then: centred modulator (#314), rule robustness (#316: shared noise, homeostasis, tanh yardstick), frozen readout (#317); pilot 3 pinned rate 1e-3 / budget 3000; probes 5–6 showed the MLP yardstick cannot hold a policy under the rule even with a frozen readout (hidden-layer collapse without lateral decorrelation) — it enters the panel as registered and is read as a finding; a **sparse random MLP** arm is the pre-registered follow-up. Panel complete 2026-09-06 (`add-l4-panel`): **verdict `sanity_floor_fail`** — T1 +6.6 (q .50, 4/8), T2 +9.6 (q .50, 5/8), T3 −12.4 (q .73, 4/8), T4 +6.2; band vacuous. Outcomes are seed-dependent fixed points; the wild-type Hebbian floor reaches 64–78% on three seeds with no reward and beats the rewired Hebbian floor descriptively (+16.5, 5/8) while the frozen floors tie — the wiring signal is in the floors. Data under `supporting/040-l4-panel/`. Next: A.8 logbook 040; follow-ups pre-registered separately — the sparse random MLP arm, and a rule variant with anti-Hebbian/decorrelating and pathway-specific instruction (Perks et al. 2026, Nature).)*
- [x] A.8 7a-i logbook (2×2 verdict: **`sanity_floor_fail`** — the roadmap risk table's "fails to beat its baselines" branch) + roadmap Phase 7 status sync. *(2026-09-06: [Logbook 040](../../../docs/experiments/logbooks/040-l4-panel.md) published — `sanity_floor_fail`; roadmap Phase 7 row, 7a-i bullet, risk table, novelty map and literature updated; Perks et al. 2026 added; **structured (pathway-specific) instruction** added to 7a-ii's scope; **dynamic wiring** added to Future Directions. **Ratified ordering for what follows**: panel 2 (Hebbian wiring contrast + prior sweep + initialisation factor) → S.2 imitation warm start → 7a-ii with structured instruction.)*
- [x] A.9 **Panel 2 — the Hebbian wiring contrast** (`add-l4-panel2-hebbian-wiring`, 2026-09-07): verdict **`inconclusive`** — P1 wt_hebbian − rn_hebbian +14.1 (80% CI +2.7..+25.0, q .277, 10/16 seeds; +16.2 on panel 1's seeds 1–8, +11.9 on fresh seeds 9–16), the interval clear of zero but the bimodal per-seed outcome (spread 36.5) leaves the Wilcoxon at n = 16 unpowered; P4 prior sweep (64 seeds) +3.3 (CI −0.0..+6.8, q .277; competent fractions 0.22 vs 0.16), so the wild-type advantage is mostly created by the reward-free Hebbian fixed point (learning gains +25.7 vs +9.6), not present in the prior; **P3 reverses** — synapse-count-scaled initialisation (linear, random signs, unit incoming norm) halves the wild-type Hebbian fixed point (31.8 → 15.6) and erases the wiring contrast (P2 +2.0) with no detectable difference on any frozen prior. Records under `supporting/041-l4-panel2/`; [Logbook 041](../../../docs/experiments/logbooks/041-l4-panel2.md) written 2026-09-07. Follow-ups queued: a sparse random MLP arm; a rule variant with anti-Hebbian/decorrelating terms; the plastic arms under count init are not worth running as-is (count init hurts the Hebbian fixed point).
- [x] A.10 **Panel 3 — replicating the Hebbian wiring contrast on fresh seeds** (`add-l4-panel3-hebbian-replication`, 2026-09-07, ratified ahead of S.2): verdict **`inconclusive`** — R1 wt_hebbian − rn_hebbian on seeds 17–64 +8.1 (80% CI +2.3..+14.2, q .19, 24/48 positive, 22 negative); R2 competent-fraction discordance 11 vs 6 (q .19). The estimate shrank with each fresh look (+16.2 → +11.9 → +8.1); pooled over 64 seeds +9.6 (CI +4.3..+15.0, 34/64) with competent fractions 0.39 vs 0.25. The wiring's mark is in the level of the good fixed points (wild-type's best eight 56–86% against 33–56%), not their frequency; a paired rank test sees a chance sign split. Registered power at the corrected threshold was 63–75% for +12..+14 and nearer 50% for the observed +8. **The Hebbian wiring contrast stays unconfirmed after 64 seeds and is not extended further on the same hypothesis.** Records under `supporting/042-l4-panel3/`; [Logbook 042](../../../docs/experiments/logbooks/042-l4-panel3.md) written 2026-09-07.

### SHOULD in the 7a-i window (priority order per design.md § Risks)

- [x] S.1 **6a preprint** *(**cancelled** 2026-09-15 — not deferred: D13's case for staking 6a early was a fast-moving field, and Phase 7's own results now make a **stronger combined package** than 6a alone. Publication is relooked after the Phase 7 close, on the whole arc — the ranking and rewired-null from 6a, the systematic negative with a diagnosed cause, block V's learning-speed advantage, and whatever L.0 settles. Recorded as a decision rather than left open, so the tracker closes on it.)* arXiv/bioRxiv draft — platform + ranking + rewired-null + real-worm validation, with the pre-registered 2×2 in the discussion (D13).
- [x] S.2 **Imitation warm start** (`add-l4-warm-start-tooling` PR #324/#325 + `add-l4-warm-start-panel`, 2026-09-08): verdict **`sanity_floor_fail`** with **`rule_destroys_clone`**. The clone gate passes on 8/8 seeds (chemical weights alone behind the anatomical readout: 8% → 39%; every PPO parameter: 74%, three quarters of the teacher's 99%) — the connectome can *retain* this cloned policy and the wiring does not limit that (the rewired null carries it at least as well; W2 reverses); no broader representability claim follows from one cloned teacher. The three-factor rule takes the clone apart: below its frozen clone on the wild-type (W4 −25.7, registered) and on the rewired null (−22.6, CI[−30.0,−14.7], descriptive), and below its Hebbian clone on the wild-type (W5 −25.7) though not on the rewired null (+2.2, ns); the unmodulated Hebbian rule holds it on the wild-type (39%, 6/8 competent) and not on the rewired null (18%) — the panel's one wiring-specific effect, descriptive, in the Hebbian floor again. A warm start hurts PPO (W6 −34.1, 1/8): fine-tuning from the 74% clone declines from the first block while PPO from random weights at the same low noise climbs to 69% (wild-type) and 81% (rewired null, beating the wild-type on 8/8) in 3000 episodes, against 029's 52% at 6000 with std 1.0. Attempt 1 of the plastic-set arms ran against a load defect (homeostatic targets not re-anchored on load), fixed and re-run under a dated amendment. Records under `supporting/043-l4-warm-start/`; [Logbook 043](../../../docs/experiments/logbooks/043-l4-warm-start.md) written 2026-09-08.

## Shipment 7a-ii — Receptor-Grounded Neuromodulator Stack

**OpenSpec change**: placeholder; created at milestone start
**Status**: 🟡 in progress (restructured 2026-09-08 after the diagnostic; B.1 done 2026-09-09, verdict `degree_statistics`, [Logbook 044](../../../docs/experiments/logbooks/044-l4-atlas-signs.md); B.4 screened 2026-09-09 — no mechanism passes the clone assay, rigidity closest, [Logbook 045](../../../docs/experiments/logbooks/045-l4-consolidation.md) written; the decorrelating term ran 2026-09-09 and resolved `no_recovery`, with the anti-Hebbian arm redirecting every grounded inhibitory synapse there is (5.8% of the substrate) and the Oja arm supplying 2.2% of the effective update at its pinned coefficient, a weak test of that term rather than none; next: B.4b structured instruction, with the receptor layer now a prerequisite for re-testing the inhibitory brake and a fast quality-gated brake still untested)
**Roadmap layer**: L4 (grounded)
**Approx effort**: ~4-6 active weeks
**Shipment**: ✅ **SPLIT taken 2026-09-13** ([Logbook 059](../../../docs/experiments/logbooks/059-7a-shipment.md)) — two citable results, GO unreachable on its own clause, STOP overstating. B.2, B.3, B.5, B.6 and B.7 carry recorded statuses rather than reading as outstanding. **Closing scope under D14 (2026-09-15): L.0 and V.4 — both shipped 2026-09-16** (L.0 `wiring_is_inert_as_features`, V.4 `specific_wiring_efficiency` on both cells), which fires L.1 as a MUST for the close and leaves L.1, L.2, Z.1 and Z.2 live. The rule programme is closed (R.1, R.1c, R.1d, R.2), the substrate rungs it would have re-registered are superseded by L.0–L.3, and 7b is deferred.
**Dependencies**: 7a-i panel closed
**Roadmap reference**: `docs/roadmap.md` § Phase 7 § Required deliverables 1, D4/D12

- [x] B.0 **Clone-destruction diagnostic** (probe, 2026-09-08; [record](../../../docs/experiments/logbooks/supporting/043-l4-warm-start/destruction-diagnostic.md)): both local rules rewrite a cloned competent policy on every seed (cosine 0.2–0.45 at 2000 episodes) at a near-constant, rate-insensitive drift; the reward rule finds better policies (76% at 250 episodes) and leaves them; there is no start-independent Hebbian fixed point (endpoints from clone and from random are orthogonal), yet Hebbian drift from a competent start stays competent on the wild-type (50–58%) and not from a random one (0–4%). Diagnosis: no consolidation — the normalised modulator and trace give a constant step and homeostasis removes decay as a brake. Consequence: consolidation before instruction; the one-hour clone assay gates B.5.

- [x] B.1 **Substrate deliverable** (`add-l4-atlas-signs`, 2026-09-09): the Wang 2024 atlas vendored from the OpenWorm mirror with provenance; 280 of 302 neurons carry a release identity (the slot was `None` since Phase 6); `synapse_signs: atlas` grounds 3,176 of 3,709 synapses (2,962 excitatory, 214 inhibitory), taking the network from 48% inhibitory to 13% with magnitudes, incoming norms, readout, gains and RNG stream untouched; `enforce_synapse_signs` adds Dale's law during plasticity. **Registered test verdict `degree_statistics`**: the substrate gate did NOT fire (grounded wild-type prior competent 0.23 vs the committed 0.22; G1 +0.8 ns — the prior is indifferent to signs, though the wild-type median doubles and the rewired competent fraction falls 0.16 → 0.12), G2 +4.9 ns (the wiring contrast is not rescued), and **reward-free Hebbian learning gets substantially worse** (wild-type 31.5 → 14.0, rewired 17.4 → 9.1, both intervals clear of zero), with **Dale's law worse again** on the wild-type (G4 −4.8 reverse). Mechanism: a purely potentiating rule on an 80%-excitatory network has no inhibitory brake; random signs were an accidentally balanced substrate. **The substrate's signs were not what limited the rule** — this moves the anti-Hebbian/decorrelating variant ahead of structured routing in the queue below, and B.3's receptor layer is fidelity work that should not be expected to rescue learning. Records under `supporting/044-l4-atlas-signs/`; written up as [Logbook 044](../../../docs/experiments/logbooks/044-l4-atlas-signs.md). \*(Original scope, receptor classes deferred to B.3: receptor/transmitter metadata as a **vendored-data sub-deliverable** with its own provenance doc: release identities from the Wang 2024 CRISPR neurotransmitter atlas; receptor classes from bulk-integrated CeNGEN profiles (~1-2 focused weeks of two-atlas curation). **With its own registered test**: grounded signs on the Cook 2019 wiring re-run through the frozen prior sweep and the Hebbian wiring contrast (Logbooks 041–042's protocol), asking whether the sign structure alone changes either answer.

- [x] B.2 *(**deferred** 2026-09-13 by B.8 — the plumbing exists and the work is cheap, but it feeds observations to a rule that does not learn and to a wiring contrast that does not depend on it. Its home is a substrate programme after a rule reads the wiring, not 7a-ii's closure.)* Internal-state sensory module (satiety/health into the observation — `BrainParams` plumbing exists; this is the minimal metabolic-state grounding).

- [x] B.3 *(**closed-unpaid** 2026-09-13 by B.8 — its own condition is that a rung after it can conclude, and that rung is B.5, whose gate never opened. **This is the clause GO depends on**, which is why GO was unreachable rather than merely unmet. It becomes payable if B.5 can conclude; 046's finding that a transmitter-only atlas grounds just 5.8% of synapses as inhibitory stands as the reason it would then be a prerequisite rather than fidelity work.)* Diffusible-signal layer v1 per **D12** *(raised in priority 2026-09-09: the decorrelation test showed a transmitter-only atlas grounds just 5.8% of synapses as inhibitory, too few to build an inhibitory brake from, so the receptor layer is now a prerequisite for testing that hypothesis rather than fidelity work alone)*: per-modulator global scalars (serotonin, dopamine), brain-internal, receptor-class gating; head-scope source policy as pinned (internal-state-only drive on truncated arms).

- [x] B.4 **Consolidation first** *(reordered 2026-09-08 after B.0)*: a rule mechanism that slows or stops updating once a policy is good — an update magnitude that scales monotonically with the prediction error's magnitude, so a small `|δ|` produces a small weight change (today's RMS normalisation gives a full-size step however small the raw error is), or a slow protective variable (dopamine-gated consolidation rather than dopamine-gated change) — designed and cleared through the [clone assay](../../../docs/experiments/logbooks/supporting/043-l4-warm-start/destruction-diagnostic.md#the-clone-assay), the single protocol defined with the diagnostic (wild-type clone arm, seeds 1–8, 2000 episodes, plateau tail against Logbook 043's frozen-clone values, pass = holds or improves), before any panel. Then the modulated three-factor rules — third factor = modulator concentration; receptor metadata routes which synapses see which modulators. **Run 2026-09-09** (`add-l4-consolidation`): three mechanisms implemented and screened — an elastic anchor, a per-synapse protective variable grown where a positive modulator met a large trace, and an oracle gate on the environment's episode-success flag registered as a bound rather than a candidate. Recon killed the diagnostic's first candidate before it was built: per-step prediction errors are the same size for a competent clone and a dead random start (median 0.266 vs 0.245) and no online scalar tracks policy quality, so a step monotone in |δ| is a rate change in disguise. **None of the three passes the clone assay**: anchor 13.0 (−25.7, 2/8 within hold), **rigidity 29.4 (−9.4, 6/8 within hold, endpoint cosine 0.74, rate ×0.09 — the best policy preservation this substrate has shown, failing the mean clause by 4.4 points at two catastrophic seeds)**, oracle 28.9 (−9.8). A 91% per-synapse rate cut applied where reward had been writing does not stop the drift, which is the diagnostic's rate-insensitivity again with the cut targeted. The oracle failed in the way its launch record predicted before the run — a trailing estimate at EMA rate 0.01 arrives a hundred episodes after a policy this rule destroys in tens, and where the gate did close it worked (seeds 4 and 6 finish at 55.2 and 58.6 from clones of 21.3 and 33.3) — so **a fast quality-gated brake is untested, not refuted**. Records under `supporting/045-l4-consolidation/`; written up as [Logbook 045](../../../docs/experiments/logbooks/045-l4-consolidation.md).

- [x] **Decorrelating term** (`add-l4-decorrelation`, 2026-09-09, run ahead of B.4b as Logbook 044 directed): two terms opposing the rule's positive-feedback loop — `anti_hebbian_inhibitory`, which negates the Hebbian term at grounded inhibitory synapses and has no hyperparameter, and `oja`. Registered test: 044's own Hebbian protocol re-run under each variant, seeds 1–16, against its committed per-seed table. **Verdict `no_recovery`** — D1 −0.90 (q .487), D2 −2.60 (q .487), `full_recovery` false everywhere, so 044's inhibitory-brake prediction is not supported by the arm built to test it. The registered decorrelation-share annotation separates the two failures: the anti-Hebbian arm's share is 0.057, **exactly the grounded inhibitory fraction (214 of 3,709 synapses)**, so it redirected every synapse it could and moved the outcome by −0.9 — there is not enough grounded inhibition at transmitter-only fidelity to build a brake from, which raises B.3's receptor layer from fidelity work to a prerequisite for testing this hypothesis; the Oja arm's share is 0.022, reproducing the comparator exactly on 8 of 16 seeds, so it was a **weak test of that term** at the pinned γ = 0.1 — the grid's top value of 1.0 would have contributed ~18% of the update and parity comes at γ ≈ 4.5, so the grid did offer a strong setting and the pilot chose against it on two seeds. Descriptive and post-hoc: seed 9, the comparator's best at 50.4, collapses to 0.0 under the variant and decides D1's sign — excluding it the mean delta is +2.4 on 9 of 15 seeds. Records under `supporting/046-l4-decorrelation/`; written up as [Logbook 046](../../../docs/experiments/logbooks/046-l4-decorrelation.md).

- [x] B.4b **Structured (pathway-specific) instruction** (`add-l4-structured-instruction`, 2026-09-10): verdict **`no_routing_effect`** — S1 −3.18, S2 −7.30, both q = .975. The family is one-sided, so the name means **no confirmed benefit**; the observed effect was negative on both wirings, with S2's interval entirely below zero, and the registered map had no harm branch to name that. Routing the third factor through the aminergic wiring (18 release neurons reaching 169 of 302, 71.1% of wild-type synapses; the rewired null derives its own 78.1%) made **both** arms worse and removed their good seeds: the best wild-type seed falls 70.5 → 24.3, competent seeds 3/16 → 1/16. The instructed share of the update (0.92 against a fraction of 0.71) shows the intervention was real, not marginal. **Read against I.0, which the rule failed the same day, this measures what a non-learning rule does under two routing regimes rather than whether aminergic pathways matter.** The registered consistency check reproduces panel 1's committed values **bit-for-bit**, so the substrate has not drifted across six panels. Seven runs lost to a mid-campaign branch switch were re-run and one destroyed log regenerated; the panel as scored is 16 seeds per arm with nothing imputed. Records under `supporting/047-l4-structured-instruction/`; written up as [Logbook 047](../../../docs/experiments/logbooks/047-l4-structured-instruction.md). *(Original scope:* *(added 2026-09-07 after Logbook 040 and Perks et al. 2026)*: the third factor SHALL be routed per instructive pathway — which synapses receive credit is determined by the receptor/transmitter metadata *and* by the connectivity that carries the modulatory signal to them — not only by modulator concentration and receptor class. Acceptance: the 7a-ii OpenSpec states the pathway model, its provenance, and a test that credit reaches only the synapses the wiring instructs; the 7a-i panel's global-scalar result is the registered baseline.\*)

- [x] B.5 *(**closed-unmet** 2026-09-13 by B.8 — its gate is "only after a rule variant passes the clone assay" and **four mechanisms failed it**: the elastic anchor at 13.0 against the frozen clone's 38.7, the per-synapse protective variable at 29.4 failing one clause, the oracle gate at 28.9, and the node-perturbation repair at 12.0 (20.6 with its perturbation off). The gate never opened, so this is not outstanding work. **Dated note, 2026-09-13**: when B.5 is re-registered its gate should become the block-V bar — learns the hard-food cell *and* shows the ≥ 20% time-to-competence advantage the wiring is known to carry there — rather than the clone assay; the registered gate below stands until that re-registration.)* 2×2 panel **re-run under the grounded rule** (same protocol, same frozen substrate) — **gated**: only after a rule variant passes the [clone assay](../../../docs/experiments/logbooks/supporting/043-l4-warm-start/destruction-diagnostic.md#the-clone-assay); the warm-started arms (Logbook 043's registry) join the panel as the competent-start half.

- [x] B.6 *(**deferred** 2026-09-13 by B.8, and sharper than when it was written: gap junctions are frozen, and whether the wiring advantage blocks V.1 and V.3 measured survives or grows when the electrical synapses are also plastic only became askable once an advantage existed. It belongs after a rule reads the wiring, or as a PPO arm on the block-V cells.)* SHOULD: learnable-gap-junction ablation on the *C. elegans* L4 substrate (D4).

- [x] B.7 *(**deferred** 2026-09-13 by B.8 — real value and independent of the rule's failure, but a behavioural-curve programme measured in weeks whose forgetting half depends on the MAY slow-memory chain that was never started. Phase 8 material, not 7a-ii closure.)* SHOULD: co-primary biological validation — dopamine-gated forgetting + Leifer navigation re-weighting, **sign/shape-level** (behavioural-curve machinery; the forgetting target's full reproduction depends on the MAY slow-memory chain and is reported as such).
  **I. The instrument** *(added 2026-09-09 — see the roadmap's second reframing paragraph. The three-factor rule has never learned this task on any substrate, the MLP yardstick included, so every rung above has asked whether the wiring is legible to a rule not shown to learn. Block I gates **new** B work — B.2, B.3, B.5 and the B.8 decision — until I.0 and I.1 are answered; it does not stop B.4b, whose runs were registered and launched before it and complete as registered. Its identifiers are "I.n" because 7b below already uses "C.n".)*

- [x] I.0 **Positive control** (`add-l4-rule-positive-control`, 2026-09-10): **`fail`, control valid.** Does the rule as implemented learn a minimal task at all — a contextual bandit or a two-unit toy where reward-modulated Hebbian learning is known to work — from random weights to a competent level? Registered before it ran, with the pass rule fixed. **Result:** on a one-step contextual association with both bounds in closed form, the analytic reference closes 99.9% of the floor-to-optimum gap on 8/8 seeds and the unmodulated floor arm does not pass — so the control is valid — while the three-factor arm fails at every rate across two orders of magnitude and ends **below the cue-blind floor** (≈ −0.75 against −0.69). The diagnosis is decisive: a live trace and a well-behaved modulator with a **gradient alignment of +0.031 mean and +0.009 median** (both recorded by the harness), so the update is nearly orthogonal to the policy gradient. **The rule as implemented is not a policy-gradient estimator.** Consequences as registered: the seven results in 040–046 are reframed as characterising a non-learner, I.1 is the critical path, and no substrate rung runs until it lands. Records under `supporting/048-l4-rule-positive-control/`; written up as [Logbook 048](../../../docs/experiments/logbooks/048-l4-rule-positive-control.md).

- [x] I.1 **Eligibility with the noise inside it** (`add-l4-node-perturbation`, 2026-09-10): **PASSES the positive control.** Each plastic unit's PRE-activation is perturbed by `N(0, σ²)` — on the connectome at every settling step — and the eligibility carries `pre ⊗ ξ` rather than `pre ⊗ post`. At σ = 0.2 the variant reaches **−0.196 against a floor of −0.6909 and an optimum of −0.1353, on 8/8 seeds** (per-seed −0.184 to −0.213), where the old rule sits near −0.75 *below* the floor. The control is valid (reference passes, floor does not) and the registered "passed by some other route" branch does not fire: performance and gradient alignment rise together across the grid (−0.777/+0.027, −0.477/+0.118, −0.196/+0.263), the passing arm's alignment being **+0.263** against the old rule's +0.009. Nothing but the eligibility changed — same modulator, scaling, bound and recipe — so this confirms and repairs Logbook 048's diagnosis. **Clone assay run 2026-09-10** (`add-l4-perturbation-clone-assay`): the variant **fails** it (mean 12.0 against the committed frozen clone's 38.7, 1/8 within hold) — but the frozen-perturbation control, registered by spec review while the arm was running, shows the rule is not what failed. With weights untouched (cosine 1.00) that control scores **8.9**, already 9.1 in its first quarter: **σ = 0.2 perturbation alone takes a competent policy from 38.7 to 8.9**, and the plastic arm's 12.0 is *above* that baseline. The assay at this σ could not test retention because no competent policy survived to be retained. The finding is the tension: the σ that makes the rule learn is the σ that makes a competent policy unrunnable, and σ = 0.05 reached 7/8 above the control's floor without clearing its learning bar. **The panel stays gated**; σ-annealing was the obvious untested next mechanism and was run as I.1b below, where it failed the control. Records under [`supporting/050-l4-perturbation-clone-assay/`](../../../docs/experiments/logbooks/supporting/050-l4-perturbation-clone-assay/details.md). Records under [`supporting/049-l4-node-perturbation/`](../../../docs/experiments/logbooks/supporting/049-l4-node-perturbation/details.md). *(Original scope:* A three-factor variant whose eligibility is the deviation of post-synaptic activity from its mean times pre-synaptic activity — node perturbation on the units, or the exploration noise propagated into the trace — so the rule is a policy-gradient estimator rather than reinforced correlation. Cleared on the **MLP yardstick first**: the yardstick must learn, or the variant is not an instrument either. Then the clone assay. Only then any connectome arm.\*)

- [x] I.1b **Annealing the perturbation scale** (`add-l4-sigma-annealing`, 2026-09-10): **`fail`, control valid. The clone assay did not run.** I.1's tension is a statement about a *constant* scale — σ = 0.2 learns (89% of the gap, 8/8 seeds) and the frozen control shows the same σ takes a competent policy from 38.7 to 8.9 with no weight written — so the registered mechanism was a geometric decay, σ₀ = 0.2 → σ_final = 0.02 over the first half of the budget, with trace normalisation on so the decay does not also cut the effective rate (the trace carries the perturbation, so the update's magnitude is linear in σ). **The schedule fails the positive control**: mean −0.4795 against a −0.4131 threshold, **38.1% of the floor-to-optimum gap where the constant σ = 0.2 arm closes 89.0%**, landing on the constant σ = 0.05 arm's 38.4%; 7/8 seeds above floor, so the seed clause is met and the mean clause is not (seed 6 at −1.024 decides it; median −0.4004, and no seed is dropped). **The reason is in the schedule's shape**: a geometric decay puts σ below 0.05 at trial 6,021 of 20,000, so **70% of the run sits below a scale the grid had already shown cannot clear the bar** — it annealed *through* the learning phase rather than after it. The registered alignment split locates the failure: **+0.1756 over the decay against +0.0510 at the floor**, so the estimator was aimed while the scale was large and the schedule did not break it. As registered the failure stops the sequence and is reported as a property of *this* schedule, not re-tuned; the design named this outcome in advance and how to read it — a constraint on `E` relative to the task's horizon rather than a refutation of annealing. The assay arms, configs and the harness's binned trajectory are built and tested but **not run**. Records under [`supporting/051-l4-sigma-annealing/`](../../../docs/experiments/logbooks/supporting/051-l4-sigma-annealing/details.md).

- [x] I.1c **step 0 — the endpoints, perturbation off** (`add-l4-endpoint-evaluation`, 2026-09-11): **`fail`, integrity clean.** Two independent critical reviews of a proposed low-σ programme converged on an experiment it had skipped and that was cheaper than any of its gates: every score the clone assay gave the node-perturbation arm was taken *while its own perturbation ran*, and nothing had run its endpoint weights clean. The arm is the comparator's config with one key changed, plus a load-integrity check — frozen updates mean a run's final weights are the ones it loaded, so its cosine to the clone must reproduce the assay's recorded value; all eight reproduced to three decimals, so these are the endpoints. **Mean 20.6 against the committed 38.7, 2/8 within hold — it fails**, and the verdict licenses the low-σ programme rather than the assay amendment. Removing the perturbation is worth **+8.6** over the same weights' 12.0 under it: the tax is real and does not close the 18.1-point shortfall. **The effect is bimodal and that is the finding**: six seeds degraded (worst −58.5), two improved (+29.4, +14.3), **seed 2 reaching 73.4 from a clone of 44.0 — the highest full-clear rate this substrate has produced in the phase**, stable at 71.0 over its first 500 episodes on a frozen policy. Mean 20.6, **median 9.2**, and a paired one-sided Wilcoxon against each seed's own clone gives **p = 0.074**, so this panel does not establish degradation at 5%. Nothing measured predicts the split (delta vs cosine +0.08; vs clone quality −0.47 — the best clone was damaged worst). So "the rule rewrites a competent policy into a worse one" is false as a general statement, and **I.2's premise is now demonstrated rather than argued**. Records under [`supporting/052-l4-endpoint-evaluation/`](../../../docs/experiments/logbooks/supporting/052-l4-endpoint-evaluation/details.md).

- [x] I.2 **A statistic and a metric matched to the outcome's shape** (`add-l4-mixture-statistic`, 2026-09-11). The family carries one member per component of the mixture — competent-fraction discordance at the committed 20.0 (panel 3's registered secondary R2, promoted; the threshold is panel 2's) and the level among each arm's **own** competent seeds (the contrast 042 named and declined to register) — beside the all-seeds Wilcoxon, retained so a re-read stays comparable with the record. Both directions, BH-FDR, an outcome map naming every cell. The graded metric needed no new measurement: every committed panel CSV already carries plateau-tail mean foods. **Three structural corrections during the build, each recorded**: the level contrast is per arm, because pairing over the union of competent seeds lets a frequency difference masquerade as a level one and would make `frequency_only` unreachable; its p-value comes from a pooled-label permutation null, because resampling each arm as observed centres the draws on the observed difference and so measures where the effect is rather than how often chance produces one (an earlier build used those draws and reported two contrasts as `level_only`; under the correct null, enumerated exactly rather than sampled so the result depends only on the two multisets, they are p = 0.1333 and 0.0536 and the finding does not survive); and the registered split criterion was **demoted to descriptive** after simulation showed it firing on 81–100% of draws from a null where both arms come from one bimodal law, so a doubly-non-significant panel is `no_effect` whatever its spread and `mixed_response` needs the two contrasts significant against each other. **The re-read of 17 contrasts across 9 committed tables promotes none of them**: 14 read `no_effect` and 3 `degrades`. The descriptive level differences on the Hebbian wiring comparison are large and consistent (+25.0, +22.7, +12.9) and none is significant against a pooled-label null, because those contrasts have two to five competent seeds an arm — below the combinatorial floor where any distribution-free test can resolve a difference after correction. **The wiring hypothesis stays closed and the re-read adds nothing toward reopening it**, and the committed record's negative reading survives a statistic matched to the outcome's shape rather than being an artefact of the wrong test. 052 re-reads `no_effect` against its registered `fail`, so that panel establishes nothing either way at n = 8. Every committed verdict stands; the re-read reproduces panel 3's committed R1 and R2 exactly. Records under [`supporting/053-l4-mixture-statistic/`](../../../docs/experiments/logbooks/supporting/053-l4-mixture-statistic/details.md).

- [x] I.3 **The unexamined knobs** (`add-l4-instrument-knobs`, 2026-09-11): **the eligibility horizon is the limiting setting, and raising it recovers the rule.** The registered platform was not usable — on disjoint seeds the MLP yardstick under I.1's rule ends *below its own frozen control*, with no learning signal to vary a knob against — and the horizon was unmeasurable on the undelayed control, which resets the trace every trial (`trace_decay` 0.0/0.9/0.99 identical to four decimals). So the control gained a **delay**: the scored action is taken while the cue is visible and the reward arrives `D` steps later, with `D = 0` bit-identical to the committed arm. What a delay imposes is **dilution, not decay** — the credited step is one term among `D + 1`, and a pure scalar decay would be divided out by the recipe's trace normalisation, which a zero-filler delay demonstrated by reading identically at every length. **Result at the pinned `trace_decay 0.9`**: 89.0% of the floor-to-optimum gap at `D = 0`, 71.0% at 5, 44.8% at 10, and **−6.8% at 20 — below the cue-blind floor**. At `0.99` the same `D = 20` cell closes **45.3%**, a 52-point swing from one setting; `0.999` adds little, so the useful range is bounded. The nominal credit ratio — the decay weight on the scored step, not a measured tensor share — tracks it at Spearman +0.989, which is a correlation and not a threshold: four cells fall below the registered bar and their ratios reach 0.051. **This is the first mechanism in the phase that predicts the central puzzle rather than describing it** — the rule's only success is a one-step task and every multi-step task has failed, at 244 to 2400 steps per episode, orders beyond where the pinned decay already falls below the floor. The other two knobs are near-neutral: homeostasis off closes 95.2% against on at 89.0% (both pass), and the exploration noise is near-best at the pinned 0.37 (0.22 slightly better at 92.3%, 1.0 fails at 48.4%, reproducing from the other side what the panels found when they pinned it). Records under [`supporting/054-l4-instrument-knobs/`](../../../docs/experiments/logbooks/supporting/054-l4-instrument-knobs/details.md).

- [x] I.3b **The horizon on a multi-step task** (`add-l4-horizon-multistep`, 2026-09-11): **`does_not_transfer`.** I.3 found the eligibility horizon limiting on the one-step control, and **every result in this phase ran at the pinned 0.9** — no panel config sets `trace_decay` — on episodes of 244 to 2400 steps, so the mechanism had never been tested where it would matter. Six arms on the MLP yardstick (`trace_decay` ∈ {0.9, 0.99, 0.999}, each with its **own** frozen control at the same horizon), 8 seeds, 3000 episodes, scored on plateau-tail mean foods with I.2's graded family. **The learning arm is far worse than doing nothing at every horizon, and raising the horizon makes it worse**: 0.393 → 0.148 → 0.144 foods against a frozen control at 2.233, below its control on 6, 8 and 7 of 8 seeds. The registered 0.5-foods minimum never came into play; there is no improvement to detect. **The drift measurement says which failure this is** — relative weight distance from the frozen control is 1.28–1.31 at every horizon, so the weights move further than their own norm: not a rule starved of credit and sitting still, but one writing a great deal in a direction that makes the policy worse, with the horizon barely changing how much. An internal check passed: the three frozen controls return identical values, as they must, since `trace_decay` cannot reach behaviour when nothing is written. **The horizon suspicion is closed**, and the connectome was deliberately not spent on it — ten hours a run against eight minutes — which is what this result vindicates. Records under [`supporting/055-l4-horizon-multistep/`](../../../docs/experiments/logbooks/supporting/055-l4-horizon-multistep/details.md).

- [x] I.4 **Re-read the ladder** (`add-l4-ladder-reread`, 2026-09-12): **no committed verdict changed, and the obvious re-read is wrong.** With I.0–I.3b answered, the tempting conclusion is that the phase's negatives are artefacts of a broken instrument. Classified one registered contrast at a time — **32 across eight logbooks** (040–047; I.4 was registered for 040–046 and 047 postdates it), each carrying its committed value, its committed result and **which rule was running when it was measured** — that reading covers **ten**. **Sixteen are substrate findings block I does not reach**: four ran under no rule (the 64-seed frozen prior sweeps, the frozen clones) and twelve under the **unmodulated, reward-free Hebbian** rule, which I.0 never tested and which 040 recorded settling at **78.3%, 64.4% and 67.3%** on individual seeds with no reward at all — a mechanism shown to reach competent behaviour on the task is not one that could not have learned. **Five are about neither**, their premise never established by any optimiser: [034](../../../docs/experiments/logbooks/034-connectome-structure-controls.md) ran the same wiring contrast under PPO weight search in Phase 6a and found the wirings indistinguishable (**−3.28**, CI[−8.56, +1.61], q = 0.770, the null nominally higher, and no advantage in learning efficiency either), and 043's low-noise PPO arms put the rewired null **ahead by 12.6 on 0 of 8** — so neither tested regime found a confirmed wild-type advantage, and those nulls are uninformative about both the wiring and the instrument, a classification a repaired rule would not have changed. **One, W6, is about PPO** and carries a correction issued on the record: the earlier reading that PPO also destroys competent policies is **withdrawn**, since the two warm starts are different starts (the rule from the 38.7 chemical-weights clone, PPO from the 73.7 full-parameter clone) and what is comparable is what each optimiser does without a clone — PPO **68.5** from random weights against the rule's **17.8**, below its own unmodulated floor. **041, 042, 044 and 046 survive whole**; 040, 043 and 047 split; 045 survives as an instrument finding with its scope narrowed to what braking a non-estimator does. The registration expected the premise category to hold most of the record; it holds **5 of 32**, because the registered three kinds had no place for the reward-free Hebbian arms, and the refinement is recorded rather than the expectation dropped. **For B.8**: GO is unreachable as written — its clause requires the receptor-gated neuromodulator stack (B.3), never built and queued behind block I; 7b's gate is not merely unmet but has no demonstration under any optimiser; the STOP clause named this outcome in advance (the diagnosis itself is the Phase 7 deliverable), and what separates STOP from SPLIT is B.8's own decision. The **low-σ programme licensed by 052 is deferred, not retired**, behind a task the repaired rule can be shown to learn, since 055 shows it writing 1.28–1.31 times its own weight norm in a worsening direction at σ = 0.2. Written up as [Logbook 056](../../../docs/experiments/logbooks/056-l4-ladder-reread.md), with the classification table and per-result reasoning under [`supporting/056-l4-ladder-reread/`](../../../docs/experiments/logbooks/supporting/056-l4-ladder-reread/details.md).

  **V. The wiring premise** *(added 2026-09-12 after [Logbook 056](../../../docs/experiments/logbooks/056-l4-ladder-reread.md). Block I established what the phase's negatives are evidence about, and five registered contrasts turned out to rest on a premise no tested optimiser had demonstrated: that learning finds a wild-type advantage. Every one of them, and every wiring contrast in the project, had run on the integrated C3 cell. Block V tests the premise on cells matched to behaviours the animal performs, under PPO, before the shipment decision inherits a reading.)*

- [x] V.1 **The wiring contrast on a behaviour the circuit is wired for** (`add-wiring-premise-contrast`, 2026-09-12): **`specific_wiring_efficiency` — the first positive wiring result in the project.** On a klinotaxis foraging cell under lethal thermal pressure, under PPO, the wild-type connectome reaches competence **~35% sooner** than its degree-preserving rewired null — **396 episodes against 613**, pooled over **64 paired seeds**, all four efficiency metrics at q ≤ 0.001 with 43–48 of 64 seeds. **Replicated**: panel 1 (16 seeds) +46.4%, panel 2 (16) +32.6% without significance, panel 3 (32, registered before it ran) **+31.8% at q = 0.012–0.027** — a winner's curse in which the first panel drew high and the independent panels agree, with a Mann-Whitney between panels at p = 0.318 detecting no between-panel difference — a failure to detect one rather than evidence that none exists. **Both controls hold at 64 seeds**: both wirings learn (gates 64/64, q = 0.000) and **the untrained prior is indistinguishable** (−0.17, q = 0.735), the third confirmation after 041's +3.3 and 044's +0.77 — so the advantage is created by learning, not inherited from the graph. **Two bounds are part of the claim**: it is **speed, not performance** — the endpoint is saturated and the null is nominally ahead there (−0.47 foods of 20, q = 0.004), consistent with 034, 043 and this panel's own food-only cell, which measured the axis on which this substrate has no advantage — and it is **PPO, not a local rule**, so 7b's gate as written is still unmet while its premise is now supported. **The pilot on disjoint seeds 101–104 earned its place**: both cells sat at the ceiling (food-only at **100.00% on both wirings**, contrast exactly 0.00, frozen random weights already at 57.60%), the registered remedy (`target_foods_to_collect` 10 → 20) was applied once as registered and did not unsaturate them, and the primary moved to the **efficiency axis** under a dated amendment — 034's own committed harness applied unchanged, which reads flat and nominally negative on the food-only cell and so does not manufacture positives. A registration defect is recorded rather than worked around: the replication's outcomes conflated effect size with significance, and panel 3's were rewritten to separate them. 320 runs, all succeeded; no committed verdict changes. Records under [`supporting/057-wiring-premise-contrast/`](../../../docs/experiments/logbooks/supporting/057-wiring-premise-contrast/launch.md); written up as [Logbook 057](../../../docs/experiments/logbooks/057-wiring-premise-contrast.md).

- [x] V.2 **Why the seeds differ** (probe, 2026-09-12; [record](../../../docs/experiments/logbooks/supporting/057-wiring-premise-contrast/probe-v2.md)): **nothing predicts, and the reason closes a hypothesis.** All 64 rewirings regenerated from their seeds and scored on four properties fixed before looking, Spearman against that seed's time-to-competence, BH across the four. **None predicts** (P1 ρ = +0.053, P4 ρ = −0.104, both q = 1.000), and **three cannot in principle** because they barely vary across degree-preserving rewirings. Two findings fall out. **The settling-depth hypothesis is false**: it supposed a rewiring could push the thermosensory route beyond the four hops the substrate settles for, and none does — all **39 of 39** motor-readout neurons are reachable from AFD within four hops in *every* graph, the wild type included, so reachability is saturated in a 302-neuron graph with 3,709 edges. **And the advantage is not a shortcut**: the wild type's shortest AFD → motor path is **3 hops against every rewiring's 1–2**, with a longer characteristic path length (3.086 against 2.576–2.598) — the rewirings are better connected by these measures and learn slower. What is left are the hypotheses these measures cannot see: which neurons sit on the route, how degrees pair across edges, and the motif structure a degree-preserving swap destroys. The probe cannot strengthen 057's result and does not touch it; it removes one candidate mechanism and sharpens V.3.

- [x] V.3 **Difficulty or temperature** (`add-wiring-premise-difficulty`, 2026-09-13): **`specific_wiring_efficiency` — difficulty is sufficient and the thermosensory projection is not necessary.** 057 left two readings of its positive result; the cells behind it differ in three respects and **only two bind** — the thermal cell's episodes end 14.7% `health_depleted`, 12.4% `max_steps` and **0.6% `starved`**, so satiety is nearly inert and the separable non-temperature factor is the episode budget. This arm raises difficulty by the budget alone, no temperature and no projection. At a **calibrated** `max_steps: 350`, over **32 paired seeds**, the wild type reaches competence in **892 episodes against 1165 — +23.5%**, above the registered 20% minimum, three of four efficiency metrics significant (q = 0.003, 0.017, 0.029; 21–26/32), both gates **32/32**, prior indistinguishable (−0.01, q = 0.841). **So 057's claim generalises** from "a foraging cell under thermal pressure" to "a foraging cell hard enough to discriminate" — not to any hard task, only hard foraging cells of this family. **The calibration was load-bearing**: the inherited `max_steps: 500` could not bind (plateau-tail episodes complete in a mean of 310 steps), and the declared grid {150, 250, 350} found the band one step wide — at 250 learning was plainly happening while **no arm crossed the 30% threshold the primary metric needs**, so the contrast would have been censored for every seed and read as a clean null. **Three qualifications recorded with the result**: the fourth efficiency metric (time to an arm's *own* plateau) does **not** move (−23.7, 15/32) where all four moved on the thermal cell; the peak contrast is significant at q = 0.001 but **+4.05 is under its registered 5-point minimum**, so this stays a speed claim; and because `rewire_seed` derives from the run seed, this arm's **32 rewirings are exactly those the thermal panels used** — the cells are independent in task and initialisation, not in rewiring, and a fresh-rewiring panel (seeds 65+) is what would close that. 176 runs, all succeeded; no committed verdict changed. Records under [`supporting/058-wiring-premise-difficulty/`](../../../docs/experiments/logbooks/supporting/058-wiring-premise-difficulty/launch.md); written up as [Logbook 058](../../../docs/experiments/logbooks/058-wiring-premise-difficulty.md).

- [x] V.4 **A fresh-rewiring panel** (`add-wiring-fresh-rewiring`, 2026-09-16): **`specific_wiring_efficiency` on both cells — the caveat closes, and both estimates come in above their comparators.** Every prior wiring result drew its rewired graphs from run seeds 1–64 and V.3 reused 1–32 exactly, so the project's two positives **shared their nulls** rather than corroborating each other on independent ones. This re-runs both panels on seeds **65–96**, fresh to both, with **no new configs** — all eight arms are the ones V.1 and V.3 ran, asserted byte-unchanged since their panels by test (thermal against `431a4689`, hard350 against `48ba778c`). **256 of 256 runs succeeded.** The wild type reaches competence **+55.3%** sooner than its degree-preserving rewired null on the thermal cell (**309.22 against 691.50** episodes, ahead on **24 of 32** seeds) where V.1 measured +35.4%, and **+40.1%** sooner on `hard350` (**827.75 against 1380.88**, **28 of 32**) where V.3 measured +23.5% — **all four efficiency metrics at q = 0.000 on both cells**, against the registered ≥ 20% minimum. **Nothing is censored**: crossing rates are 100% in all four arms. Both gates fire **32/32** and **both untrained priors are null** (+0.640, q = 0.164; +0.192, q = 0.327) — the check that the wirings are not separated before an update, which matters here because a fresh-seed panel varies initialisation along with the graph. The thermal peak axis returned **`saturated`** (96.14%/94.14%, above the 90% ceiling), the registered reason the efficiency axis is primary there, now confirmed outside the seeds it was decided on; **the `hard350` peak axis improved on V.3**, returning `specific_wiring` at **+8.267** points where V.3 got `below_min_effect` at +4.05. **The instrument did not change and a test asserts it**: `wiring_premise.py` and `connectome_structure_efficiency.py` byte-identical to `main`, the new driver re-declaring none of the minimum, the verdicts, the crossing floor or the arm mapping; both comparators were re-scored through it first and reproduce field for field (V.3: 892.03/1165.34, +23.5%, 21/32; V.1: +217.11, +35.4%, 44/64). **What it does not do is separate rewiring from initialisation** — `rewire_seed` stays unset, as V.1 and V.3 ran it, so a fresh seed moves graph, task draw and initial weights together. What is excluded is that the committed figures rode on a particular set of shuffles; that initialisation contributes is **not** excluded, and isolating the graph needs `rewire_seed` pinned, which nothing has registered. **No mechanism is claimed** — V.2 found no graph property predicting learning time across 64 rewirings. **May not be cited as** evidence that the wiring helps a learner the animal could host: this is PPO, and 034, R.1c, R.2 and L.0 stay negative. Two reporting-driver defects are recorded, neither affecting a number: a verdict printed off the seed count on the 4-seed pilot (second appearance of that class, now a test), and a branch map keyed on the peak axis's `specific_wiring` that reported the result as "unrecognised" — the guarding test had the harness's vocabulary hand-copied into it, and deriving it from source surfaced three unhandled verdicts. Records under [`supporting/065-wiring-fresh-rewiring/`](../../../docs/experiments/logbooks/supporting/065-wiring-fresh-rewiring/launch.md); written up as [Logbook 065](../../../docs/experiments/logbooks/065-wiring-fresh-rewiring.md).

- [x] B.8 **The 7a shipment decision** (`add-7a-shipment-decision`, 2026-09-13): **SPLIT-shipment**, taken under the roadmap's own clause. **7a ships two results.** A **systematic negative with a diagnosed cause** — the three-factor rule was not a policy-gradient estimator (alignment +0.009, below the cue-blind floor on a task its analytic reference solves to 99.9%), the eligibility that repairs it reaches +0.263 and passes that control, and the repair still fails on every multi-step task writing 1.28–1.31× its own weight norm in a worsening direction, with six further interventions each failing on the record. And a **positive wiring result on learning speed** — **+35.4%** off time-to-competence on a foraging cell under thermal pressure (64 paired seeds, replicated) and **+23.5%** with temperature removed (32 seeds), both learning gates passing on every seed and the untrained prior indistinguishable in three independent measurements, so the advantage is **created by learning, not inherited from the graph**. **GO was unreachable on its own clause** — it names B.3, whose condition depends on B.5, whose gate four mechanisms failed — and **STOP would have overstated the record in the opposite direction**, since the substrate holds a competent policy (73.7%) and PPO solves the task (68.5%) while cross-species was never attempted. Both are recorded with why rather than reached by elimination. **What ships may not be cited as** a local rule reading the wiring, a performance advantage, a result about any hard task, or evidence about the thermosensory pathway. **7b's gate stands as written**: its letter (a *local rule* beating the null) is unmet, its rationale (a wiring-indifferent learner measures nothing) is satisfied, and the divergence is recorded as a decision rather than resolved by interpretation. **The forward programme is rule families, bounded**: the 1/N scale test, then e-prop cleared in the protocol's order, with **two stopping conditions fixed in advance** — failing its own frozen floor on the hard-food cell by the registered gate, or an implementation passing 3 active weeks — on either of which 7b proceeds under PPO with the biological-plausibility claim given up. Phase 7 sits at **7a complete / 7b pending** and is **not** COMPLETE. Written up as [Logbook 059](../../../docs/experiments/logbooks/059-7a-shipment.md).

  **R. The rule-family programme** *(added 2026-09-13 by [Logbook 059](../../../docs/experiments/logbooks/059-7a-shipment.md)'s SPLIT-shipment decision. Block V gave the rule programme the positive control it never had: the wild-type wiring reaches competence sooner than its rewired null under PPO, with a size, so a candidate rule that learns the block-V cells can now be held to whether it **also** shows the advantage. Bounded by two stopping conditions fixed in advance — failing the registered frozen-floor gate on the hard-food cell, or an implementation passing 3 active weeks — on either of which 7b proceeds under PPO with the biological-plausibility claim given up.)*

- [x] R.1 **The perturbation dimension** (`add-l4-perturbation-scale`, 2026-09-13): **`mixed`, and the mixture is the finding — the rule solves a multi-step foraging cell at eight perturbed units and collapses to 3.0% full clear at 128.** The rule's one success and every failure differed in a dimension **no experiment had varied**: the one-step control it passes perturbs **8** units, every failing MLP yardstick arm ran **128**, and the connectome perturbs **302** neurons at each of four settling steps — **1208 draws per scored decision**. Two sweeps, the arithmetic tested where the rule works before being read off where it fails. **S1, on the one-step control, says the dimension costs almost nothing**: every width 8→128 passes the control's own registered rule on 8/8 seeds, the analytic reference reaches 0.999 of the gap at every width so none is void, **0 of 48 seeds censored**, and time-to-criterion **falls** as the dimension grows — slope **−0.216**, bootstrap CI **[−0.331, −0.092]**, against a prediction of **+1.0** and a registered bar of +0.5. A **dated amendment** added the yardstick's exact arrangement (two plastic layers of 64, 128 units) after that left depth the only remaining shape difference: it passes at 0.853 of the gap and reaches criterion **fastest of every cell** (median 958 trials against 2478 at 128×1), so **neither the dimension nor the architecture changes whether the control passes** — the registered pass/fail capability is unaffected across the whole grid. Both do have measurable effects on *how long it takes*: the slope above, and a 958-trial median at 64×2 against 2478 at 128×1. **S2, on the calibrated hard-food cell, says the same dimension is decisive**: at **8 units** the rule reaches **19.64 foods of 20 and 90.7% full clear against PPO's 19.69 and 87.6%**, from a frozen control at 1.69 — then **19.64 → 18.83 → 15.72 → 8.34 → 4.23** foods and **90.7% → 79.2% → 53.6% → 12.9% → 3.0%** full clear across the grid, rank correlation **−1.000**, with relative drift from each width's own frozen control rising **0.91 → 1.07 → 1.27 → 1.38 → 1.39**. So the dimension is **associated with** the degradation, and I.3b's **1.28–1.31** drift need not be read as a signal pointing the wrong way — the same magnitude of writing is productive at low N and destructive at high N. **The attribution is not isolated**: I.3b ran at 128 units *and* 2400 steps against this cell's 350, so dimension and horizon are not separated here, and on the connectome a third axis — the settling-step multiplier, 4 draws per unit per decision — is untested entirely. The mechanism claim is therefore **qualified pending R.1c or an equivalent settling-step control**. The capability arm passes at **every** width (19.69–19.94 foods, 87.6–97.0% clear), so no width is capacity-limited and the declared one-layer alternative was not needed. **Neither pre-registered row fits** — `scale_limited` required a positive slope *and* 128 failing the control, `not_scale_limited` required no rescue — and the registration fixed the treatment in advance, so both halves are stated rather than resolved toward the nearer verdict. **The constraint is an interaction between dimension and horizon**, not an effect of either alone. Dilution of per-unit credit across units and across decisions is the candidate mechanism and would produce exactly this pattern, but a **product law is more than this design supports**: the two axes were varied on different platforms — the dimension here, the horizon across I.3 and I.3b — never factorially at matched settings, and a matched sweep (the same widths at two or more horizons on one platform) is what would distinguish a product from any other interaction. So qualified, the interaction spans I.3's horizon finding and this one, which neither does alone. Two instrument defects the pilot exposed were fixed before the campaign, both of the shape that turns a sample size into a finding: drift took its seeds from a module constant and read **no pair at all** on the pilot's disjoint 101–104, and the capability gate reported **`fails`** where an exact one-sided test cannot reach the level at four pairs (floor 0.0625) — it now reports `underpowered`, and an undecided width is **neither a null nor uninterpretable**. **May not be cited as** a result about the connectome (1208 draws sits far beyond this grid's failing end, and read plainly the result predicts failure there), a result about any other cell (the 2400-step C3 cell still fails at 128), a claim the rule is a good gradient estimator (alignment +0.263, unchanged), or a measurement of the 1/N exponent. **No committed verdict changed** — 059's negative recorded what had been measured, every arm it summarised ran at 128 or 302 units, and any re-read is a **new registration**. Written up as [Logbook 060](../../../docs/experiments/logbooks/060-l4-perturbation-scale.md).

**Sequencing, stated explicitly (2026-09-13).** R.1 makes the wiring contrast runnable *in principle*
and **not yet in practice**, because the two facts collide: the contrast needs the **connectome**
(rewiring is a connectome operation — `rewire_degree_preserving` acts on a connectome, and an MLP has
no wiring to rewire), while the connectome cannot be run below **302 units × 4 settling steps**, an
order of magnitude past the 128 units at which R.1 measured 3.0% full clear. **So R.1c gates R.1b**,
and R.1b gates the letter of 7b's gate. The order is **R.1c → R.1b → (7b's gate reconsidered)**, with
**R.2b** available in parallel, since it needs no connectome, and **R.2's first two stages** likewise — e-prop is cleared in the protocol's order, and its one-step control and hard-food cell come before any wiring contrast. **R.2's third stage is not parallel**: the wiring contrast against the registered 20% bar is a connectome operation and depends on the same thing R.1b does, a rule that reaches competence there.

**Update, 2026-09-14 — R.1c resolved `not_reducible`, and the chain did not shorten, it changed
shape.** The mechanism now exists, so R.1b is no longer blocked on a *missing capability*: it is blocked
on a rule that does not reach competence on the connectome at any perturbation dimension from 1208
draws per decision down to 39, with credited-synapse drift invariant at 1.37–1.38× throughout. The
order is therefore **R.1d (or R.2) → a rule that learns the cell → R.1b → (7b's gate reconsidered)**,
and no further work on the perturbation set shortens it. R.1d then resolved the same day
(`readout_helps_but_not_enough`, below) without producing a competent rule, so the head of that chain
became **R.2**.

**Update, 2026-09-15 — R.2 produced a competent rule and the chain did not open, it forked.** Two arms
reach competence on the connectome, and the better of them has its chemical matrix **frozen**: every
arm that writes the wiring did worse than the readout-only control. So "a rule that learns the cell"
is satisfied and "a rule that writes the wiring to any benefit" is not, and those are different gates.
The substrate rungs — B.5, B.1, B.4, B.4b — ask the second and **stay gated**. R.1b's registered
time-to-competence form also asks the second and stays blocked; its **frozen-features** form asks a
question the readout-only arm can answer today and is the live continuation.

**Correction, 2026-09-13** (this entry first read "R.3 available immediately, since the block-V cells
are MLP-runnable" — wrong on both counts). **R.3's rungs are connectome rungs**, so R.1c gates their
*running* too: the block-V cells are `connectomeppo_*` configs; B.1 grounds synapse signs across the
302-neuron connectome; B.5's gate is the clone assay on a connectome policy; and **B.4b cannot run on
an MLP at all** — the substrate raises, because routing the modulator through aminergic wiring needs
neurons with transmitter identities and a dense layer has none. Only B.4 is rule-level enough to be
arguable, and its assay was the connectome clone. R.3's *re-registration documents* can be authored
without the connectome; none of the work they register can run without R.1c.
Nothing in R.1 shortens that chain, and reading R.1 as having opened 7b's gate would skip two
unbuilt steps.

- [x] R.1b **The wiring contrast under a working local rule** *(**closed-superseded** 2026-09-15 by D14: its registered form — a time-to-competence contrast under a rule that writes the wiring — has no rule to run it with, and its frozen-features form is **L.0** below, which is MUST for the Phase 7 close. Closed here so the successor carries it.)* *(registered by 060, not run; **still blocked after R.1c resolved `not_reducible` 2026-09-14** — the blocker is no longer a missing mechanism but a rule that does not reach competence on the connectome at any perturbation dimension, and **R.1d resolved 2026-09-14 without reaching competence either**, so the remaining gate on it is R.2)*: wild type against its degree-preserving rewired null **under this rule at 8 perturbed units**, on the block-V cell, against block V's registered **≥ 20%** minimum on time-to-competence. This is the contrast **7b's gate actually asks for** and the first time it has been runnable: the gate's letter requires a local rule beating the null, and until this runs the gate **stands as written**. **Blocked on a rule that reaches competence on the connectome** — not on a missing mechanism, which R.1c built: the substrate's rewiring is a connectome operation, and no perturbation dimension from 1208 draws per decision down to 39 produces an arm that becomes competent there, so block V's time-to-competence contrast has nothing to measure. R.1d did not produce such a rule either, and **R.2 resolved 2026-09-15 with a competent rule whose substrate is frozen** — every arm that writes the wiring did worse than the readout-only control, by 3.9 to 15.9 foods. So R.1b in its registered form, a *time-to-competence* contrast under a rule that writes the wiring, has **no rule to run it with**. It becomes runnable in a **changed form**: wild type against its degree-preserving rewired null as **frozen features**, under the readout-only arm, which asks whether the wild-type wiring supplies better fixed features to a small learned readout than a shuffle does. That is a clean question and arguably closer to "is the wiring legible to a learner" than the original; it is registered fresh in its own change, and the registered form stays blocked. The alternative is a statement of what an MLP-only wiring contrast could mean, which is not obvious — an MLP has no wiring to rewire.

- [x] R.1c **A reduced-perturbation-dimension mechanism for the connectome** (`add-l4-reduced-perturbation`, 2026-09-14): **`not_reducible` — the connectome's failure is not the perturbation dimension.** The mechanism is built and reusable: the per-unit perturbation is restrictable to a declared set, and because the eligibility is `pre ⊗ perturbation` a unit drawing nothing leaves **every synapse onto it** with a zero trace, so restricting the draw restricts what can be credited without touching the rule. Recon then found something sharper than a knob: the readout mean-pools **only the 39 VB/DB/VA/DA motor neurons**, so at settling step `s` a unit can influence the action only from within `depth − s` hops — and on the Cook 2019 graph at depth 4, **672 of 1208 draws per scored decision are causally connected while 536 (44.4%) cannot change the action at all and every one of them is credited**, with **25** units at four hops or more never able to contribute at any step. **Masking exactly those, and restricting further to 109 and 39 units, does not make the rule learn.** Ten arms, seeds 1–8, 3000 episodes, 80 runs at the calibrated σ 0.1: shifts −0.384 (`full`, 302 units), +0.094 (`causal`, 277), −0.126 (`hop1`, 109), **+0.601 (`motor`, 39, on 7/8 seeds)** and +0.435 (`motor_last`, 39 units but 39 draws), **none significant** (q 0.31–0.73) and **none clearing the ~1.6-foods binding minimum**; full clear never exceeds **0.08%** against PPO's **19.31 foods** on this exact cell over 32 seeds. **The campaign's durable finding is a mechanism**: credited synapses drift **1.37–1.38× their own norm at every set from 302 units down to 39** — the same signature I.3b measured on the MLP (1.28–1.31), reproduced across a 31-fold change in draws per decision — while **excluded synapses drift 0.013–0.016**, confirming at real scale the bench claim that the homeostatic rescale leaves them as jitter rather than decay. So the rule is **not starved of signal at any dimension**; it writes a great deal in a direction that does not help, and narrowing what it credits does not change that. **Two knobs calibrated on this substrate for the first time are both nulls**: σ (0.05/0.1/0.2 → 4.321/4.361/2.989 foods, so the carried 0.2 was costing 46% and the campaign ran at 0.1) and the action noise (`initial_log_std` −1.0/−0.5/0.0 → 4.361/4.056/4.154, the pinned value standing, with wider noise lifting the **frozen** arm and collapsing the gap — floor food bought by wandering, which the registered guard was written to catch). **Three independent axes, one ceiling**: the learning arm never leaves **2.4–4.4 foods of 20** across σ, action noise and a 31-fold dimension range, which is a structural limit rather than a hyperparameter one. **R.1b stays blocked and 7b's gate is untouched** — block V's contrast is on *time to competence* and no arm becomes competent, a gap between this change's own verdict condition and its stated consequence that was surfaced **before** the campaign rather than after. **Four corrections are on the record**: `causal`'s registered unit/synapse counts (302/3709 → 277/3538, before any arm ran); the σ calibration's stated reason (the frozen prior is action-noise-limited, not σ-limited, so the result stands for a different reason); the campaign's first launch at the **rejected** σ 0.2, caught at 32 of 80 and restarted, whose partial data supplies the cleanest argument for eight seeds over four (`full` went +0.61 on 4/4 pilot seeds to −0.112 on 4/8 registered); and a drift reader that found nothing for all 80 runs because it assumed the MLP checkpoint layout, whose repair is what made the split measurement possible. **May not be cited as** a result about the perturbation dimension in general (R.1 found it decisive on the MLP; together they say it does not transfer), as evidence the causal mask is worthless (it removes draws that provably cannot matter and remains the correct default), as a claim about the 2400-step C3 cell, or as a statement that the connectome cannot learn — PPO reaches 19.31 foods on it. Written up as [Logbook 061](../../../docs/experiments/logbooks/061-l4-reduced-perturbation.md).

- [x] R.1d **The frozen readout** (`add-l4-frozen-readout`, 2026-09-14): **`readout_helps_but_not_enough` — the readout is part of the limit, its SCALE does the work, and PPO's direction is the worst of three.** Under the rule the connectome writes **`w_chem` alone**, so the sensory projection, the action-noise scale and the motor readout are all frozen; R.1c eliminated the first two (PPO moves the projection 0.177 relative at cosine 0.985; the action noise was swept by hand and is null), leaving this one. Four readouts at R.1c's best operating point — the `motor` mask, σ 0.1, `initial_log_std: -1.0` — seeds 1–8, 3000 episodes, 48 new runs, all succeeded. **Substituting the readout more than doubles what the rule reaches, 3.751 → 9.639 foods of 20**, and all three substituted arms beat their own frozen control (**q 0.016–0.026**, 6–8 of 8 seeds) where the anatomical one does **not** (q 0.098). **So the readout is genuinely part of the limit.** But **none reaches competence** — best **5.75%** full clear against the committed 20% threshold, and against PPO's **18.945 foods** harvested on this exact cell at these seeds and this action scale — so it is a result and not a rescue: **R.1b stays blocked and 7b's gate is untouched**, since block V's contrast is on *time to competence* and that is undefined for every arm here. **The scale does the work, not the direction**: **+4.51 foods** from the norm alone with the anatomical direction held, against **−2.14** from PPO's direction at that norm, which points at the **readout-scale / action-noise interaction** — a 5.5× readout against a fixed exploration noise commits far harder — rather than at reading the motor classes better. **A fourth arm added after inspecting the harvested checkpoints, before any arm ran, changed the conclusion.** PPO's readout is **7.820 against the anatomical 1.414 (5.5×) at a cosine of −0.178**, so it *replaces* the prior rather than refining it, which left the registered three arms differing in norm and direction at once. With `anatomical_scaled` (anatomical direction at PPO's norm) the matched-norm ordering is **random 9.639 > anatomical 8.265 > PPO 6.123**: **PPO's own direction is the worst of the three** — the co-adaptation caveat registered in advance, now measured, since PPO's readout is tuned to PPO's `w_chem` while the rule always starts from a random one. `ppo` is accordingly **bimodal**, above ten foods on three seeds and near zero on three, so its mean is the least informative number in the table; `rotated`'s best seed reaches **17.3 of 20**. Without that arm this would have read as *PPO found a better readout*, the opposite of the ordering — and no registered ordering label fits what was measured, so the harness reports it as **`mixed`**: *any change of direction helps* would need both substituted directions above `anatomical_scaled`, and `ppo` sits below it. **Credited-synapse drift stays 1.38–1.42×** the weight's own norm across all four arms, against R.1c's **1.37–1.38** across every perturbation dimension from 302 units to 39, so the invariance now spans **two independent structural axes** — dimension and readout — which is the strongest remaining argument that the failure is in **credit assignment** rather than in the substrate's input or output maps. Two methodological notes: reusing R.1c's anatomical pair rather than re-running it was made conditional on a **load-path equivalence test**, which passed with all 16 topology tensors, the rule's baseline and the homeostatic norm targets **bit-identical**, saving 16 runs on evidence rather than argument; and every prepared checkpoint asserts before it is written that `w_chem`, `food_gains`, `log_std` and the wiring buffers come through untouched, because a wholesale load would change an axis R.1c pinned, make the arm a clone assay, and move the homeostatic norm targets the rule re-anchors from the weights it loads. **This could not have satisfied D1 whatever it returned** — a rule taking its readout from a gradient-trained run is not a biologically plausible local learner — and the harness carries that as a field rather than as prose. **May not be cited as** evidence PPO found a good readout (it found the worst of three at its own norm), a general result about readout direction (one norm, one mask, one cell, eight seeds), a claim that scale is all that matters (a random direction beat the anatomical one by +1.37 at matched norm), or a rescue. Written up as [Logbook 062](../../../docs/experiments/logbooks/062-l4-frozen-readout.md).

- [x] R.2 **e-prop** (`add-l4-eprop`, 2026-09-15): **`learns_without_the_substrate` — two arms reach competence on the connectome, the first in this programme, and the control says the wiring did not do it.** D1's named fallback: the eligibility is `eps_ij = Σ_s psi_j^(s+1)·h_i^(s)` from the settling dynamics, the sign comes from a broadcast learning signal `L_j = Σ_k B_jk g_k` folded in the moment the action exists, and **no perturbation is drawn**. Six learning arms and one shared frozen floor, seeds 1–16, 3000 episodes, **112 of 112 runs succeeded**. **`readout_only` reaches 17.570 foods of 20 at 52.61% full clear and `plastic_readout` 13.675 at 40.22%**, both against a shared floor of **2.353** and PPO's matched **18.945**, on **16/16 seeds at q = 0.000**. **But the better arm is the one whose chemical matrix is FROZEN.** `plastic_readout − readout_only` is **−3.895 foods (p 1.000)**, and every arm that writes the substrate sits below the control — −3.895, −4.162, −14.285, −14.589, −15.851 — while the control's own credited drift is exactly **0.00**, the check that the withholding happened. What learned this cell is an **8-parameter linear readout over four pooled motor-class means, on frozen recurrent features**, so the substrate's plasticity is not merely unnecessary here but **actively harmful**, and the **1.37–1.42×** credited drift invariant across R.1c, R.1d and now R.2 is what the cost of writing the wiring looks like. **059's third outcome was therefore split**, on the control's evidence and for the reason R.1c caught in itself — a verdict condition weaker than its consequence: `learns_the_cell` now requires a competent arm that **writes the substrate** and clears the readout-only control by the registered 1.0-food minimum. **No arm does**, so the gate is met **in letter and not in substance**. **The one-step control predicted the plausible arm's failure structurally, before the campaign**: feedback alignment needs the forward path to the output to align with B-transpose and a frozen readout cannot, so at 20,000 trials the broadcast arm's best is **−0.7031** against a cue-blind floor of −0.6909 frozen and reaches the optimum of **−0.1353 exactly on 8/8 seeds** plastic. That retired this change's own exclusion of a plastic readout, which had cited a collapse Logbook 040 measured **under the Hebbian rule**, where the post-synaptic factor is the output rather than the error; under e-prop the readout's eligibility is its **exact gradient**. The control held on its own terms (`analytic` −0.1361, `hebbian` −0.8753, both unchanged to five decimals) and both required expectations were met. **Three further findings**: `scalar` sits **below** its floor (−0.63 foods, 0.00% clear), so a dynamics trace with no per-unit sign is destructive rather than neutral; at matched reach the **true direction is worth +10.123 foods** (p 0.000) while reach at a matched source is worth **−0.304** (p 0.948), and the fourth cell of that design — true directions reaching all 302 units — is one **the mechanism forbids**; and the **registered hop-distance prediction failed to discriminate**, the rule writing nearly uniformly over graph distance (0.0103 at the motor pool against 0.0065 at units with no path to it, no monotone falloff). e-prop's learned readouts are **not PPO's**: norm 5.326 at cosine −0.04 to anatomical, against PPO's 7.820 at −0.178. **Three corrections on the record**: 4.361 was carried in four places as R.1c's `motor` arm level when it is the σ-calibration figure (the arm is 3.751/3.150 at +0.601); the symmetric projection had to be derived **live**, since the anatomical contrast overwrites the readout after construction; and the harness printed `does_not_learn — the programme stops` on a four-seed pilot, where the exact test cannot reach the gate at all (its smallest reachable p is 2⁻⁴), so a pilot now withholds the verdict. **May not be cited as** evidence that a local rule learns the connectome, a result about e-prop or feedback alignment in general, or a claim that the readout is all that matters anywhere — it stands in for the entire motor periphery, and this result is partly a measurement of that gap. Written up as [Logbook 063](../../../docs/experiments/logbooks/063-l4-eprop.md).

- [x] R.2b **A matched dimension × horizon sweep** *(**deferred** 2026-09-15 by D14: it would turn 060's mechanism sentence from "interaction" to "product" for **node perturbation**, a rule family R.2 closed. Cheap — the MLP cell runs in minutes — and worth doing if 060's wording is ever load-bearing for a write-up, but it is not Phase 7 closure work and no committed verdict depends on it.)* *(registered by 060, not run)*: R.1 varied the dimension on a one-step control and a 350-step cell; I.3 and I.3b varied the horizon at a single dimension. The two axes have **never** been crossed at matched settings, so the interaction is established while the **product law is not**. The same widths at two or more horizons on one platform is what would distinguish a product from any other interaction, and it is cheap — the MLP cell runs in minutes. Until it runs, every mechanism sentence in 060 stays at "interaction", not "product".

- [x] R.3 **Re-registration of the substrate rungs** *(**closed-superseded** 2026-09-15 by D14: its condition was "if the programme produces a rule that learns the hard-food cell", and R.2 produced one whose substrate is **frozen** — so B.5, B.1, B.4 and B.4b stay gated on a rule that writes the wiring to some benefit, which no longer exists to re-register them against. The ladder's successor rungs are **L.0–L.5** below, under the narrowed claim D14 restates. **Outcome: the rungs were not re-registered, and B.3 remains closed-unpaid** — its own condition is that B.5 can conclude, and B.5's gate is a rule that writes the wiring to some benefit, which R.2 established does not exist.)*

  *Superseded historical context, retained for the record and no longer live instruction.* 059 fixed the condition "*if the programme produces a rule that learns the hard-food cell, B.5, B.1, B.4 and B.4b become askable and are re-registered fresh on the block-V cells with the block-V bar*", and **R.1 met it as written on 2026-09-13** — which is why this item was opened. The plan recorded then was that each rung would be re-registered fresh rather than re-run under its old registration, that B.5's gate would move from the clone assay to the block-V bar, and that B.3 would become payable once B.5 could conclude. **None of that happened**: the condition's letter was met by a rule that solves a one-step control and no multi-step task, and the three campaigns after it (R.1c, R.1d, R.2) closed the write-the-wiring class entirely, so the rungs have no learner to be re-registered against. Block I's findings are about node perturbation specifically and were not revisited, which stands.

### The ladder, with an instrument (D14, 2026-09-15)

R.2 closed the rule programme and left the substrate ladder its first working instrument: `readout_only`, an 8-parameter readout learned by its own exact gradient on the frozen wiring, reaches competence on the connectome (17.570 foods, 52.61% full clear, 16/16 seeds). Every rung above was tested with a rule that could not learn, so "the wiring did not start to matter" was never separable from "nothing could have shown it". The plausibility claim under which these run is the narrowed one D14 restates — *a small local readout reads the wiring's fixed features* — and every result is a performance claim. In order, each in its own change with the rewired null and the frozen floor as controls. L.0 runs first and gates the rest: L.1 is promoted to MUST if it reads null (the width question a null raises), while L.4 and L.5 are gated the other way, on it reading **positive** — against a null there is nothing for a feature ablation to have changed:

- [x] L.0 **The frozen-features R.1b** (`add-l4-frozen-features`, 2026-09-16): **`wiring_is_inert_as_features` — 034's degree-statistics verdict now extends to a third learning regime.** The 2×2's primary contrast under the one plausible learner that reaches competence: wild type against its degree-preserving rewired null, both **frozen**, with `readout_only`'s 8-parameter readout learning by its own exact gradient — the connectome as a fixed feature map, where a degree-preserving rewiring changes that map and nothing else. On `hard350`, the cell V.3 ran this contrast on under PPO, over **32 paired seeds** at 3000 episodes, **128 of 128 runs succeeded**. **No significant advantage at the registered ≥ 20% bar** (q = 0.971 one-sided, block V's own instrument). **The contrast is interpretable rather than voided**: both wirings learn (**+14.801** and **+14.701** foods over their own frozen floors, q = 0.000 each), the untrained prior does **not** separate (−0.975, q = 0.217 two-sided), and `w_chem` drift reads **0.00** on both learning arms — the check that the substrate really was frozen. **The point estimates lean the other way**, reported because it is not noise-shaped: on three of four metrics the null is ahead, competent on a median **405** episodes against the wild type's **568** and ahead on **22 of 32** seeds — exactly the count the registered power arithmetic named — at **q = 0.058 reversed, short of the gate and post-hoc in direction**, so recorded as a lean and not a result. **The most concrete difference is not statistical**: five wild-type seeds never became competent within 3000 episodes against one for the null, which is why the means (1039.8 against 644.7) diverge much further than the medians. **No mechanism is offered** — V.2 scored 64 rewirings on four graph properties fixed in advance and found nothing predicting learning time. **The seed count was the load-bearing choice**, registered with its arithmetic because a null closes the phase: 16 pairs would have given **57.3%** power at the midpoint of V.3's observed win rate against **79.2%** at 32. **Three corrections**: a discrepancy reported three times in progress updates did not exist (whole-run mean read against the harness's plateau-tail mean; the runs are byte-identical to R.2's across all 3000 episodes on all 16 shared seeds, and task 4.3 passes exactly); the registered power table was corrected before the run, having quoted 63% and 85% from a rounded midpoint; and the harness announced a gate failure the design made unavoidable on the four-seed pilot, where the smallest reachable p is 2⁻⁴ and no gate could have passed. **May not be cited as** evidence for D2's primary (the wiring is frozen, so nothing converts the SPLIT), as "the shuffle beats the connectome", or as a quantitative comparison with V.3's +23.5%. Written up as [Logbook 064](../../../docs/experiments/logbooks/064-l4-frozen-features.md).

- [ ] L.1 **Readout width** *(**MUST for the close — the conditional FIRED 2026-09-16**, L.0 having read `wiring_is_inert_as_features`. It is also the right test for L.0's reverse-direction lean: a bottleneck could produce both the null and the lean. Registered as a conditional before L.0 ran, decided 2026-09-15 in advance of L.0 so the promotion is not a reaction to its result: a null at the 2×4 pooling with no width test leaves the obvious question unanswered, whether the readout was simply too small to see the wiring's features, and the answer is days of work reusing everything. If L.0 reads positive it stays SHOULD and asks "how much more" instead; carried to the phase after 7 either way if the close arrives first)* — is the four-class pooling the bottleneck through which the wiring's features are invisible? A readout over all 39 motor neurons (78 parameters) under `readout_only`, against the pooled 2×4, on both wirings. If L.0 is null at width 4 and positive at 39, the wiring's features exist and the bottleneck hid them; if both are null, the pooling was not the limit. Days of work, reuses everything; a rung the ladder did not name, pulled ahead because R.2 motivates it directly.

- [ ] L.2 **Intrinsic dynamics** (ladder rung 3; *SHOULD within the window, carried otherwise*) — units with a time constant, so within-step settling becomes across-step state. A reservoir with no temporal dynamics is a poor reservoir, and it is the direct follow-up to R.2's hop-distance null (the rule writes uniformly over graph distance). The first rung whose result the platform's core recurrence depends on; scoped in its own change.

- [x] L.3 **The body** (ladder rung 4) — stays in the phase after 7. Sibernetic/c302 interop is Phase-8-sized, and there is nothing to embody until L.0–L.2 say whether the fixed features carry anything a richer motor output could exploit. 7b's comparative sweep sits behind it.

- [x] L.4 **Synapse signs as features** *(**closed-unopened** 2026-09-16: the gate was L.0 reading positive and it read `wiring_is_inert_as_features`. Against a null there is nothing for a feature ablation to have changed. Reopens if L.1 finds the pooling was hiding structure. Originally added 2026-09-15, SHOULD, gated on L.0)* — does grounding the signs change what the frozen substrate *computes*, as opposed to what a Hebbian rule does with them? B.1 answered the second and its verdict stands ([Logbook 044](../../../docs/experiments/logbooks/044-l4-atlas-signs.md), `degree_statistics`): the untrained prior is indifferent to signs (+0.8 ns, measured under **no rule**, so nothing reaches it), and the other three gates are findings about reward-free Hebbian learning on signed weights — which I.4 confirmed are **not** instrument-limited, that rule having reached 78.3%, 64.4% and 67.3% on individual seeds. This asks the question none of them asked: the grounded network is **80% excitatory**, and in a fixed recurrent substrate the excitatory/inhibitory balance is a primary determinant of what it computes, so `synapse_signs: atlas` under `readout_only` is a feature ablation and not a re-run. A flag on L.0's machinery. **Gated because it is only interpretable after L.0**: against a null there is nothing for signs to have changed, and the informative version is "which part of the wiring carries the effect".

- [x] L.5 **Gap junctions as features** *(**closed-unopened** 2026-09-16: the gate was L.0 reading positive and it read `wiring_is_inert_as_features`. Against a null there is nothing for a feature ablation to have changed. Reopens if L.1 finds the pooling was hiding structure. Originally added 2026-09-15, SHOULD, gated on L.0)* — the same distinction for the electrical synapses. B.6 was deferred on whether the wiring advantage survives when gap junctions are *also plastic*, a plasticity question that R.2 closed along with its class; whether including them changes what the frozen reservoir computes is untested and is a flag (`enable_gap_junctions`) on L.0's machinery. Gated for L.4's reason. Note the asymmetry the substrate already carries: gap junctions are symmetric and degree-scaled, so they are the part of the wiring least like a directed circuit and most like the degree statistics 034 measured — which makes their contribution to a positive L.0 worth knowing before any of it is cited as structure-function.

## Shipment 7b — Cross-Species Comparative Learning (Head-Circuit Scope)

**OpenSpec change**: placeholder; created at milestone start
**Status**: ⏸️ **deferred to the phase after 7 (D14, 2026-09-15)**. The comparative core — C.3, C.5, C.6, C.7 — moves behind the substrate ladder, to run on a substrate where the wiring has been shown to matter; C.1, C.2 and C.4 are rule-independent infrastructure and become **unscheduled**, built on data-driven need rather than on this phase's clock. Phase 7 closes without 7b, recorded as SPLIT. Three reasons, each from the record: the learner C.5 names (D11/D12's grounded modulated rule) does not exist and R.2 closed the substitute path; under the PPO fallback the wiring is endpoint-inert (034) but learning-speed-relevant (block V, +35.4%), so a PPO sweep on time-to-competence would be a legitimate experiment — but one that measures the wiring as a learning-speed prior under gradient descent, not the question 7b was registered to ask; and the D9 scaffold puts a *more* artificial readout at the centre than the 2×4 map R.2 showed does the learning, so a cross-species difference would be dominated by "which species' pooled interneurons are the better 4-dim input". When it runs, the control that separates wiring legibility from readout capacity is named **before** the first comparative run, not after a pilot reads well.
**Roadmap layer**: cross-species
**Approx effort**: ~8-12 active weeks; lands beyond the window by default expectation
**Dependencies**: pipeline tasks (C.1/C.2/C.4) are rule-independent and may start after 7a-i; the comparative runs (C.3/C.5) use the **grounded modulated rule** and are **gated (2026-09-08) on a fidelity rung turning positive** — a registered result in which the wild-type wiring beats its rewired null under a local rule — because transferring a wiring-indifferent learner between species measures nothing about wiring — **gate reviewed and left standing 2026-09-13 by B.8: its letter is unmet (blocks V.1 and V.3 ran PPO) while its rationale is now satisfied (the wiring is not indifferent — +35.4% and +23.5% off time-to-competence), and the divergence is recorded as a decision rather than resolved by reading the letter loosely. It opens if the bounded rule programme produces a rule that reads the wiring, or is amended to the rationale if that programme reaches either of its two stopping conditions** — **resolved 2026-09-15 by D14: neither exit fired. The programme closed on R.2 in a state none of its three outcomes named (a competent learner whose substrate is frozen), so the gate is retired with 7b rather than opened or amended, and 7b moves to the phase after 7**; D12's head-scope third-factor source policy presupposes the diffusible layer.
**Roadmap reference**: `docs/roadmap.md` § Phase 7 § Required deliverables 2, D3/D9/D11/D12

- [x] C.1 **D9 scaffold** *(**unscheduled infrastructure** — 7b deferred to the phase after 7, D14 2026-09-15; see the shipment header above)*: AVA/AVB command-interneuron readout for head-truncated arms + scaffold-sensitivity check (second readout on one behaviour) + per-behaviour sensor-coverage audit of the truncated scope (evasion declared distal-only).
- [x] C.2 Head-truncation of Cook 2019 *(**unscheduled infrastructure** — 7b deferred to the phase after 7, D14 2026-09-15; see the shipment header above)* (nose→RVG, chemical-only) + validation of the truncated *C. elegans* baseline.
- [x] C.3 SHOULD — **dauer pathfinder, runs first** *(**comparative core, deferred** — 7b deferred to the phase after 7, D14 2026-09-15; see the shipment header above)* (D3/D11): Yim 2024 nerve-ring loader + comparative run — builds the multi-connectome pipeline with no homology tax.
- [x] C.4 *P. pacificus* ingest *(**unscheduled infrastructure** — 7b deferred to the phase after 7, D14 2026-09-15; see the shipment header above)*: shared-core CSV loader (`stevenjcook/cook_et_al_2025_pristionchus` / *Science* SI), species-keyed classification table + validation pathways, explicit **homology mapping table** artefact, species-keyed sensor/motor projection map (replacing the hard-coded elegans tuples in `connectome_ppo.py`).
- [x] C.5 **Comparative cross-connectome learning sweep** *(**comparative core, deferred** — 7b deferred to the phase after 7, D14 2026-09-15; see the shipment header above)* (D11 MUST): two homologous behaviours (klinotaxis, thermotaxis), under the grounded modulated L4 rule (D12 head-scope policy) with PPO as secondary context; one pre-registered transfer metric per behaviour; single-animal-vs-shared-core sensitivity run.
- [x] C.6 SHOULD: homology-mapped **weight-transplant transfer** *(**comparative core, deferred** — 7b deferred to the phase after 7, D14 2026-09-15; see the shipment header above)* (zero-shot + fine-tune; non-homologous-edge policy stated) — the arm that earns the "transfer of trained agents" claim.
- [x] C.7 SHOULD: species-appropriate third behaviour *(**comparative core, deferred** — 7b deferred to the phase after 7, D14 2026-09-15; see the shipment header above)* — elegans distal-only evasion at head scope; pacificus predatory approach/bite (**new predation-task env**, scoped in its own change).
- [x] C.8 7b logbook + roadmap sync. *(**deferred with the shipment** — 7b deferred to the phase after 7, D14 2026-09-15; see the shipment header above)*

## Synthesis

**OpenSpec change**: placeholder; created at Phase 7 close
**Status**: 🔲 not started
**Dependencies**: *(amended 2026-09-15, D14)* **7a + the frozen-features R.1b + V.4** — Phase 7 is marked COMPLETE (SPLIT: the diffusible layer, the modulated rules and 7b recorded as unmet MUSTs, deferred) when those have shipped and this synthesis publishes. 7b no longer gates the close.

- [ ] Z.1 Phase 7 synthesis logbook: exit-criteria walkthrough (MUST/SHOULD/MAY), the 2×2 terminal verdict against the claim-discipline bars, cross-species findings, honest negative-result documentation where applicable (Phase 5 precedent). **Settles the remaining SHOULDs as done-or-carried** — L.1, L.2, L.4 and L.5 (S.1 was cancelled 2026-09-15) — so no task is unmarked when this tracker archives, and states the terminal reading of the 2×2: the D2 primary (plastic wild-type beats plastic rewired-null) is **unmet** and cannot be met by anything in the closing scope, since L.0's learner leaves the wiring frozen; what L.0 settles is whether the wiring is legible to a learner that **reads** it rather than writes it.
- [ ] Z.2 `docs/roadmap.md`: Phase 7 status → ✅ COMPLETE; Timeline Overview + exit criteria + Success Levels flipped to terminal state.

> Archiving `phase7-tracking` is an operator-side step that does NOT block task completion (same precedent as `phase5-tracking` / `phase6-tracking`: an "archive me" task would self-block). The archive happens after the synthesis PR merges, via `openspec archive phase7-tracking`.
