# Tasks — Add Transgenerational Memory

Tasks are grouped by commit (per the plan's 8-commit grouping). Each group represents one PR-sized landing. Tests land alongside the production code they cover.

## 1. Scaffold strategy + Literal extensions (Commit 1)

- [x] 1.1 Create `quantumnematode/evolution/transgenerational_inheritance.py` with `TransgenerationalInheritance` skeleton implementing the `InheritanceStrategy` Protocol. Top-1 elite `select_parents`, single-elite `assign_parent`, `.tei.pt` `checkpoint_path`, `kind() -> "transgenerational"`. All methods runnable but the substrate-loading paths return placeholder no-ops pending commit 4.
- [x] 1.2 Extend `quantumnematode/evolution/inheritance.py` Protocol docstring + `kind()` Literal to include `"transgenerational"`. Update module docstring to document the fourth strategy.
- [x] 1.3 Extend `quantumnematode/utils/config_loader.py`: add `"transgenerational"` to the `EvolutionConfig.inheritance` Literal. No `TransgenerationalConfig` block yet (that lands in commit 6 alongside the YAML). Also: extended `evolution/__init__.py` exports, `evolution/loop.py` `_expected_kind` dispatch dict, and `scripts/run_evolution.py` CLI choices + strategy factory to keep the Literal extension runnable end-to-end (otherwise YAML loads with `inheritance: transgenerational` would crash at loop init or CLI parse).
- [x] 1.4 Tests: `tests/.../evolution/test_transgenerational_inheritance.py` (11 cases): `kind()` literal returns `"transgenerational"`; extended known-kind set guard (all 4 strategies in `{"none","weights","trait","transgenerational"}`); `checkpoint_path` format + zero-padding; `assign_parent` returns None (3 cases); `select_parents` single-elite + lex-tie + matches Baldwin/Lamarckian(1) byte-for-byte + empty-input + length-mismatch (5 cases); `isinstance` Protocol conformance check.
- [x] 1.5 Carry forward the `add-transgenerational-memory` rename + status update in `openspec/changes/phase5-tracking/tasks.md` (line 223: "(not yet created)" → "(scaffolded in PR #158; implementation in progress)"; status "ready to start" → "in progress").
- [x] 1.6 `uv run pytest -m "not smoke and not nightly"` passes 2665/2665. `uv run pre-commit run -a` passes all 10 hooks.

## 2. TransgenerationalMemory dataclass (Commit 2)

- [x] 2.1 Created `quantumnematode/agent/transgenerational_memory.py` with the `TransgenerationalMemory` dataclass: `logit_bias: torch.Tensor`, `lineage_depth: int`, `source_genome_id: str`. `frozen=True`. `__post_init__` validates shape (`ndim == 1`), dtype (`float32`), `lineage_depth ≥ 0`, and clamps `|x| ≤ LOGIT_BIAS_CLAMP` (= 2.0) via `object.__setattr__` on a `.detach().clone()` copy so the input tensor is never mutated.
- [x] 2.2 Implemented `apply_to_logits(logits)` returning `logits + self.logit_bias` (broadcast over leading dims), preserving shape/dtype/device, no in-place mutation.
- [x] 2.3 Implemented `inherit_from(parents, decay_factor)` as a `@classmethod`. Top-1 elite semantics (reads `parents[0]` only). Validates `0.0 ≤ decay_factor ≤ 1.0` and non-empty parents. Increments `lineage_depth`. Inherits `source_genome_id`.
- [x] 2.4 `extract_from_brain(brain, env, probe_positions, rng_seed)` shipped as a `NotImplementedError`-raising placeholder. **Functional implementation deferred to the F0 Substrate Extraction Pipeline commit** (it requires env coupling — pathogen lawn at a known position + probe-position generator + brain-policy adapter — which belongs to the loop-integration commit). The placeholder preserves the public-API surface so downstream importers can register the symbol today.
- [x] 2.5 Implemented module-level `save(substrate, path)` / `load(path)` via `torch.save` / `torch.load`. Round-trip preserves all fields byte-equivalently. `save` creates the parent dir if missing (mirrors Lamarckian capture). `load` raises `FileNotFoundError` with the missing path in the message.
- [x] 2.6 Tests: `tests/.../agent/test_transgenerational_memory.py` (20 cases): construction clamp + no-input-mutation + shape/dtype/depth rejections + frozen-instance assertion (7 cases); `inherit_from` geometric decay F0→F3 + depth increment + source preservation + empty-parents + decay range validators + multi-parent picks parents[0] (7 cases); `apply_to_logits` additive + broadcast + no-mutation + distinct-object (2 cases); save/load round-trip + save-creates-parent-dir + load-missing-file (3 cases); `extract_from_brain` raises NotImplementedError (1 case).
- [x] 2.7 `uv run pytest packages/.../test_transgenerational_memory.py -v` passes 20/20.

## 3. LSTMPPO TEI prior application (Commit 3)

- [x] 3.1 In `quantumnematode/brain/arch/lstmppo.py`: added `self.tei_prior: torch.Tensor | None = None` + `self._tei_prior_rollout_snapshot: tuple[torch.Size, torch.dtype] | None = None` to `__init__`. `run_brain()` adds `self.tei_prior` to actor logits before softmax when set, and records the shape/dtype snapshot. `learn()` entry asserts the snapshot matches the current `tei_prior` (raises `RuntimeError` on mismatch — set→None, shape change, or dtype change). `learn()`'s internal PPO update applies the same bias at the training-time forward pass (before softmax) so the PPO ratio stays well-defined. The snapshot is cleared after each completed PPO update so the next rollout window starts fresh. Byte-equivalence preserved when `tei_prior is None` (no bias add, no snapshot capture, no assertion fires).
- [x] 3.2 Confirmed: no runner-side changes. The attribute is set externally by `fitness.evaluate` (a follow-up addition) before invoking either runner; the runners remain TEI-agnostic.
- [x] 3.3 Tests: `tests/.../brain/arch/test_lstmppo_transgenerational_prior.py`. 10 cases passing — (a) default `tei_prior=None` baseline; (b) `bias=[+2, 0, 0, 0]` elevates action-0 frequency >0.5 over 100 rollout steps; (c) clearing `tei_prior` to None; (d) `prepare_episode` does NOT clear `tei_prior`; (e) attribute survives across 5 episode boundaries; (f) sampling/update forward passes produce identical biased distributions → PPO ratio ≈ 1.0 on first update; (g) `learn()` raises `RuntimeError` on mid-window mutation — split into 3 separate tests covering set→None, shape change, dtype change; (h) `learn()` with `tei_prior=None` throughout runs without raising.
- [x] 3.3b Confirmed: multi-agent symmetry tests deferred to the loop-integration commit (commit 3's brain-level tests are runner-agnostic by construction).
- [x] 3.4 `uv run pytest -v` on the new test file: 10/10 pass.

## 4. F0 substrate extraction pipeline + strategy integration (Commit 4)

- [x] 4.1 In `EvolutionLoop._resolve_per_child_inheritance`: at gen 0 under `inheritance: transgenerational`, return `(None, capture_path, parent_id)` where `capture_path = inheritance/gen-000/genome-{gid}.pt` (matches Lamarckian's `.pt` extension). The F0 worker writes trained weights to disk via the existing `fitness.evaluate` weight-capture path. At gen 1+, return `(None, None, parent_id)` (Baldwin-shaped, no capture; F1+ workers will receive the substrate via a separate kwarg path in commit 5).
- [x] 4.2 New `EvolutionLoop._run_f0_substrate_extraction(elite_id, gen_ids, solutions)` method, invoked from the main loop's post-`select_parents` block when `_substrate_inheritance_active() and gen == 0`. It (a) locates the elite's params in `solutions` via `gen_ids.index(elite_id)`, (b) decodes a fresh brain from the elite genome via the same encoder + sim_config, (c) loads the F0 elite's `.pt` weights via `load_weights`, (d) invokes `TransgenerationalMemory.extract_from_brain` with a deterministic probe-params sequence and `extraction_seed=424242` (sentinel for now; commit 5 wires the configurable `cfg.transgenerational.extraction_seed`), (e) saves the substrate to `inheritance/gen-000/genome-{elite_id}.tei.pt` via the strategy's `checkpoint_path`, (f) GCs every `.pt` file (preserves `.tei.pt`). Defensive on missing elite weights and missing elite in gen_ids (logs warning + skips).
- [x] 4.3 Collapsed into commit-5 task 5.3b — the `tei_prior_source` computation lives on the same call site as the worker-tuple extension, so it ships with that work in commit 5 (see `EvolutionLoop._compute_tei_prior_source`). The F0-side write (commit 4) produces the `.tei.pt` artifact that the commit-5 read side consumes.
- [x] 4.4 Added new `_substrate_inheritance_active()` helper on `EvolutionLoop` (returns `kind() == "transgenerational"`). Extended `_resolve_per_child_inheritance` (loop.py:454+) to handle `kind() == "transgenerational"` with the gen-0 vs gen-1+ split per task 4.1. Existing `_inheritance_active()` and `_inheritance_records_lineage()` semantics unchanged for the other three strategies.
- [x] 4.5 The new `_run_f0_substrate_extraction` walks `gen-000/` and deletes every `.pt` file (preserving the `.tei.pt` we just wrote, which has a distinct suffix). No GC-pass needed on F1+ directories (they're never created).
- [x] 4.6 Tests: `tests/.../evolution/test_f0_substrate_pipeline.py` (8 cases): `_substrate_inheritance_active()` returns True for TEI / False for the other three; `_resolve_per_child_inheritance` at gen 0 transgenerational returns `(None, capture_path, "")`; gen 1+ returns `(None, None, parent_id)` AND `(None, None, "")` when no elite yet; `_run_f0_substrate_extraction` logs+skips on missing weights file; logs+skips on elite-not-in-gen_ids; Lamarckian path unaffected. Also: 3 new `extract_from_brain` tests in `test_transgenerational_memory.py` (replaced the prior NotImplementedError placeholder test with: deterministic-on-seed, returns F0 substrate with correct lineage_depth/source_genome_id, rejects empty probe_params).
- [x] 4.7 `uv run pytest -v` on all transgenerational tests: 30/30 pass. `uv run pre-commit run -a`: all 10 hooks pass.

## 5. Per-generation `lawn_schedule` consumer in EvolutionLoop (Commit 5)

- [x] 5.1 Added `TransgenerationalConfig` and `LawnScheduleEntry` Pydantic models to `quantumnematode/utils/config_loader.py`. Validators: `decay_factor ∈ [0, 1]`; schedule entries cover `[0, generations)` exactly once each; `generation` indices non-negative; `ppo_train_episodes ≥ 0`.
- [x] 5.2 Added the inheritance ↔ transgenerational pairing validator: `transgenerational.enabled=true ⇒ inheritance=transgenerational`; `enabled=false ⇒ inheritance=none`. A symmetric guard rejects `inheritance=transgenerational` with `transgenerational=None`.
- [x] 5.3 In `quantumnematode/evolution/loop.py`: just before `optimizer.ask()` at the top of each generation, the new `_build_per_gen_sim_config(gen)` helper produces a per-gen `sim_config_for_gen = sim_config.model_copy(update={...})` Pydantic-v2 copy with `predators.enabled` set from the schedule entry's `pathogen_lawns_enabled` and `evolution.learn_episodes_per_eval` overridden by `ppo_train_episodes`. The per-gen copy replaces the existing `sim_config` tuple element in each worker tuple — each generation's workers receive distinct `model_copy` instances. The base `sim_config` is NOT mutated. Outside TEI runs the helper returns `self.sim_config` unchanged so behaviour is byte-equivalent.
- [x] 5.3b Extended `_evaluate_in_worker` worker tuple to 12 elements: `tei_prior_source: tuple[Path, float, int] | None` appended after the existing 11. For non-TEI strategies this is `None`, preserving worker contract. Co-evolution call sites (`coevolution.py`) also pass `None` to preserve the shape. The new `_compute_tei_prior_source(gen)` helper computes the value per-gen — at gen 0 returns `None`; at F1+ returns `(self._tei_f0_substrate_path, decay_factor, lineage_depth=gen)`. `_tei_f0_substrate_path` is persisted in the checkpoint pickle so resume from F1+ recovers the path.
- [x] 5.3c Extended `LearnedPerformanceFitness.evaluate` with a new kwarg `tei_prior_source: tuple[Path, float, int] | None = None`. When non-None: (i) loads the F0 substrate via `TransgenerationalMemory.load(path)`, (ii) applies `inherit_from([f0], decay_factor)` `lineage_depth` times, (iii) dispatches via `hasattr(brain, "tei_prior")` — sets the depth-N `logit_bias` on the brain (logger.warning if False), (iv) bypasses the existing `learn_episodes_per_eval > 0` validator so F1+ can run with `learn_episodes_per_eval=0` (eval phase only, no train). The substrate-set call happens immediately after `brain = encoder.decode(...)` and BEFORE `_build_agent`. The runner code remains TEI-agnostic.
- [x] 5.3d Updated `EpisodicSuccessRate.evaluate` to accept `tei_prior_source` for ABI symmetry (always ignored). The CLI guard in 5.3e blocks the unsupported `transgenerational + success_rate` combination at startup before this code path is reached.
- [x] 5.3e Updated the existing CLI guard in `scripts/run_evolution.py` to mention `transgenerational` alongside `lamarckian`/`baldwin`: when any of the three is resolved AND `--fitness success_rate` is selected, exit with code 1 BEFORE constructing the optimizer. Error message points to `--fitness learned_performance` or `inheritance: none`.
- [x] 5.4 Tests: `tests/.../evolution/test_loop_transgenerational_smoke.py` — 8 unit cases covering `_build_per_gen_sim_config` (base config returned when transgenerational absent; schedule overrides applied; base sim_config never mutated), `_compute_tei_prior_source` (None at gen 0; None when substrate missing; tuple at F1+; None for non-TEI runs), and checkpoint round-trip of `_tei_f0_substrate_path` (TEI + non-TEI paths).
- [x] 5.5 Extended `tests/.../utils/test_config_loader.py` with 8 new cases: 3 for `TransgenerationalConfig`/`LawnScheduleEntry` (defaults, decay range rejection, duplicate-generation rejection), 5 for `EvolutionConfig`'s pairing validators (TEI-enabled requires inheritance=transgenerational; TEI-disabled requires none; transgenerational without config block rejected; out-of-range schedule generation rejected; well-formed config accepted).
- [x] 5.6 `uv run pytest -m "not smoke and not nightly"` passes 2727/2727. `uv run pre-commit run -a` passes all 10 hooks.

## 6. Config + campaign shell (Commit 6)

- [x] 6.1 Created three sibling YAMLs under `configs/evolution/`:
  `transgenerational_pathogen_avoidance_lstmppo_klinotaxis.yml` (TEI-on pilot/full arm),
  `transgenerational_pathogen_avoidance_lstmppo_klinotaxis_tei_off.yml` (TEI-off control),
  and `transgenerational_pathogen_avoidance_lstmppo_klinotaxis_smoke.yml` (F0-only calibration smoke). Population 16, generations 4, brain `lstmppo` with klinotaxis sensors. Uses the existing `predators:` YAML block with `movement_pattern: stationary` (the schema key — `predator_type` in the original spec wording was a typo; the underlying `PredatorType` enum is still STATIONARY). `transgenerational` block with `enabled: true`, `decay_factor: 0.6`, `extraction_seed: 424242`, and `lawn_schedule` covering F0 (pathogen on, ppo_train_episodes=50) + F1/F2/F3 (pathogen off, ppo_train_episodes=0). `inheritance: transgenerational`. Six-field hyperparam_schema matches the M3/M4 LSTMPPO predator pilots. TEI-off sibling differs only in `inheritance: none` + `transgenerational.enabled: false` (the pairing validator pins this; CLI cannot override the `transgenerational:` block, so a sibling YAML is the cleanest expression of the control arm). Smoke sibling differs in `generations: 1` + `population_size: 6` + 1-entry schedule (the coverage validator requires exactly one entry per gen in `[0, generations)`, so CLI `--generations 1` against a 4-entry schedule would fail).
- [x] 6.2 Created `scripts/campaigns/phase5_m6_transgenerational_lstmppo_klinotaxis.sh`. Subcommands `--smoke`, `--pilot`, `--full`. `--smoke` runs F0-only 1 seed × pop 6 × 1 generation (TEI-on arm only) for the calibration gate. `--pilot` 1 seed × pop 6 × 4 gens, paired TEI-on + TEI-off. `--full` 4 seeds (42, 43, 44, 45) × pop 16 × 4 gens, paired arms. `inheritance` and `transgenerational.enabled` are pinned by the sibling YAMLs (the pairing validator ties them at config-load), so only `--population`, `--generations`, `--seed`, and `--fitness learned_performance` are forwarded to `run_evolution.py`.
- [x] 6.3 Output directory convention `evolution_results/m6_transgenerational/{tei_on|tei_off}/seed-{N}/<session_id>/` documented in the campaign shell's header.
- [x] 6.4 Added `evolution/transgenerational_pathogen_avoidance_lstmppo_klinotaxis.yml` to `SIMULATION_CONFIGS` parametrize in `tests/.../test_smoke.py` (the existing `test_run_simulation_smoke` covers it at `--runs 2 --theme headless`). `run_simulation.py` ignores the `evolution` / `hyperparam_schema` / `transgenerational` blocks, so this smoke confirms the YAML loads and the brain runs in the configured env. End-to-end coverage of the TEI evolution loop is exercised by the unit tests in `test_loop_transgenerational_smoke.py` (commit 5) and will get a final wall-time check via the calibration-gate smoke in commit 8.
- [x] 6.5 `uv run pytest -m "not smoke and not nightly"` passes 2733/2733. `uv run pre-commit run -a` passes all 10 hooks.

**Known follow-up (fixed in this PR):** The F0 substrate extraction probe at `EvolutionLoop._build_f0_probe_params` constructs `BrainParams` without populating `stam_state` of the correct env-derived dim. On configs with both predators and `stam_enabled: true`, the runtime feature pipeline emits 11 zeros for STAM (registry default) while the brain was constructed with the inferred STAM dim (e.g. 7 for 2 channels) → input_dim mismatch → `torch.layer_norm` raises. Fixed by extending `_build_f0_probe_params(brain=brain)` to derive the effective STAM dim from `brain.input_dim - sum(non_stam_classical_dims)` and zero-fill `stam_state` to that size. Two new regression tests in `test_f0_substrate_pipeline.py`. The TEI-on smoke configuration now runs end-to-end through F0 substrate extraction without crashing.

## 7. Per-gen choice-index evaluator + paired-arm aggregator (Commit 7)

- [x] 7.1 Created `scripts/campaigns/transgenerational_per_gen_eval.py`. Reads each session's new `per_gen_elites.jsonl` artefact (also added in this commit — a small `EvolutionLoop` addition that snapshots each generation's elite params after `select_parents`), re-runs each gen's elite against a fresh env for `--eval-episodes` episodes, and counts steps inside any predator's damage radius via post-hoc agent-path inspection (exact for stationary predators, which is what the pilot uses). Outputs `per_gen_choice_index.csv` with columns `seed, arm, generation, genome_id, episode, total_steps, steps_inside_damage_radius, choice_index, pathogen_choice_index`.
- [x] 7.2 Created `scripts/campaigns/aggregate_m6_pilot.py`. Reads `per_gen_choice_index.csv` and produces `retention_table.csv`, `decision_gate.csv`, and `summary.md` (markdown). Decision gate: F1 ≥ 40% × F0, F2 ≥ 25% × F0, F3 ≥ 15% × F0, monotone non-increasing (F0 ≥ F1 ≥ F2 ≥ F3). Cross-seed verdict: GO iff ≥2 seeds pass; PIVOT iff exactly 1; STOP otherwise. Summary markdown includes paired-arm retention comparison (TEI-on vs TEI-off) when both arms present.
- [x] 7.3 `pathogen_choice_index` alias column added in evaluator output (parallel to underlying `choice_index`) for vocabulary clarity.
- [x] 7.4 Added 8 tests in `tests/.../campaigns/test_aggregate_m6_pilot.py` for the decision-gate logic: pass case, F1-ratio fail, monotone violation, incomplete-generations skip, GO/PIVOT/STOP boundaries, and the per-episode averaging in `build_retention_table`. Plus 2 tests in `test_f0_substrate_pipeline.py` covering the F0 probe regression (stim-shape match under STAM, legacy `None` shape preserved when `brain` arg omitted).
- [x] 7.5 `uv run pytest -m "not smoke and not nightly"` passes 2743/2743. `uv run pre-commit run -a` passes all 10 hooks.

## 8. F0 calibration smoke + pilot + full campaign + logbook (Commit 8)

Execution-only work below; the only code that landed in commit 8 was the survival-weighted-fitness modifier on `LearnedPerformanceFitness` + the aggregator's F0 training-time-fitness override flag (both required by the audit-driven diagnostic chain; see design.md "Post-execution audit addendum" § Implementation divergence #1 + #2) plus the per-gen-evaluator termination-reason tracking. Logbook itself is the headline deliverable.

- [x] 8.1 Ran F0 calibration smoke (`--smoke`). Initial run with `damage_radius=3, K=50` returned F0 ≈ 0.93 (ceiling-saturated, outside the `[0.45, 0.85]` envelope). Operator iterated: Path A retune (damage_radius 3→5) + Path AA hyperparameter sweep (K ∈ {500, 1000}) + Path AAA composite fitness (`fitness_survival_weight: 1.0`). Final calibrated F0 elite at survival_rate ≈ 0.46 (within the envelope under the survival-rate metric — see Implementation divergence #1 + #2 in design.md). The choice_index metric remained geometry-dominated at ~0.93 even for calibrated F0; survival_rate was promoted to primary signal at the metric layer. See [`tmp/evaluations/transgenerational/transgenerational_scratchpad.md`](../../../tmp/evaluations/transgenerational/transgenerational_scratchpad.md) lines 700-1100 for the full Path A → AA → AAA forensic chain.
- [x] 8.2 Ran pilot (`--pilot`, Path AAA variant with pop=8, K=1000). Aggregator produced preliminary STOP under choice-index gate (0/1 pass; monotone fails because F1 > F0 — geometry-dominated metric trap). Survival-rate gate with F0 override produced preliminary PIVOT for TEI-on (substrate effect ~+0.04 absolute over TEI-off at F1). See scratchpad lines 1199-1330. Forensic finding: the framework runs cleanly end-to-end, but the metric + control-arm-compute-asymmetry both need addressing before a verdict is interpretable — flagged for the audit (8.5).
- [x] 8.3 Paused for user review of preliminary pilot outputs. User confirmed Path AAA approach (composite fitness) for the full campaign and asked to keep iterating rather than easily give up (user instruction "I want us to explore a path that doesn't result in easily giving up"). Aggregator monotone-gate fix (F0 training-time override) approved to land in parallel with the full campaign run.
- [x] 8.4 Ran full campaign (`--full`): 4 seeds (42, 43, 44, 45) × pop 16 × 4 gens × paired TEI-on / TEI-off arms. Wall-time ~14 hours (lower than the planned 16 because TEI-on F1+ runs K=0 — only eval phase consumes compute at F1+). Aggregator outputs stashed under [`artifacts/logbooks/018-transgenerational-memory/full/`](../../../artifacts/logbooks/018-transgenerational-memory/full/) — `decision_gate.csv` + `retention_table.csv` + `summary.md` for the choice-index gate; `survival_decision_gate.csv` + `survival_retention_table.csv` + `survival_summary.md` for the survival-rate gate with F0 override; `per_gen_choice_index.csv` for the per-(arm, seed, gen, episode) raw data. Choice-index verdict STOP 0/4 either arm; survival-rate verdict PIVOT TEI-on 1/4 / STOP TEI-off 0/4. Paired TEI-on > TEI-off delta is +0.07 / +0.11 / +0.04 absolute at F1 / F2 / F3 means across seeds.
- [x] 8.5 Paused for user review of full-campaign outputs. User requested a critical review of the data + identified two fundamental design issues (substrate shape gradient-unconditional, fitness-on-avoidance-alone confound). Follow-up deep-dive sweep against live code surfaced two more (F0 probes have no pathogen context, F1+ asymmetric compute + missing `transgenerational+weights`) + three secondary concerns (lex-tie-break determinism, decay factor never tuned, F0 substrate bit-identical across 3/4 seeds). A re-verification pass confirmed one inaccuracy in the initial framing (composite fitness does NOT reward starvation as originally claimed — the upstream cause is PPO training reward + env geometry creating a "circle right always" local optimum that composite fitness then faithfully ranks). User approved the four-issue framing for the logbook + the M6.9+ follow-up scope.
- [x] 8.6 Wrote [`docs/experiments/logbooks/018-transgenerational-memory.md`](../../../docs/experiments/logbooks/018-transgenerational-memory.md). 215 lines; mirrors the M4 INCONCLUSIVE precedent: framework shipped, literal aggregator output preserved for both gates, post-pilot audit demotes to INCONCLUSIVE, M6.9+ scoped lightly. Cites Posner 2023 + Hunter critique + Murphy rebuttal + Akinosho/Vidal-Gadea 2025 per the original framing intent. Pathogen-lawn-as-STATIONARY-predator methodology documented in § Background + § What shipped. The original M6 framing ("first computational replication of the canonical defended TEI mechanism") was demoted in light of the audit to "framework shipped + INCONCLUSIVE" — the four-issue audit means M6 cannot honestly claim replication.
- [x] 8.7 Stashed run artefacts under [`artifacts/logbooks/018-transgenerational-memory/full/`](../../../artifacts/logbooks/018-transgenerational-memory/full/). LFS audit: 6 CSVs + 2 markdown summaries, largest file 73 KB (`per_gen_choice_index.csv`) — well under the 100 KB threshold; the CSVs are LFS-tracked anyway via the existing `artifacts/**/*.csv` rule in `.gitattributes`.
- [x] 8.8 Sanitised stashed artefacts for absolute home paths. `grep -rE '/Users/|/home/|C:\\Users\\' artifacts/logbooks/018-transgenerational-memory/full/` returns no matches; staged content scanned clean.
- [x] 8.9 Updated [`openspec/changes/phase5-tracking/tasks.md`](../phase5-tracking/tasks.md) M6.1–M6.8 ticks + lightly-scoped M6.9–M6.12 sub-milestones (substrate redesign, training-reward + env redesign, env-derived extraction probes, `transgenerational+weights` configuration) per the four-issue audit. Updated [`docs/roadmap.md`](../../../docs/roadmap.md) Phase 5 milestone tracker M6 row to "framework shipped, INCONCLUSIVE on the science" — same framing as the existing M4 row. Substrate-redesign detail is deliberately **not** planned in either tracker; the next PR will scaffold a dedicated OpenSpec change for M6.9+ with deeper planning + any further design issues a fresh review surfaces.
- [x] 8.10 OpenSpec change archived via the `openspec-archive-change` skill — note: archiving happens in the commit immediately following this one (the audit-trail commit ticks 8.1-8.10 here so the change is "done" before archive moves the directory). No spec-sync was performed per the INCONCLUSIVE convention: the audit-flagged D1 / D2 / D6 decisions stay in the change archive for reference rather than promoted to main specs (the M4 INCONCLUSIVE archive followed the same convention).
