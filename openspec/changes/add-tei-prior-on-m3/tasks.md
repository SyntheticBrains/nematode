# Tasks — `add-tei-prior-on-m3` (M6.13)

Tasks grouped by commit (per design.md § Migration Plan). Each group is one PR-sized landing. Tests land alongside the production code they cover. Defaults preserve M3 / M6 / M6.9+ byte-equivalence everywhere.

## 1. Composed inheritance strategy class + Protocol widening (Commit 2)

- [ ] 1.1 Create `packages/quantum-nematode/quantumnematode/evolution/lamarckian_transgenerational_inheritance.py` with class `LamarckianTransgenerationalInheritance`. The class SHALL implement the four `InheritanceStrategy` Protocol methods with semantics IDENTICAL to `LamarckianInheritance` for `select_parents` / `assign_parent` / `checkpoint_path`, and `kind()` returns `"weights+transgenerational"`. Construction signature: `LamarckianTransgenerationalInheritance(elite_count: int = 1)`. The constructor SHALL reject `elite_count < 1` with `ValueError` (mirrors Lamarckian's existing validation).
- [ ] 1.2 Widen `InheritanceStrategy.kind()` Protocol return Literal in `quantumnematode/evolution/inheritance.py` from `Literal["none", "weights", "trait", "transgenerational"]` to `Literal["none", "weights", "trait", "transgenerational", "weights+transgenerational"]`. The widening is backwards-compatible — existing strategies continue returning their pre-M6.13 values; only the new composed class adds the new value.
- [ ] 1.3 Re-export `LamarckianTransgenerationalInheritance` from `packages/quantum-nematode/quantumnematode/evolution/__init__.py` so import paths match the pattern of the four existing strategies.
- [ ] 1.4 Tests in `tests/.../evolution/test_lamarckian_transgenerational_inheritance.py` (~16 cases): construction-arg validation; `select_parents` cross-strategy parity with Lamarckian (top-fitness lex-tie); `assign_parent` round-robin with 1/4/8 child indices; `checkpoint_path` returns canonical `.pt` path (NOT `.tei.pt`); `kind()` returns composed literal; protocol-conformance via `isinstance(instance, InheritanceStrategy)`; rejects `elite_count <= 0`; cross-strategy parity test (composed and Lamarckian return identical results for the same `select_parents` / `assign_parent` inputs); `composed_kind_distinct_from_either_parent_strategy`; `composed_strategy_protocol_methods_complete`; `composed_kind_in_known_set` (extending the Protocol-conformance test).
- [ ] 1.5 `uv run pytest -m "not smoke and not nightly"` passes. `uv run ruff check` + `uv run pyright` clean on changed files. M3 / M6.9+ regression tests still green.

## 2. Config validator relaxation (Commit 3)

- [ ] 2.1 Widen `EvolutionConfig.inheritance` Literal in `packages/quantum-nematode/quantumnematode/utils/config_loader.py` to include `"weights+transgenerational"`. Update Pydantic Field default unchanged (`"none"`); update the field's docstring to enumerate all 5 values.
- [ ] 2.2 Relax `_validate_inheritance` pairing rule so `transgenerational.enabled=True` is accepted with EITHER `inheritance == "transgenerational"` OR `inheritance == "weights+transgenerational"`. Add a new sub-rule rejecting `inheritance == "weights+transgenerational"` when the `transgenerational:` block is absent / `None` (composed mode requires the substrate to be configured). Add a sub-rule rejecting `transgenerational.enabled=False` paired with `inheritance == "weights+transgenerational"` (mirrors the existing `lamarckian + transgenerational.enabled=False` rejection — composed mode requires substrate to be active).
- [ ] 2.3 Add new sub-rule: under `inheritance == "weights+transgenerational"`, every `lawn_schedule` entry with `generation > 0` MUST have `ppo_train_episodes > 0`. Error message lists every offending entry and points to either setting K>0 at F1+ OR switching to `inheritance: transgenerational` for the pure-TEI K=0 path.
- [ ] 2.4 Extend the single-elite rule (existing `inheritance_elite_count != 1` rejection for Lamarckian) to ALSO reject under `inheritance == "weights+transgenerational"` (composed mode uses the same single-elite-broadcast pattern).
- [ ] 2.5 Extend the architecture-changing-fields rejection (existing for `inheritance == "lamarckian"`) to ALSO apply to `inheritance == "weights+transgenerational"` (both modes warm-start weights; same constraint).
- [ ] 2.6 Tests in `tests/.../utils/test_config_loader.py` AND/OR `test_config.py` (~4 cases): cross-product matrix — `(inheritance, transgenerational.enabled)` cells. Positive cells: `(none, F)`, `(none, None)`, `(lamarckian, None)`, `(baldwin, None)`, `(transgenerational, T)`, `(weights+transgenerational, T)`. Negative cells: `(weights+transgenerational, None)`, `(weights+transgenerational, F)`, `(lamarckian, T)`, `(baldwin, T)`, `(transgenerational, F)`, `(transgenerational, None)`. Plus new sub-rule tests: `(weights+transgenerational, T)` with F1+ K=0 in lawn_schedule MUST reject; same config with F1+ K=500 passes.
- [ ] 2.7 `uv run pytest -m "not smoke and not nightly"` passes. M3 / M6.9+ regression tests still green (existing validator behaviour unchanged for legacy cells).

## 3. Loop integration (Commit 4)

- [ ] 3.1 Extend `_expected_kind` dict in `EvolutionLoop.__init__` to include `"weights+transgenerational": "weights+transgenerational"`. The kind-mismatch defensive check now accepts the composed strategy instance under the composed config value.
- [ ] 3.2 Widen `_inheritance_active()` predicate from `kind() == "weights"` to `kind() in {"weights", "weights+transgenerational"}`. Widen `_substrate_inheritance_active()` predicate (if it exists per PR-A — verify in code; otherwise the existing `kind() == "transgenerational"` check widens analogously) from `kind() == "transgenerational"` to `kind() in {"transgenerational", "weights+transgenerational"}`.
- [ ] 3.3 Add a fifth branch in `_resolve_per_child_inheritance` for `kind == "weights+transgenerational"`. The branch SHALL return the same `(parent_warm_start_path, child_capture_path, parent_id)` tuple shape as the existing `"weights"` branch — same `checkpoint_path` calls, same `assign_parent` resolution, same warm-start path lookup, same warning-on-missing-parent fallback.
- [ ] 3.4 Add a new helper `_combined_inheritance_active()` to `EvolutionLoop` that returns `True` when `kind() == "weights+transgenerational"`. Use this to guard the inline GC loop at `loop.py:~863-866` inside `_run_f0_substrate_extraction` (`if not self._combined_inheritance_active(): <inline GC>`). Ordering note: the main-loop GC at `loop.py:~1649` already runs BEFORE F0 substrate extraction at `loop.py:~1705`; under composed mode it keeps only the elite's `.pt` (because `_inheritance_active()` widens to True). The skip prevents the inline GC from then deleting that surviving elite `.pt`. Under pure-TEI (`kind() == "transgenerational"`), the main-loop GC does NOT fire, so the inline GC remains load-bearing for that path — the skip is composed-mode only.
- [ ] 3.5 Update `scripts/run_evolution.py` strategy-factory `elif` chain to construct `LamarckianTransgenerationalInheritance(elite_count=cfg.inheritance_elite_count)` for the composed value.
- [ ] 3.6 Tests in `tests/.../evolution/test_loop_m613_smoke.py` (~6 cases): `composed_kind_loop_init_kind_mismatch_raises` (instance vs config string mismatch); `composed_kind_2gen_smoke_runs` (2 gens, 2 children, end-to-end); `f0_pt_survives_substrate_extraction_under_composed` (F0 `.pt`s on disk after pipeline runs); `f1_warm_start_path_resolves_to_f0_elite_pt`; `f1_workers_receive_both_warm_start_and_substrate` (eval_args tuple has BOTH `warm_start_path_override` AND `tei_prior_source` non-None at F1); `lineage_csv_records_inherited_from_for_composed_mode`.
- [ ] 3.7 Extend existing `tests/.../evolution/test_inheritance.py` kind-set assertion to include `"weights+transgenerational"`.
- [ ] 3.8 `uv run pytest -m "not smoke and not nightly"` passes. Specifically verify the existing regression tests stay green: `tests/.../evolution/test_inheritance.py` (M3 / NoInheritance / Baldwin / Transgenerational protocol-conformance + kind() set assertions); `tests/.../evolution/test_transgenerational_inheritance.py` (M6.9+ pure-TEI strategy unit tests); `tests/.../evolution/test_loop_transgenerational_smoke.py` (M6 / M6.9+ substrate-cascade loop smoke); `tests/.../evolution/test_loop_smoke.py` (general loop smoke). Widened predicates SHALL only fire under the new composed kind; non-composed paths SHALL be byte-equivalent.

## 4. Three-arm campaign configs + launcher shell (Commit 5)

- [ ] 4.1 Create `configs/evolution/transgenerational_m613_tei_weights.yml`: `inheritance: weights+transgenerational`, full `transgenerational:` block (`enabled: true`, bias_network, decay_shape, probe_ring with `safe_probes` extension as in PR-A's tei_on arm), `lawn_schedule` with F1+ `ppo_train_episodes: K_test` (K_test placeholder set to 1000; will be retuned post-smoke calibration). Reuse M6.10 env subtree (15×15, 4 predators, damage=25, `min_food_predator_distance=4`, `reward_mode: gradient_proximity`) and `fitness_metric: survival_rate`.
- [ ] 4.2 Create `configs/evolution/transgenerational_m613_weights_only.yml`: `inheritance: lamarckian`, `transgenerational:` block ABSENT, `lawn_schedule` with F1+ `ppo_train_episodes: K_test` (same value as tei_weights). Same env + reward + fitness_metric subtrees as tei_weights.
- [ ] 4.3 Create `configs/evolution/transgenerational_m613_control.yml`: `inheritance: none`, `transgenerational:` block ABSENT, `lawn_schedule` with F1+ `ppo_train_episodes: K_test` (same value). Same env + reward + fitness_metric subtrees.
- [ ] 4.4 Create `configs/evolution/transgenerational_m613_smoke.yml`: forks tei_weights with `population_size: 6, generations: 2, learn_episodes_per_eval: K_test` for calibration smoke.
- [ ] 4.5 Create `scripts/campaigns/phase5_m613_tei_prior_lstmppo_klinotaxis.sh`: subcommands `--smoke` / `--pilot` / `--full` (forked from PR-A's `phase5_m69_...sh`). Launch-time parity check: `fitness_survival_weight`, `fitness_metric`, `env.grid_size`, `env.predators.count`, `env.predators.predator_damage`, `env.foraging.min_food_predator_distance` MUST match across the three arm YAMLs; shell exits before any worker is dispatched on mismatch. Stderr captured into the parity_out command substitution (`2>&1`) so MISMATCH diagnostics reach the failure branch (PR-A learning).
- [ ] 4.6 Tripwire automation: T2' (substrate-diversity, reuses PR-A's `m69_substrate_diversity.py` unchanged) + T4' (substrate magnitude) programmatic; T1' (F0 envelope) + T3' (M3-headroom, NEW) surfaced as next-step echo hints reading from `eval_diagnostics.jsonl`. The T3' eval reads `weights_only F0` + `weights_only F1` + `control F1` from the smoke run's per-genome eval diagnostics and emits PASS/FAIL with a numeric recommendation for K_test adjustment.
- [ ] 4.7 Output directory convention `evolution_results/m613_tei_prior/{tei_weights|weights_only|control}/seed-{N}/<session_id>/` documented in the shell header.
- [ ] 4.8 Add `evolution/transgenerational_m613_tei_weights.yml` to `SIMULATION_CONFIGS` parametrize in `tests/.../test_smoke.py`.
- [ ] 4.9 `uv run pytest -m "not smoke and not nightly"` passes. Shell `--help` output is correct. ShellCheck (via pre-commit) passes.

## 5. Three-arm aggregator + reframed verdict (Commit 6)

- [ ] 5.1 Create `scripts/campaigns/aggregate_m613_pilot.py` (fork of `aggregate_m69_pilot.py`). The primary verdict pair SHALL be `tei_weights − weights_only` (NOT `tei_on − control`). The aggregator produces:
  - `retention_table.csv` (per arm × seed × gen: mean survival_rate).
  - `decision_gate.csv` (per arm × seed: ratio + monotone passes; per-arm overall).
  - `summary.md` (markdown; cross-arm comparison; per-seed gen-2 monotonicity warnings).
  - `cross_arm_verdict.csv` (Wilcoxon p-value + bootstrap CI bounds per arm-pair).
  - `pilot_pivot_decision.md` (only when `--mode pilot`; classifies the pilot outcome against the M6.13 six-row pivot table from design.md § D6).
  - `pr_close_note.md` (when GO at `--mode full`) OR `m613_null_finding_note.md` (when STOP at `--mode full`).
- [ ] 5.2 Cross-arm primary verdict per evolution-framework spec § "M6.13 Cross-Arm Primary Verdict (Reframed)": GO iff `tei_weights` passes per-arm gate (≥ 2/4 seeds) AND `tei_weights − weights_only` paired-seed delta satisfies BOTH Wilcoxon one-sided p < 0.10 AND ≥ 5pp absolute delta with non-overlapping 80% bootstrap CIs (1000 resamples). Both checks MUST agree on direction.
- [ ] 5.3 Secondary verdicts: `weights_only − control` (M3 re-reproduction; should match PR-A's +17.5pp scaled to K_test) and `tei_weights − control` (composed-arm vs floor).
- [ ] 5.4 Pilot pivot classification (M6.13 six rows from design.md § D6): row 1 (substrate-inert, STOP), row 2 (clear GO at K_test), row 3 (K-sensitivity, 2-5pp Δ), row 4 (substrate-interferes, STOP), row 5 (M3-saturation pivot, K_test too large), row 6 (PPO-destabilised, STOP). Classification SHALL be deterministic — exactly one row matches any given outcome.
- [ ] 5.5 Tests in `tests/.../campaigns/test_aggregate_m613_pilot.py` (~14 cases): three-arm retention table; per-arm gate logic; Wilcoxon p-threshold passes/fails; bootstrap CI bounds; primary-verdict AND-of-checks for the reframed pair; K_TOO_LARGE / M3_SATURATION_WARNING flags; pivot table emission classifies each of the six rows correctly on fixture inputs; PR-close-note emitted iff primary GO; null-finding note emitted iff primary STOP.
- [ ] 5.6 `uv run pytest -m "not smoke and not nightly"` passes (M6.9+ aggregator tests + new M6.13 tests).

## 6. K_test calibration smoke + pilot + full campaign + logbook (Commit 7 + 8)

Execution-only work below; the only code that lands in commits 7+8 is logbook 020 + any user-review-driven YAML retunes from the smoke / pivot table.

- [ ] 6.1 K_test calibration smoke pass 1 at K=1000: `--smoke` on `tei_weights` config (or `weights_only` standalone), 1 seed × pop 6 × 2 gens. Read T1' / T2' / T3' / T4' from emitted `eval_diagnostics.jsonl`. Decide: K stays / drops to 500 / bumps to 1500 per design.md § D5.
- [ ] 6.2 K_test calibration smoke pass 2 if needed (cap at 2 passes total). On second-pass failure, STOP and document as "PR-A env doesn't support M6.13 testing."
- [ ] 6.3 Update the three arm YAMLs' `learn_episodes_per_eval` and `lawn_schedule` F1+ entries to the chosen K_test value. Commit as a `chore(configs): M6.13 land K_test = N` commit.
- [ ] 6.4 Pilot run × 1 (single seed × pop 8 × 4 gens × 3 arms) at chosen K_test. Aggregator runs in `--mode pilot`. User-review pause held BEFORE unblocking full campaign per `feedback_logbook_review_before_verdict.md`.
- [ ] 6.5 Pivot table classification at pilot review: which of the six rows matched? If STOP: skip 6.6 + 6.7. If GO: proceed to 6.6. If K-sensitivity: rerun pilot at K=500 + K=1500 (+6 wall-h cap). If PIVOT-K-too-large: rerun pilot at K=500 (+3 wall-h cap).
- [ ] 6.6 Full campaign (only if pilot GO): 4 seeds × pop 16 × 4 gens × 3 arms at K_test. Aggregator runs in `--mode full`. User-review pause held BEFORE logbook 020 verdict finalisation.
- [ ] 6.7 Logbook 020 written: `docs/experiments/logbooks/020-tei-prior-on-m3.md`. Documents M6.13 verdict (GO / STOP); references the pilot pivot decision row; references the aggregator's `cross_arm_verdict.csv` for the primary delta; cites PR-A's +17.5pp M3 finding for comparison; documents the M6.14 trigger (frequency-prior ablation, only if GO).
- [ ] 6.8 Stash run artefacts under `artifacts/logbooks/020-tei-prior-on-m3/`. Audit file sizes vs `.gitattributes` LFS rules. Sanitise for absolute home paths.
- [ ] 6.9 Update `openspec/changes/phase5-tracking/tasks.md` M6.13 tick. Update `docs/roadmap.md` M6.9+/M6.13 row with PR verdict. If GO: scaffold M6.14 OpenSpec change (frequency-prior ablation) as a separate change directory. If STOP: document M6.13 as "TEI doesn't transfer in either form on this RL substrate; M6.14 NOT triggered" and close the M6 thread.
- [ ] 6.10 Archive this OpenSpec change via `openspec-archive-change` after verdict published. Per the M4/M6/M6.9+ INCONCLUSIVE/STOP precedent: spec deltas stay in archive for reference (no main-spec sync if verdict is STOP; sync the composed-strategy requirements if verdict is GO).
- [ ] 6.11 Push branch + open draft PR; reference logbook 020 in PR body; ask before push per `feedback_ask_before_push_and_pr.md`.
