# Tasks: M3 — Lamarckian Inheritance Pilot (Predator Arm)

Single PR. Order is dependency-first: Protocol + config + validators land before the loop hook so the framework is testable in isolation, then the pilot config + campaign + run + logbook close the milestone.

## 1. InheritanceStrategy Protocol + implementations

- [x] 1.1 Create `packages/quantum-nematode/quantumnematode/evolution/inheritance.py` with the `InheritanceStrategy` Protocol (`select_parents`, `assign_parent`, `checkpoint_path`).
- [x] 1.2 Implement `NoInheritance` (default no-op): `select_parents` returns `[]`, `assign_parent` returns `None`, `checkpoint_path` returns `None`. The loop's per-child step at task 4.2 guards with `isinstance(strategy, NoInheritance)` so `checkpoint_path` is never invoked under no-op. Returning `None` (rather than raising) keeps the Protocol method shape uniform and avoids tripping type-checkers that flag a `NotImplementedError`-raising method as broken.
- [x] 1.3 Implement `LamarckianInheritance(elite_count: int = 1)`: `select_parents(gen_ids: list[str], fitnesses: list[float], generation: int) -> list[str]` zips the two lists, sorts by fitness desc and breaks ties on `genome_id` lexicographic order, returns the top-`elite_count` IDs; `assign_parent(child_index: int, parent_ids: list[str]) -> str | None` returns `parent_ids[child_index % len(parent_ids)]` or `None` when `parent_ids == []`; `checkpoint_path(output_dir: Path, generation: int, genome_id: str) -> Path` returns `output_dir / "inheritance" / f"gen-{generation:03d}" / f"genome-{genome_id}.pt"`.
- [x] 1.4 Re-export `InheritanceStrategy`, `NoInheritance`, `LamarckianInheritance` from `quantumnematode/evolution/__init__.py`.
- [x] 1.5 Module docstring lists tournament / roulette / soft-elite as future strategies behind the same Protocol (so M4's BaldwinInheritance has a documented home to slot into).

## 2. EvolutionConfig + validators

- [x] 2.1 Add `inheritance: Literal["none", "lamarckian"] = "none"` and `inheritance_elite_count: int = Field(default=1, ge=1)` to `EvolutionConfig` in `packages/quantum-nematode/quantumnematode/utils/config_loader.py` (after `warm_start_path`).
- [x] 2.2 Add a `model_validator(mode="after")` on `EvolutionConfig` rejecting: `inheritance != "none"` AND `learn_episodes_per_eval == 0`; `inheritance != "none"` AND `warm_start_path is not None`; `inheritance_elite_count > population_size`; `inheritance != "none"` AND `inheritance_elite_count != 1` (M3 ships single-elite-broadcast only — multi-elite parent selection is M4-or-later scope). Error messages match the wording in the spec scenarios.
- [x] 2.3 Extend `SimulationConfig._validate_hyperparam_schema` (or add a sibling `model_validator`) to reject: `inheritance != "none"` AND `hyperparam_schema is None`; `inheritance != "none"` AND any field in `hyperparam_schema` is in `_ARCHITECTURE_CHANGING_FIELDS` (reuse the existing constant — single source of truth).

## 3. LearnedPerformanceFitness kwargs

- [x] 3.1 Add `warm_start_path_override: Path | None = None` and `weight_capture_path: Path | None = None` kwargs to `LearnedPerformanceFitness.evaluate` in `packages/quantum-nematode/quantumnematode/evolution/fitness.py`. Signature stays back-compatible (defaults are `None`); `EpisodicSuccessRate.evaluate` does not accept the kwargs.
- [x] 3.2 In the warm-start branch (currently the conditional that calls `load_weights(brain, evolution_config.warm_start_path)`), prefer `warm_start_path_override` when set; fall through to `evolution_config.warm_start_path` otherwise. The validator (task 2.2) makes the two mutually exclusive at YAML load time, so in practice only one is ever set.
- [x] 3.3 After the K-train loop completes and BEFORE the L-eval phase begins, if `weight_capture_path is not None`, call `save_weights(brain, weight_capture_path)`. Create the parent directory with `mkdir(parents=True, exist_ok=True)` first.

## 4. EvolutionLoop hook + GC + checkpoint v2

- [x] 4.1 Add `inheritance: InheritanceStrategy = NoInheritance()` constructor kwarg to `EvolutionLoop` in `packages/quantum-nematode/quantumnematode/evolution/loop.py`. Initialise `self._selected_parent_ids: list[str] = []`.
- [x] 4.2 In the per-child loop that builds `eval_args`, compute `child_capture_path = strategy.checkpoint_path(self.output_dir, gen, gid)` (only when `not isinstance(strategy, NoInheritance)`) and `parent_warm_start = strategy.checkpoint_path(self.output_dir, gen - 1, parent_id)` where `parent_id = strategy.assign_parent(idx, self._selected_parent_ids)`. Pass `None` for both when the strategy is no-op (preserves M2 behaviour).
- [x] 4.3 Extend the `_evaluate_in_worker` tuple at [loop.py:96](packages/quantum-nematode/quantumnematode/evolution/loop.py#L96) to carry the two new `Path | None` fields (positional, end of tuple to preserve ordering of existing fields). The worker unpacks them and forwards into `LearnedPerformanceFitness.evaluate` via the new kwargs. `Path` objects pickle cleanly across the pool boundary; no serialisation work needed.
- [x] 4.4 After `optimizer.tell` returns and BEFORE `self._prev_generation_ids = gen_ids`, call `next_selected = strategy.select_parents(gen_ids, fitnesses, gen)`. The `select_parents` signature is `(gen_ids: list[str], fitnesses: list[float], generation: int)` per task 1.3 — IDs + scores, not full `Genome` objects. The full `Genome` instances built inside the lineage loop at [loop.py:326-334](packages/quantum-nematode/quantumnematode/evolution/loop.py#L326-L334) fall out of scope by this point in the existing code; passing only the local `gen_ids` and `fitnesses` lists avoids needing to hoist them.
- [x] 4.5 Implement GC: when `gen >= 1`, delete every file in `<output_dir>/inheritance/gen-{gen-1:03d}/` whose ID is NOT in `self._selected_parent_ids` (the OLD selected set — those parents' children just finished evaluating); always delete every file in `<output_dir>/inheritance/gen-{gen:03d}/` whose ID is NOT in `next_selected`. Use `Path.glob("genome-*.pt")` to enumerate files, then extract the `<gid>` token via `path.stem.removeprefix("genome-")` (cleaner than regex for this fixed pattern), then `unlink(missing_ok=True)` for non-survivors.
- [x] 4.6 Update `self._selected_parent_ids = next_selected` before incrementing `_generation`.
- [x] 4.7 Bump `CHECKPOINT_VERSION` from 1 to 2. Add `selected_parent_ids: list[str]` AND `inheritance: str` (the literal `"none"` / `"lamarckian"` value resolved at construction) to the checkpoint pickle dict in `_save_checkpoint` (snake_case keys, matching the existing `prev_generation_ids` field at [loop.py:203](packages/quantum-nematode/quantumnematode/evolution/loop.py#L203)); restore both in `_load_checkpoint`. In `_load_checkpoint`, after the version check, compare the loaded `inheritance` against the current `EvolutionConfig.inheritance` and raise a clear error if they differ (per spec scenario "Resume rejects mismatched inheritance setting").
- [x] 4.8 Add a `Path.exists()` guard before passing `parent_warm_start` to a worker; if missing, log a `logger.warning` naming the missing genome ID and pass `None` instead (from-scratch fallback). Spec scenario "Resume from checkpoint preserves selected parent IDs" requires this.

## 5. Lineage CSV schema

- [x] 5.1 Add `inherited_from` to `CSV_HEADER` in `packages/quantum-nematode/quantumnematode/evolution/lineage.py`.
- [x] 5.2 Add `inherited_from: str = ""` parameter to `LineageTracker.record`. The loop passes `parent_id or ""` (empty for gen 0, no-inheritance, or fallback).
- [x] 5.3 Confirm `aggregate_m2_pilot.py` reads `history.csv` only (not `lineage.csv`) — no changes needed there. If anything reads `lineage.csv`, update it to handle the new column.

## 6. CLI wiring

- [x] 6.1 Add `--inheritance {none,lamarckian}` argparse flag to `parse_arguments` in `scripts/run_evolution.py` (mirrors `--algorithm`).
- [x] 6.2 Thread the override into `_resolve_evolution_config` (`if args.inheritance is not None: overrides["inheritance"] = args.inheritance`). Pydantic re-validation catches invalid combinations at startup.
- [x] 6.3 In `main()`, after `evolution_config` is resolved, construct the strategy: `LamarckianInheritance(elite_count=evolution_config.inheritance_elite_count)` if `evolution_config.inheritance == "lamarckian"` else `NoInheritance()`. Pass `inheritance=strategy` into the `EvolutionLoop` constructor.
- [x] 6.4 Update the existing comment near `run_evolution.py:336-345` that says "Lamarckian inheritance, which is out of scope" — note that Lamarckian is now in scope via `evolution.inheritance`, but the existing weight-encoder + LearnedPerformanceFitness guard stays (the validator now also rejects `inheritance: lamarckian` + weight encoder for the same reason).

## 7. Tests

- [x] 7.1 New `packages/quantum-nematode/tests/quantumnematode_tests/evolution/test_inheritance.py`: 12 tests covering `NoInheritance` no-op contract, `LamarckianInheritance.select_parents` top-K + lex tie-break + length-mismatch raise, `assign_parent` round-robin + empty + single-elite, `checkpoint_path` round-trip, constructor guard against `elite_count < 1`.
- [x] 7.2 New `test_weight_capture.py`: 5 tests covering `weight_capture_path` triggers `save_weights` exactly once after train + before eval (mocked); `weight_capture_path=None` writes nothing; `warm_start_path_override` triggers `load_weights` with the override path (mocked); override wins over `evolution.warm_start_path` when both present at fitness level (validator forbids the combination at YAML level); neither set → no load call.
- [x] 7.3 Modify `test_loop_smoke.py`: added `test_loop_with_lamarckian_inheritance_3_gens` (12 lineage rows, gen 0 from-scratch, gen 1+ shares one parent, gen-002 has 1 file), `test_inheritance_directory_garbage_collection` (4-gen run leaves only gen-003), `test_lamarckian_resume_rejects_inheritance_mismatch`. Updated `test_checkpoint_contains_required_keys` for the v2 key set (+selected_parent_ids, +inheritance with default-no-op values).
- [x] 7.4 Modify `test_config.py`: 8 new tests — 6 validator-rejection tests (one per spec rule) + happy-path lamarckian YAML loads cleanly + defaults preserve pre-M3 behaviour.
- [x] 7.5 Modify `test_lineage.py`: `test_lineage_records_inherited_from` (round-trip 6th-column write+read with parent IDs) + `test_lineage_inherited_from_defaults_to_empty` (callers omitting the kwarg get the empty string). Updated `test_csv_header_columns` for the 6-column shape.
- [x] 7.6 Confirm full evolution test suite passes: `uv run pytest packages/quantum-nematode/tests/quantumnematode_tests/evolution/ -v` — 148 tests pass (was 118 in M2; +30 new + minor updates to 2 existing tests). All M2 tests on `inheritance: none` paths still pass byte-for-byte.

## 8. Pilot config + campaign scripts

- [x] 8.1 New `configs/evolution/lamarckian_lstmppo_klinotaxis_predator_pilot.yml` — clone of `configs/evolution/hyperparam_lstmppo_klinotaxis_predator_pilot_tpe.yml` with `inheritance: lamarckian` + `inheritance_elite_count: 1` added to the `evolution:` block AND the schema's `rnn_type` + `lstm_hidden_dim` entries DROPPED (architecture-changing fields are incompatible with per-genome warm-start; the validator on `SimulationConfig._validate_hyperparam_schema` rejects them under inheritance). The remaining 4 evolved fields are `actor_lr`, `critic_lr`, `gamma`, `entropy_coef`; `rnn_type` defaults to "gru" and `lstm_hidden_dim` to 64 in the brain block (matching M2.12 defaults). Same K=50 / L=25 / 4 seeds (42-45) / parallel=4 / pop=12 / 20 gens.
- [x] 8.2 New `scripts/campaigns/phase5_m3_lamarckian_lstmppo_klinotaxis_predator.sh` — clone of `scripts/campaigns/phase5_m2_hyperparam_lstmppo_klinotaxis_predator_tpe.sh` with `OUTPUT_ROOT=evolution_results/m3_lamarckian_lstmppo_klinotaxis_predator` and the new CONFIG path. Header docstring re-targeted at M3 (explains schema shrink + control arm).
- [x] 8.3 New `scripts/campaigns/phase5_m3_lamarckian_lstmppo_klinotaxis_predator_control.sh` + matching YAML `configs/evolution/lamarckian_lstmppo_klinotaxis_predator_control.yml` — within-experiment from-scratch control. Same brain + sensing + 4-field schema + K/L budget as the lamarckian arm; only `inheritance: none` differs. The control YAML is a sibling of the pilot YAML rather than a reuse of M2.12's TPE config because the M3 schema has 4 fields (M2.12 had 6) and we want the only variable in the comparison to be inheritance, not "M3 schema vs M2.12 schema". `OUTPUT_ROOT=evolution_results/m3_lamarckian_lstmppo_klinotaxis_predator_control`. Same 4 seeds.
- [x] 8.4 No new run_simulation.py-driven baseline — reuse `scripts/campaigns/phase5_m2_hyperparam_lstmppo_klinotaxis_predator_baseline.sh` (optimiser- and inheritance-independent; re-run under the M3 revision per task 9.6 below).

## 9. Aggregator + decision logic

- [x] 9.1 New `scripts/campaigns/aggregate_m3_pilot.py` taking `--lamarckian-root`, `--control-root`, `--baseline-root`, `--seeds 42 43 44 45`, `--output-dir`. Reads `history.csv` from each seed under each arm. Argparse `--baseline-root` help text documents the canonical path (`evolution_results/m2_hyperparam_lstmppo_klinotaxis_predator_baseline/`, produced by task 9.6).
- [x] 9.2 Generate two-curve plot: x-axis = generation, y-axis = `best_fitness` per generation (the column written by `EvolutionLoop._write_artifacts` to `history.csv`); for each arm plot the across-seed mean as a solid line with a ±1 std shaded band. Lamarckian and control on the same axes, distinct colours; the `run_simulation.py` baseline mean as a horizontal dashed line. `best_fitness` is the right statistic because the GO/PIVOT/STOP gate (task 9.4) measures generations-to-0.92 on best fitness, not mean. Save as `convergence.png` under `--output-dir`.
- [x] 9.3 Per-seed table: generation at which best fitness first reaches ≥ 0.92 (write as `convergence_speed.csv`).
- [x] 9.4 Decision verdict per the spec / plan: GO if both `mean_gen_lamarckian_to_092 + 4 ≤ mean_gen_control_to_092` AND `mean_gen1_lamarckian ≥ mean_gen3_control`. PIVOT if exactly one. STOP if neither. Write to `summary.md`.
- [x] 9.5 **Decided post-pilot: DROPPED.** No TPE posterior collapse symptom observed — lamarckian saturated at fitness 1.00 across all 4 seeds with hyperparameters spanning ~6× actor_lr range and ~120× entropy range (logbook 013 § Hyperparameter spread analysis). The diagnostic plot was conditional on observing the collapse symptom; with the symptom absent, building the plot would speculation rather than diagnosis. The plot remains a known-cheap follow-up if M4 (or a multi-elite re-run post-M3) ever exhibits the symptom.
- [x] 9.6 Re-ran the M2.11 baseline campaign under the M3 revision via `OUTPUT_ROOT=evolution_results/m2_hyperparam_lstmppo_klinotaxis_predator_baseline scripts/campaigns/phase5_m2_hyperparam_lstmppo_klinotaxis_predator_baseline.sh`. **Wall-time ~2 min total** for 4 seeds × 100 episodes (much faster than the original ~70-min estimate; `--theme headless --log-level WARNING` keeps it lean — no rendering, minimal logging overhead). Per-seed: 42→15%, 43→16%, 44→15%, 45→22%, mean **17.00%** — exactly matches M2.11's published 0.15/0.16/0.15/0.22 mean 0.170. Confirms `run_simulation.py` is reproducible across the M2/M3 revisions. Logs archived under `artifacts/logbooks/013/m3_lamarckian_pilot/baseline/seed-*.log`.

## 9b. Pre-pilot smoke (reduced K=50/L=25 run)

Mechanical correctness check at real per-episode budget before committing ~100 min of full pilot wall-time. Catches silent failures of the kind logbook 012 documented (env mis-construction, sensing-mode mismatch, per-episode reseed bugs) AND the new M3 risks (parent checkpoint round-trip on a real LSTMPPO brain, multiprocessing serialisation under `parallel_workers=4`, GC race with concurrent worker writes).

- [x] 9b.1 Lamarckian smoke: ran via `scripts/run_evolution.py --config configs/evolution/lamarckian_lstmppo_klinotaxis_predator_pilot.yml --fitness learned_performance --generations 3 --population 6 --seed 42 --output-dir evolution_results/m3_smoke_lamarckian/`. Wall-time **~85s** (much faster than the original 12-15 min estimate — predator deaths shorten episodes aggressively per logbook 012). Exit 0; all artefacts present (best_params.json, checkpoint.pkl, history.csv, lineage.csv, inheritance/).
- [x] 9b.2 Control smoke: same overrides against `lamarckian_lstmppo_klinotaxis_predator_control.yml`. Wall-time **~65s**. Exit 0.
- [x] 9b.3 Verified lamarckian smoke artefacts: `lineage.csv` has 18 rows (3 × 6 ✓), gen-0 rows have empty `inherited_from` (all 6 ✓), gen-1 rows all share parent `a582c3bb...` (the gen-0 0.84 elite — single-elite broadcast ✓), gen-2 rows all share parent `3ff2d162...` (the gen-1 0.76 elite ✓), `history.csv` has 3 rows with stable best=0.84 across gens, `inheritance/gen-002/` has exactly 1 file (`genome-da42c365....pt` — the gen-2 winner), `gen-000/` and `gen-001/` removed entirely (clean GC ✓).
- [x] 9b.4 Verified control smoke artefacts: no `inheritance/` directory created (no-op path ✓), all 18 `inherited_from` cells empty ✓, history.csv plausible (best=0.84 stable; mean=-0.0 at gen 1 reflects TPE exploration with no inheritance head-start, recovers to 0.35 by gen 3). The "M2.12 ballpark of ~0.5-0.8 mean" line in this task was loose — control gen-1 mean dipping to ~0 is normal for TPE-without-inheritance early-gen variance per logbook 012's Part 4 narrative. Importantly: **lamarckian gen-1 mean (0.69) >> control gen-1 mean (0.0)**, which is exactly the M3 floor-metric hypothesis at smoke scale.
- [x] 9b.5 Wall-time check: M2.12 = 14 min/seed for 18000 episode-units at parallel=4 → ~187 ms/episode-unit worker-time. Smoke = 75s × 4 / 1350 = ~222 ms/episode-unit. **Within ~20% of M2.12** ✓.
- [x] 9b.6 Smoke artefacts deleted (`rm -rf evolution_results/m3_smoke_lamarckian evolution_results/m3_smoke_control`) after the full pilot runs completed and the logbook published. Smoke served its purpose (validated framework correctness at real K=50/L=25 + parallel=4 scale before committing the ~2-hour full-pilot wall) and was no longer needed for re-inspection.

## 10. Pilot run + logbook

- [x] 10.1 Ran the lamarckian campaign — **wall ~60 min total** (4 seeds × ~15 min/seed at parallel=4). All 4 seeds reach `best_fitness=1.00` by gen 2-7. Mean gen-to-0.92 = **4.50**.
- [x] 10.2 Ran the control campaign — **wall ~53 min total** (4 seeds × ~13 min/seed at parallel=4; ~2 min/seed faster than lamarckian, dominated by absent `save_weights` IO). All 4 seeds reach best fitness in [0.88, 0.96]; seed 42 saturates at 0.88. Mean gen-to-0.92 = **9.75** (with seed 42 → 21 fallback per the aggregator's "never reached" treatment).
- [x] 10.3 Ran the aggregator — produced `summary.md` (verdict: literal PIVOT under the spec floor metric, but FOUR robustness checks unanimously support GO; see logbook 013 § Decision-gate verdict and § Floor metric was off-by-one), `convergence.png` (two-curve best_fitness mean ± 1 std), and `convergence_speed.csv` (per-seed gen-to-0.92).
- [x] 10.4 Archived per-seed artefacts under `artifacts/logbooks/013/m3_lamarckian_pilot/{lamarckian,control,baseline,summary}/`. The lamarckian arm also includes each seed's surviving final-gen winner checkpoint at `inheritance/genome-*.pt` — these are valid M4 hand-off points.
- [x] 10.5 Published logbook `docs/experiments/logbooks/013-lamarckian-inheritance-pilot.md` mirroring logbook 012's structure: Objective, Background, Hypothesis (5 hypotheses, 4 confirmed + 1 not-observed-as-feared = TPE collapse), Method, Results (per-seed tables for both arms, convergence plot embed, wall-time table, the four cheap robustness checks), Analysis (rescue-seed pattern, realignment phenomenon, TPE collapse non-observation, M4 carry-forward), 10 numbered Conclusions, Next Steps. **Decision: GO ✅** — speed gate +5.25 gens (PASS by 1.3× threshold); corrected floor metric (gen-2 lam vs gen-3 ctrl, the right reference because gen-1 is by-construction identical between arms): +0.42pp PASS.
- [x] 10.6 Published supporting appendix `docs/experiments/logbooks/supporting/013/lamarckian-inheritance-pilot-details.md` with per-seed history tables (full 20 gens per seed per arm), evolved-hyperparameter tables, hyperparameter-spread analysis (rules out TPE collapse), speed-margin sensitivity (5 alternative treatments + threshold sensitivity at 0.80/0.84/0.88/0.92), cross-schema confounder check (M3 4-fld vs M2.12 6-fld), save/load round-trip methodology (18 LSTMPPO trained tensors bit-exact), wall-time analysis, and reproducibility notes.

## 11. Tracker + roadmap updates

- [x] 11.1 Rewrote the M3 sub-tasks in `openspec/changes/2026-04-26-phase5-tracking/tasks.md`: M3.4 dropped (saturated arms can't measure inheritance signal); M3.5 renamed to predator-arm; M3.6 updated to mention TPE base + within-experiment control + K=50/L=25 budget; added M3.9 (within-experiment control + cross-schema confounder check) and M3.10 (pre-pilot smoke at K=50/L=25 scale). Final sub-task count = 10 (was 8). All ticked.
- [x] 11.2 Flipped M3 status header in the tracker to `complete` with a one-paragraph summary covering: decision (GO), speed-gate margin (+5.25 gens), per-seed best fitness (all 4 lamarckian → 1.00 vs control's 0.88-0.96), inheritance-rescues-TPE-unlucky-seed pattern (analogue to M2.12 rescuing M2.11's seed 43), and M4 carry-forward.
- [x] 11.3 Flipped the M3 row in `docs/roadmap.md` Phase 5 Milestone Tracker to `✅ complete` with the GO outcome blurb.
- [x] 11.4 Updated M4 row in tracker: status flipped from `🔲 not started` to `🟢 unblocked` with a note that M4 starts on M3's predator config + TPE + the new `BaldwinInheritance` strategy; M4.5 task description updated to mention M3's per-seed final-gen winner checkpoints under `artifacts/logbooks/013/.../inheritance/genome-*.pt` as valid hand-off points if M4 wants to start from M3's best-known policies.

## 12. Quality gates

- [x] 12.1 `openspec validate --changes add-lamarckian-evolution --strict` passes (also re-verified add-lamarckian-evolution + 2026-04-26-phase5-tracking, both ✓).
- [x] 12.2 `uv run pre-commit run -a` clean (ruff check, ruff format, pyright, mdformat, markdownlint, tests — all pass).
- [x] 12.3 Full project test suite (`uv run pytest -m "not nightly"`) passes — 2304 tests in ~2 min. Evolution suite specifically: 148 pass (118 baseline + 30 new M3 tests).
- [x] 12.4 Branch is PR-ready: framework + tests + pilot + control + baseline + aggregator + logbook 013 + tracker + roadmap all complete; 19 commits on `feat/m3-lamarckian-evolution`; openspec strict-clean; pre-commit clean; full project test suite (2305 tests) passes. PR will be opened separately by the user with the GO verdict + the deferred follow-ups (multi-elite + early-stop-on-saturation + M3 spec-archive — see logbook 013 § Next Steps) noted in the description.
