# Tasks: Baldwin Effect first valid measurement

## 1. Spec self-review (gate before implementation)

- [x] 1.1 Ran `openspec validate add-baldwin-retry` and `openspec validate --all` — both clean (`evolution-framework`, `add-baldwin-retry`, `phase5-tracking` all ✓).
- [x] 1.2 Re-read all four artifacts. F1 evaluator CLI contract is unambiguous after round-1 fix B2 (sim_config plumbing pattern explicitly described). Proposal's "modified capabilities = none expected" is consistent with the actual MODIFIED requirement via Decision 0's escape hatch (correcting M4's design-flawed F1 spec is a "bug fix" per the policy, not a new framework feature) — the qualifier "expected" in proposal § Capabilities leaves room for the bug-fix scope.
- [x] 1.3 Verified schema bounds against `LSTMPPOBrainConfig`. Both new arch knobs (`actor_hidden_dim`, `actor_num_layers`) exist as `int` fields with no Pydantic Field constraints. Bound `actor_hidden_dim ∈ [64, 256]` covers 4× the field default (64); the only existing dim validator is `lstm_hidden_dim < 2` rejected at line 129, well below our range. Bound `actor_num_layers ∈ [1, 3]` covers ±1 around the default (2); checked the constructor at lstmppo.py:373 — `actor_num_layers=1` produces a standard 1-hidden-layer MLP (loop iterates 0 times); `actor_num_layers=3` produces 3 hidden Linears. Both extremes safe. `HyperparameterEncoder.genome_dim` is `len(sim_config.hyperparam_schema)` — trivially returns 8.
- [x] 1.4 Cross-checked Decision 5's gate thresholds. Speed gate `+2` is roughly 2-3σ at n=8 per Decision 4's SE arithmetic (SE ≈ 0.6-0.9 gens). **F1 gate `+0.05` is ~1.4σ at n=8 (per-seed binomial sd at L=25 ≈ 0.1 at p=0.5; SE ≈ 0.035 across n=8 seeds; +0.05 ≈ 1.4σ).** This is tighter than the speed gate's σ multiplier — a truly null Baldwin effect would pass with ~10% probability under noise. The +0.05 threshold trades type-II for type-I error: tighter (+0.10 ≈ 3σ) would fail to detect a real-but-small Baldwin signal at K'=10 (which is an inherently small-budget test where the maximum elite-vs-baseline lift is bounded by what 10 episodes can buy). Defensible with this caveat recorded for the post-pilot audit.

## 2. F1 evaluator redesign

- [x] 2.1 Rewrote `scripts/campaigns/baldwin_f1_postpilot_eval.py` to the paired K'-train + L-eval comparison via `LearnedPerformanceFitness.evaluate`. K and L plumbed via a `sim_config` copy (`model_copy(update={"evolution": evolution.model_copy(update={"learn_episodes_per_eval": k_prime, "eval_episodes_per_eval": episodes})})`) — extracted into the `_build_sim_config_for_kprime` helper. K=0 frozen-eval path removed.
- [x] 2.2 Schema-prior baseline genome synthesised via `encoder.initial_genome(sim_config, rng=np.random.default_rng(seed))`, then mutated to a stable `genome_id` before evaluation. The same per-seed RNG seeds the env's per-episode trajectory in both runs (same seed → same env init), so the only between-arm difference is the genome.
- [x] 2.3 CLI: added `--k-prime` (default 10) and `--episodes` (default 25). Both reject `<= 0` at argparse-time via `parser.error(...)`. `--baldwin-root`, `--config`, `--seeds` (default n=8: 42-49), `--output-dir` preserved.
- [x] 2.4 Append-mode CSV (`open("a", newline="")`): `f1_learning_acceleration.csv` with header `seed, k_prime, episodes, elite_success_rate, baseline_success_rate, signal_delta`. Header written only if the file doesn't yet exist. Re-running with K'=25 after K'=10 appends new rows without losing the prior K'=10 rows (verified by `test_csv_append_mode_preserves_prior_rows`).
- [x] 2.5 Per-seed RNG: same `seed` passed to both `LearnedPerformanceFitness.evaluate` calls within a seed. Verified by `test_initial_genome_same_seed_produces_identical_params` and `test_initial_genome_different_seeds_produce_different_params`.
- [x] 2.6 Unit tests at `packages/quantum-nematode/tests/quantumnematode_tests/evolution/test_baldwin_f1_postpilot_eval.py` (11 tests, all passing): (a) CLI rejection for `--k-prime 0/-5/0` and `--episodes 0`; (b) `initial_genome` returns 8-dim params under an 8-field schema + decode round-trip; (c) append-mode CSV preserves prior rows; (d) per-seed RNG determinism + seed-sensitivity; (e) `_resolve_session` handles both direct and nested layouts; (f) `_build_sim_config_for_kprime` mutates K + L correctly + rejects no-evolution config. Tests use `_make_8field_sim_config` to extend M4's 6-field YAML to 8 fields in-memory (M4.5's 8-field YAML is created in task group 3).
- [x] 2.7 Pre-commit clean on both files (mdformat, markdownlint, ruff check + format, pyright, pytest hook all ✓).

## 3. Pilot configs (8-field schema, n = 8 seeds)

- [x] 3.1 Created `configs/evolution/baldwin_lstmppo_klinotaxis_predator_retry_pilot.yml` with the 8-field schema per Decision 1: 4 hyperparam knobs (actor_lr, critic_lr, gamma, entropy_coef) + 2 innate-bias knobs (weight_init_scale, entropy_decay_episodes) + 2 NEW arch knobs (actor_hidden_dim ∈ [64, 256], actor_num_layers ∈ [1, 3]). Inheritance: baldwin + early_stop_on_saturation: 5. Documented the audit findings + design decisions inline.
- [x] 3.2 Created `configs/evolution/control_lstmppo_klinotaxis_predator_retry_pilot.yml`. Identical to Baldwin config except `inheritance: none`. Schema is byte-identical (same 8 fields, same bounds, same order — verified at YAML load via the schema-equalisation check below).
- [x] 3.3 Lamarckian rerun reuses the existing `configs/evolution/lamarckian_lstmppo_klinotaxis_predator_pilot.yml` (4-field schema) — no new YAML needed. Primary purpose: comparative-gate baseline at n=8; secondary: M3 reproducibility on the n=4 subset.
- [x] 3.4 Hand-tuned baseline reuses M2.11's existing artefacts (4 seeds, 42-45). Note for logbook 015 + aggregator: baseline horizontal line on the convergence plot SHALL be annotated `(n=4 seeds 42-45)` so the n-asymmetry vs the n=8 pilot arms is explicit.
- [x] 3.5 Both new YAMLs parse cleanly via `load_simulation_config`. Verified the schema-equalisation property at YAML level: both YAMLs have IDENTICAL `hyperparam_schema` blocks (8 fields, byte-identical name/type/bounds/log_scale tuples in the same order). Inheritance field is the only diff.

## 4. Campaign scripts

- [x] 4.1 Created `scripts/campaigns/phase5_baldwin_retry_baldwin.sh` — patterned after M4's Baldwin script with two diffs: 8-field config + n=8 seeds (42-49). OUTPUT_ROOT is `evolution_results/baldwin_retry_baldwin_lstmppo_klinotaxis_predator` (distinct from M4's directory).
- [x] 4.2 Created `scripts/campaigns/phase5_baldwin_retry_control.sh`. Same structure as the Baldwin script but uses the matching 8-field control config + `--inheritance none`. OUTPUT_ROOT distinct from Baldwin's.
- [x] 4.3 Created `scripts/campaigns/phase5_baldwin_retry_lamarckian_rerun.sh`. Reuses M3's existing lamarckian config (4-field) under the M4.5 code revision; n=8 seeds. Documented the schema-asymmetry rationale inline (Lamarckian doesn't participate in audit-A1 schema-equalisation; sole role is comparative-gate baseline at n=8). Adds `--early-stop-on-saturation 5` at runtime since the M3 config doesn't set it. OUTPUT_ROOT distinct from M4 Lamarckian rerun's.
- [x] 4.4 Each script's header documents expected wall-time per the design's compute estimate (~2.5-3 hours per arm at parallel=4 with early-stop enabled; total pilot wall ~3-4 hours with all three arms in parallel + F1 post-pilot).
- [x] 4.5 Pre-commit clean on all three scripts (mdformat n/a; bash scripts pass the file-end + large-files + tests hooks). Scripts marked executable via `chmod +x`.

## 5. Aggregator updates

- [x] 5.1 Created new `scripts/campaigns/aggregate_baldwin_retry_pilot.py` (chose new-file over extending M4's aggregator — logbook 014 references `aggregate_m4_pilot.py` by name; modifying it would invalidate that audit trail). Decision documented in the script's module docstring.
- [x] 5.2 Implemented `_check_schema_equalisation` — reads `history.csv` first data row per seed (the framework writes 1-indexed history.csv where the first row labels `generation = 1`; equivalent to `lineage.csv` `generation == 0`), averages across seeds, and forces verdict to INCONCLUSIVE if `|Δ| > 0.05` between Baldwin and Control means. Tests verify pass at `|Δ| = 0.02` and fail at `|Δ| = 0.15`.
- [x] 5.3 Implemented `_compute_verdict` with the recalibrated 3 gates: speed `+2`, F1 `+0.05` (was `+0.10` in M4), comparative `+4`. Reference values are the M4.5 pilot's own arm means (not M4's published). 4-way verdict: INCONCLUSIVE if schema-equalisation fails; GO if all three gates pass; PIVOT if speed only; STOP otherwise. Tests cover all 16 gate combinations.
- [x] 5.4 Implemented `_read_f1_csv(f1_csv, k_prime=...)` — reads `f1_learning_acceleration.csv` with new columns (`seed, k_prime, episodes, elite_success_rate, baseline_success_rate, signal_delta`), filters by `--k-prime` (default 10). Tests verify K' = 10 and K' = 25 rows can coexist + filter selects the requested rows.
- [x] 5.5 Implemented `_plot_convergence` — 4-curve plot (Baldwin + Lamarckian + Control mean ± std bands; baseline horizontal line annotated `(n=4 seeds 42-45)` per round-2 review M5; F1 elite + baseline scatter markers at the right margin distinguishing elite from baseline). Writes `convergence.png` to the output directory.
- [x] 5.6 Implemented `_format_summary` writing `summary.md` with the schema-equalisation header (table + status), 3 gates with margins, verdict + verdict-text, per-seed table including F1 elite + baseline + signal columns. INCONCLUSIVE path skips the gates section + emits the audit-A1-not-closed text directly.
- [x] 5.7 Unit tests at `packages/quantum-nematode/tests/quantumnematode_tests/evolution/test_aggregate_baldwin_retry.py` (11 tests, all passing in 0.26s): schema-equalisation check (3 tests covering pass/fail/at-threshold with fp arithmetic note); verdict logic (4 tests covering INCONCLUSIVE / GO / PIVOT / STOP across gate combinations); F1 CSV K' filter (3 tests + missing-file error path); helpers exposure.

## 6. Pre-pilot smoke (gate before full pilot launch)

- [x] 6.1 Schema-equalisation property PASSED at smoke scale.  Both arms (1 seed × 1 gen × pop=4) produced byte-identical output: first-gen `best=0.16, mean=0.04, std=0.0693` for both Baldwin and Control, with the same 4 child genome IDs in the same order and the same per-child fitnesses.  TPE with identical schema + identical seed produces identical samples in identical order — audit A1 closure mechanism confirmed.  Aggregator's pre-flight check (|Δ| ≤ 0.05) will trivially pass at full pilot scale.  Wall: ~14s per arm.
- [x] 6.2 F1 evaluator mechanical contract VERIFIED.  Append-mode CSV worked correctly across two K' invocations (K'=10 then K'=25); both rows coexist with the right columns. Both K' values produced 0/0 elite/baseline at this smoke scale because the "elite" is the best of 4 random gen-0 genomes (best fitness 0.16) — not really evolved. This is the expected smoke result; signal-detection power awaits the full pilot's gen-20 elite.  Risk 4's K'=25 fallback path works mechanically.
- [x] 6.3 Gen budget probe (3 gens × pop 6 × seed 42) trajectory: `[0.16, 0.88, 0.96]`.  TPE hits 0.96 by gen 3 — well above the 0.92 target.  Risk 1 dispelled: **20-gen budget is plenty**, no need to bump to 25 or 30.  At full pilot's pop=12 expect saturation in 3-7 gens (mirrors M3 lamarckian's pattern + early-stop will fire). Wall: 1m 9s for 18 evals at parallel=4 (~3.83s per eval).
- [x] 6.4 Wall-time projection: full pilot per arm ≈ ~3.4h WITHOUT early-stop; with `early_stop_on_saturation: 5` firing (30-50% wall savings per M3/M4 pattern) → per-arm ~2-2.5h; 3 arms in parallel + F1 post-pilot → **total pilot wall ~3h**.  Comfortably below the 6h Risk 5 threshold.  **n=8 holds** (no need to drop to n=6).

## 7. Pre-pilot user review checkpoint

- [x] 7.1 Presented user with: smoke task 6.1-6.4 results (audit A1 mechanism confirmed at smoke scale; 8-field TPE saturates by gen 3 — 20-gen budget is plenty; F1 evaluator mechanical contract verified; wall-time projection ~3h). User approved pilot launch after the implementation-review pass that caught + fixed Bug 3 (empty `history.csv` guard).

## 8. Run full pilot

- [x] 8.1 Launched all three arms (Baldwin + Control + Lamarckian rerun) in parallel via the three campaign scripts at 2026-05-03T14:50:28Z. n=8 seeds (42-49) per arm with `early_stop_on_saturation=5` enabled.
- [x] 8.2 Monitored pilot progress via background-task notifications. No crashes; all 24 seed-arm combos completed cleanly. Lamarckian arm done at 18:43:25Z, Baldwin + Control at 18:56:22-25Z. Wall ~4h 6min total (longer than smoke's 3h projection because early-stop fired on only 1/8 Baldwin seeds — most seeds ran the full 20-gen budget).
- [x] 8.3 Ran F1 evaluator at K'=10 (gate default) AND K'=25 (Risk 4 fallback). K'=10 gave all-zeros across 8 seeds (too small a budget). K'=25 produced real signal (mean elite 0.110 vs baseline 0.000) — but elites are bit-identical to Control's elites, so the F1 signal isn't a Baldwin-vs-Control comparison. Both sets of rows preserved in the same `f1_learning_acceleration.csv` per the append-mode contract.
- [x] 8.4 Ran aggregator at K'=10 (gate default) AND K'=25 (sensitivity). Aggregator emitted STOP per its 3-gate logic (Speed FAIL margin +0.00; F1 FAIL signal +0.000 at K'=10; Comparative PASS margin +0.38 — Baldwin 6.38, Lamarckian 2.75). Schema-equalisation pre-flight: PASS, |Δ| = 0.0000.

## 9. Post-pilot REVIEW CHECKPOINT (gate before logbook 015)

- [x] 9.1 Compiled the complete post-pilot working summary in the scratchpad — pilot wall-times, schema-equalisation status (perfect closure |Δ| = 0.0000), per-seed gen-to-092 across 3 arms, F1 results at both K'=10 and K'=25, forensic decoding of all 8 Baldwin elite hyperparam genomes (revealing TPE explored ALL 8 fields including the new arch knobs — `actor_hidden_dim` consistently above default 64), trajectory comparisons (Baldwin = Control bit-identical at every gen), proposed verdict explicitly marked PROPOSED.
- [x] 9.2 Presented the working summary to the user with the structural finding (Baldwin and Control are bit-identical because `inherited_from` is metadata-only and TPE's selection doesn't see the lineage trace) and three verdict options (1: ship STOP as-is; 2: ship STOP + lessons addendum; 3: PIVOT to a B2 redesigned abstraction).
- [x] 9.3 User chose a fourth option (the **iterative-until-valid** framing): close M4.5 with the structural finding documented, keep logbook 015 IN PROGRESS, and address the abstraction redesign in a follow-up M4.6 PR. This avoids both an over-strong STOP claim (one design attempt isn't sufficient evidence for "Baldwin doesn't exhibit on this testbed") and the scope creep of folding the abstraction redesign into this PR.

## 10. Logbook 015 (post-approval)

- [x] 10.1 Created `docs/experiments/logbooks/015-baldwin-iterative-evaluation.md` (a single iterative-evaluation logbook, NO supporting appendix yet — the iteration spans M4.5 + future M4.6, so the appendix will be added once M4.6 results land if needed). Structure: Objective / Background / Iteration step 1 — M4.5 (Hypothesis / Method / Results / Analysis / Iteration verdict) / Iteration step 2 — M4.6 (planned, scope only) / Conclusions (iteration step 1) / Next Steps / Data References.
- [x] 10.2 Forward-references audit findings A1-A5 from logbook 014 in the Background section. Each M4.5 design change is mapped to its audit finding (Decision 1 → A5, Decision 2 → A1, Decision 3 → A2 + A3, Decision 4 → A4, Decision 5 → recalibration).
- [x] 10.3 Schema-equalisation check result documented: |Δ| = 0.0000 (perfect closure of audit A1). M4's measured |Δ| = 0.14 (with mismatched schemas) is now |Δ| = 0.0000 (with matched schemas).
- [x] 10.4 Both K' = 10 and K' = 25 F1 results documented. K'=10 (gate default) gave all-zeros; K'=25 (Risk 4 fallback per design) gave a real signal (mean +0.11) — but the elite is bit-identical to Control's elite so the signal isn't a Baldwin-vs-Control comparison.
- [x] 10.5 Documented the head-to-head finding from Decision 1: TPE explored ALL 8 fields broadly, NO fields pinned at defaults. `actor_hidden_dim` consistently lands at 149-251 (above default 64); `entropy_decay_episodes` consistently 453-1655 (above default 500); `weight_init_scale` 0.751-1.509; etc. **Audit A5 partially answered**: arch knobs ARE actively explored. But this exploration happens identically in Baldwin and Control, so the hyperparam discovery is verdict-orthogonal.
- [x] 10.6 Wrote the iteration-step-1 verdict (NOT a final GO/PIVOT/STOP; the iteration framing means the verdict is "structural finding identified — framework abstraction needs revision; M4.6 will fix"). Per the user-approved iterative framing, M5/M6 dependencies stay deferred (NOT "M5 proceeds without Baldwin" yet — that was Decision 6's STOP-only conclusion). Decision 6's STOP semantic was reinterpreted: it scoped to "redesigned gates STOP after audit fixes", not "abstraction is the wrong substrate from a clean evaluation".
- [x] 10.7 Copied artefacts to `artifacts/logbooks/015/m4_5_pilot/{baldwin,control,lamarckian,baseline,summary,summary_kprime25}/`. 8 seeds × 3 arms (best_params.json + history.csv + lineage.csv + checkpoint.pkl per seed; plus inheritance/ subdir for Lamarckian's surviving elite). Baseline = M2.11's reused logs. Summary directories include the K'=10 (gate default) AND K'=25 (sensitivity) aggregator outputs.
- [x] 10.8 Updated `docs/experiments/README.md` index with the new logbook 015 entry — IN PROGRESS status, full M4.5 + M4.6 framing.
- [ ] ~~10.9 Append a one-line "Update" pointer at the top of logbook 014 linking to logbook 015~~ — **DEFERRED**. Logbook 014 already references M4.5 prominently in its header (line 3 + line 11 + § Audit + § Decision + § Next Steps). Adding a one-line pointer would be redundant and require editing logbook 014's prose. Will revisit at M4.6 close (when logbook 015 has both iteration steps complete) if a pointer is still useful.

## 11. Tracker + roadmap update

- [x] 11.1 Updated `openspec/changes/2026-04-26-phase5-tracking/tasks.md` M4.5 section: M4.5.1-M4.5.6 ticked with full descriptions of what each design change accomplished + the audit finding it addressed; M4.5.7 explicitly marked DEFERRED to M4.6.7 with rationale (a final verdict-flip from M4.5 alone would over-claim or require Decision 6's STOP semantic, but Decision 6 was scoped to "redesigned gates STOP after audit fixes"). New M4.6 section added with 7 sub-tasks scoping the abstraction redesign + iteration step 2.
- [x] 11.2 M4 row in phase5-tracker stays as INCONCLUSIVE per the iterative-until-valid framing (not ticked as complete; M4.6 will close the iteration).
- [x] 11.3 Updated `docs/roadmap.md` M4 row. Reframed from "INCONCLUSIVE; M4.5 is follow-up" to "iterative evaluation IN PROGRESS; M4.5 step 1 STRUCTURAL FINDING; M4.6 step 2 PLANNED". M5/M6 dependencies stay deferred.

## 12. Pre-PR verification

- [x] 12.1 Ran `uv run pre-commit run -a`: clean (mdformat, markdownlint, ruff check + format, pyright, pytest hook all ✓). Verified at the post-Bug-3-fix state (commit 8e06904d) and again at the post-iteration-framing state.
- [x] 12.2 Ran `uv run pytest -m "not nightly" --tb=short -q`: 2359 passed (M4.5 added 22 new tests + Bug 3 fix added 2 = 24 net new). Zero regressions vs M4 baseline (was 2334).
- [x] 12.3 Ran `openspec validate add-baldwin-retry`: clean. `openspec validate --all` ✓ for evolution-framework + add-baldwin-retry + phase5-tracking.
- [x] 12.4 Self-reviewed branch diff for milestone refs in implementation code. Reuse policy holds: all M4.5/Phase-5/RQ references are confined to docs (logbook 015, tracker, roadmap, OpenSpec proposal/design) + per-campaign scripts (`scripts/campaigns/phase5_baldwin_retry_*.sh`, `aggregate_baldwin_retry_pilot.py`, `baldwin_f1_postpilot_eval.py`) + per-campaign configs (`configs/evolution/{baldwin,control}_lstmppo_klinotaxis_predator_retry_pilot.yml`) — all of which are exempt from the no-milestone-refs rule per the established convention. No milestone refs leaked into framework code (`packages/quantum-nematode/quantumnematode/`).
- [x] 12.5 Final cleanup committed (Bug 3 fix at 8e06904d; iteration framing reframe at this commit point). User STOP for branch review BEFORE push (this PR's PR-time review checkpoint).

## 13. Open PR

- [ ] 13.1 Open draft PR via `gh pr create --draft` — **PENDING USER APPROVAL** to push and open. Title: `feat(baldwin-retry): M4.5 audit closures + structural finding (logbook 015 step 1; M4.6 follow-up planned)`. Body will summarise: the audit findings A1-A5 that motivated M4.5; what M4.5 closed; the structural finding; the iteration-step-1 framing; the M4.6 follow-up plan; reusable infrastructure shipped. Links logbook 015 + tracker M4.5 + M4.6 sections.

## 14. Post-merge

- [x] 14.1 Archive the OpenSpec change via `openspec-archive-change` skill — **TO BE DONE IN THIS PR** (per user direction: archive `add-baldwin-retry` at M4.5 merge, M4.6 will be a separate change). Includes the delta sync from `specs/evolution-framework/spec.md` (the modified `Baldwin Inheritance Strategy` requirement + the new F1 evaluator scenario) into `openspec/specs/evolution-framework/spec.md`.
- [ ] 14.2 Confirm tracker + roadmap reflect the post-merge state — done at PR-time (above task 11). Will re-verify after PR merges.
