> **Implementation status (PR part 1 — foundation only).** This PR contains the T5 change definition + the **shared action-policy module and brain migrations (§1 + §2)** plus the two SHOULD pull-forwards (§8.0) — a behaviour-preserving refactor (byte-exact for MLP/connectome/equivariant-quantum; ~1e-7 declared tolerance for LSTM/CfC/spiking; every existing brain suite green). **Remaining for follow-up PRs off main:** §3 continuous-2D env, §4 continuous heads + connectome adapter, §5 transformer + Gate-2 parity, §6 floor check, §7 logbook + Gate-2 decision + tracker/roadmap flip, §8.1 SHOULD/MAY continuous heads. The change stays **active** (not archived) until those land. Migration-regression gate (§2.5) is GREEN.

## 1. Shared action-policy module + action contract (discrete parity first)

- [x] 1.1 Create `brain/arch/_policy.py`: a shared helper/mixin exposing build-head, sample, `log_prob`, `entropy`, and PPO surrogate terms, with a discrete (Categorical) mode that reproduces the current per-brain logic. *(Done: `categorical_sample_torch` + `categorical_evaluate_torch` (MLP/connectome/equivariant-quantum torch path), `categorical_logprob_entropy_torch` (LSTM/CfC/spiking given-action path, Option B), `ppo_clip_policy_loss`. Continuous tanh-Gaussian helpers land in §4.)* Follow the leaf-module precedent (`_ppo_buffer.py`, `_brain.py`): underscore-prefixed, imported directly by brain modules, NOT re-exported through `brain/arch/__init__.py` (which imports the brain modules and would invert the dependency / risk a cycle).
- [ ] 1.2 Extend the action contract: relax `ActionData.action` from required `Action` to `Action | None` (the runner already null-guards it at `agent/agent.py:855`, `runners.py:983/985`) and add an optional `continuous: tuple[float, float] | None` field; define how the carrier conveys `(speed, turn)`. Grid substrate keeps emitting `action: Action`. *(Partial: additive `continuous` field added now — zero blast radius. The `action → Action | None` relaxation is deferred to the continuous-2D wiring commit where the consuming dispatch exists: a blast-radius check confirmed relaxing it now breaks pyright at `runners.py:720`/`:802` with no consumer yet, so it lands coupled with task 1.3 + §3.)*
- [ ] 1.3 Route the agent/runner action-application to dispatch on the environment's action mode (grid → discrete `Action`; continuous-2D → continuous vector), with NO branch on the concrete brain class. Cover BOTH action-consumption surfaces: the single-action path (`runners.py:720`) AND the many-worlds runner-up path (`runners.py:983-1010` + `.action.value` logging at `:802`) — route both through the mode dispatch or guard many-worlds as discrete-only on the continuous substrate. Generalise the `move_agent(action: Action)` signature to carry the continuous vector. Explicitly guard the `top_action.action.value` log line (`runners.py:802`) so it does not `AttributeError` when `action` is `None` in continuous mode. **§1.3 readiness checklist (2026-06-06 review):**
  - **dispatch signal**: route on `env.continuous_actions` (the env property added in §3.5), not the brain class — Gate-2-safe.
  - **🔴 `reset_environment()` recreates the grid class** (`agent/agent.py:1328` hardcodes `DynamicForagingEnvironment(grid_size=…)`): on a continuous run the env silently reverts to grid after the first reset. MUST recreate the same env type with `Continuous2DParams` (polymorphic env-recreate seam, or an isinstance branch that keeps the grid path byte-identical). Same hardcoded-class issue in `env.copy()` (`env.py:3984`) for many-worlds.
  - **many-worlds**: guard as not-yet-supported on continuous (it needs the `copy()` override); focus single-world for the first continuous run. Cover the `runners.py:983-1010` runner-up path accordingly.
  - **dependency**: §3.5 + §1.3 make the env sensed + acted, but a real continuous *training* loop / G2.d floor check is **§4-gated** (brains must emit `(speed,turn)` via continuous heads); test §3.5/§1.3 via the env + a stub continuous action until then.
- [x] 1.4 Unit tests for the shared module's discrete mode (sample/log_prob/entropy) against the pre-refactor reference outputs. *(`tests/.../brain/arch/test_policy.py`: torch + numpy samplers, batch evaluate, PPO surrogate — all assert byte-equivalence to the inline ops; 6 pass.)*

## 2. Migrate MUST brains to the shared module + regression bar (GATE before continuous work)

- [x] 2.1 Migrate MLP-PPO discrete path onto `_policy.py`. *(Done: `get_action_and_value` sampling → `categorical_sample_torch`; `_perform_standard_ppo_update` → `categorical_evaluate_torch` + `ppo_clip_policy_loss`. Byte-equivalent: golden regression `test_mlpppo_policy_migration.py` (fixed `config.seed`) + full `test_mlpppo.py` suite green — 66 tests.)*
- [x] 2.2 Migrate LSTM-PPO discrete path onto `_policy.py` (handle the TEI-bias-shape coupling). *(Done, Option B: numpy `rng.choice` sampler kept verbatim → trajectory byte-identical; rollout + BPTT-update log-prob/entropy → `categorical_logprob_entropy_torch`; surrogate → `ppo_clip_policy_loss` (clip-fraction `ratio` retained). TEI per-step bias replay unaffected. ~1e-7 declared tolerance. 72 tests green incl. recurrent-stability + TEI-prior suites.)*
- [x] 2.3 Migrate CfC-PPO discrete path onto `_policy.py` (handle the AutoNCP motor-count coupling). *(Done, Option B: numpy sampler kept verbatim; rollout + BPTT-update log-prob/entropy → `categorical_logprob_entropy_torch`; surrogate → `ppo_clip_policy_loss`. AutoNCP motor head untouched. ~1e-7 tolerance. 57 tests green.)*
- [x] 2.4 Migrate connectome-PPO discrete path onto `_policy.py`. *(Done, **byte-exact** — torch `Categorical` path like MLP: rollout → `categorical_sample_torch` (probs retained for `current_probabilities`); update → `categorical_evaluate_torch` + `ppo_clip_policy_loss`. Strict-mask post-step reprojection untouched. 37 tests green (test_connectome_ppo + vectorisation).)*
- [x] 2.5 Migration-regression bar: byte-equivalence for MLP-PPO + LSTM-PPO on one fixed-seed discrete smoke config each (pre/post); CfC + connectome within a declared, documented seeded-RNG tolerance. Record results; do not proceed to §4 until green. **GATE GREEN.** Per the measured Option-B decision (design.md D6): **MLP-PPO byte-exact** (fixed-`config.seed` golden, pre/post identical) + **connectome-PPO byte-exact** (torch path, composition proof + 37-test suite); **LSTM-PPO + CfC-PPO** keep their numpy sampler verbatim (trajectory byte-identical) and route log-prob/entropy/surrogate through torch helpers — **~1e-7 declared tolerance** on the taken action (a numerical-stability improvement), full behavioural suites green. Byte-equivalence is independently guaranteed by composition: `test_policy.py` proves the shared helpers are `torch.equal` to the inline ops. *(Finding: brains entropy-seed at construction unless `config.seed` is set.)*

## 3. Continuous-2D environment + config discriminator

- [x] 3.1 Add `EnvironmentConfig.env_type` discriminator (`"grid"` default | `"continuous_2d"`) + continuous-env config fields (world bounds, body-length scale, max step displacement, capture radius, sweep amplitude) with documented defaults. *(Done: `Continuous2DConfig` (world_size_mm=50, body_length_mm=1, max_step_mm=1, capture_radius_mm=1, sweep_amplitude_mm=0.5 — biologically-grounded, documented) + `EnvironmentConfig.env_type`/`continuous` + `get_continuous_config()` getter. Backward-compat: grid configs unchanged (222-test compat suite green). Tested in `tests/.../utils/test_continuous_env_config.py`.)*
- [ ] 3.2 Add env-type dispatch in `create_env_from_config`; generalise the `EnvironmentType` alias to the base/union; route the direct `DynamicForagingEnvironment(...)` construction sites that bypass the factory. *(Done: factory `create_env_from_config` dispatches on `env_type` → `Continuous2DEnvironment` (mapping `Continuous2DConfig`→`Continuous2DParams`) | grid; factory-dispatch tests (§3.8) green. **`EnvironmentType` alias needs NO change** — `Continuous2DEnvironment` subclasses `DynamicForagingEnvironment`, so the existing alias already covers it (a payoff of the parent refinement). **Remaining**: route the other direct construction sites (`agent/agent.py` ×2, `scripts/export_screenshot.py` ×2 — fallback/default constructions); `env.copy()` override for many-worlds-on-continuous + the runner action-mode wiring land with §1.3.)*
- [ ] 3.3 Implement `Continuous2DEnvironment(DynamicForagingEnvironment)` *(parent refined from `BaseEnvironment` 2026-06-05 — see design.md D1: reuse the already-continuous sensing/source/state machinery, override only grid-coupled methods)*: float positions via an **additive optional** `AgentState.pos_continuous: tuple[float,float] | None` (+ `heading_rad`), NOT by relaxing `position: tuple[int,int]` (relaxing cascades pyright errors through grid int-consumers — readiness finding); the continuous env holds float truth + syncs a rounded int `position` view. Kinematic `(speed, turn)` movement with world-bound clamping, point-worm body. **Inheritance-leak audit checklist** (the enumerated int-position surface to override/guard; audit done 2026-06-06, Option-A viable — surface is bounded):
  - **heading**: add additive `AgentState.heading_rad: float = 0.0` (grid ignores it); kinematic move rotates it by `turn`, advances `speed·(cos,sin)`.
  - **`__init__`**: map `world_size_mm` → parent coordinate extent (`grid_size=int(world_size_mm)`) so fields/viewport build for free; store `Continuous2DConfig`.
  - **movement**: override `_apply_movement` (+ the `move_agent`/`_get_new_position_from` path) → kinematic seam `_kinematic_move(state, speed, turn)`; world-bound clamp; point-worm `body=[position]`.
  - **start position**: float centre `(world/2, world/2)` (parent uses `grid_size//2`).
  - **`visited_cells`**: adapt/disable for float point-worm (verify consumers — exploration metric/reward); don't accumulate float cells.
  - **collective int-cast** (`(int(a.position[0]), …)`, ~env.py:1106): verify/adapt under float positions.
  - **`render`**: headless-first stub (don't crash on floats); full continuous renderer is T6.render.
  - food-capture / placement / Euclidean distance → §3.4; klinotaxis continuous sampling (uses `heading_rad` + `sweep_amplitude_mm`) → §3.5. Predator contact-zone cardinal bearings: note as a known simplification (angular version when predator-on-continuous is exercised).
- [ ] 3.4 Capture-radius food consumption (replace cell-equality); continuous source placement (replace integer sampling); Euclidean distances for pheromone / predator-mechano / nearest-food. *(Done: `reached_goal_for`/`consume_food_for` overridden → Euclidean capture-radius (nearest-within-radius); `get_nearest_food_distance`(`_for`) → Euclidean. 7 capture-radius tests. **Deferred**: float source *placement* — foods stay on the integer lattice within the continuous arena (worm + capture fully continuous) to avoid the `self.foods: list[tuple[int,int]]` type ripple (same class as the position refinement); documented in `continuous_2d.py`. Pheromone/predator-mechano Euclidean is agent-side, lands with §3.5.)*
- [ ] 3.5 Physically-scaled klinotaxis lateral sampling on continuous coordinates; drop the integer-cell offset + `int(...)` position cast; feed STAM continuous readings. **§3.5 readiness checklist (2026-06-06 review):**
  - **continuous-mode signal**: add `BaseEnvironment.continuous_actions: bool` property (False) overridden True on `Continuous2DEnvironment`; the agent/runner dispatch on it (env-driven, not brain-coupled — Gate-2-safe).
  - **agent sensing branch** (`agent/agent.py`): when `env.continuous_actions`, use `agent_state.pos_continuous` as the sense position (not the int `position` at `:697`) and compute float lateral offsets from `agent_state.heading_rad` + `sweep_amplitude_mm` (not `_compute_lateral_offsets` ±1-cell off the `Direction` enum at `:758-797`). `env.get_*_concentration(position=…)` already accept floats — only the position source + lateral-offset computation branch. Relax the `int(current_pos)` STAM cast (`:858`).
  - STAM is coordinate-agnostic (stores deltas + channel scalars); feed it the float readings.
- [ ] 3.6 Unit tests: kinematics + world-bound clamping; capture-radius consumption (in/out of radius); continuous placement validity; Euclidean fields; klinotaxis continuous dC + STAM dC/dt.
- [ ] 3.7 Verify the grid environment is unchanged (T4 grid regression on an existing scenario).
- [x] 3.8 Factory-dispatch tests (`environment-simulation` spec): `env_type: continuous_2d` → `Continuous2DEnvironment`; omitted/`grid` → `DynamicForagingEnvironment`; callers route through `create_env_from_config`, not a concrete-class alias. *(Done: `TestFactoryDispatch` in `test_continuous_env_config.py` — grid-by-default (exact type) + continuous selection + extent-from-world_size_mm.)*
- [ ] 3.9 Config-loader tests (`configuration-system` spec, via `tests/.../utils/test_config_loader_yaml_compat.py`): continuous-2D env fields + continuous-action fields parse/validate; a legacy grid YAML with no `env_type` loads byte-identically (discrete default preserved). *(Partial: env-side fields + `env_type` discriminator + backward-compat tested in `test_continuous_env_config.py` (incl. invalid-env_type rejection); the continuous-**action** config fields are added + tested in §4.)*

## 4. Continuous-action heads on MUST brains + connectome adapter

- [ ] 4.1 Implement the tanh-squashed Gaussian continuous mode in `_policy.py` (2-D action; tanh + affine rescale to speed/turn bounds; log-det-Jacobian-corrected log-prob; clamped log-std; finite-output guarantees).
- [ ] 4.2 Add continuous heads to MLP-PPO, LSTM-PPO, CfC-PPO with the action mode + continuous dim (2) carried in `BrainConfig` (NOT a new `_build_infra_kwargs` per-arch branch); the existing `num_actions=4` infra-kwarg is interpreted as the discrete action-set size and ignored in continuous mode.
- [ ] 4.3 Connectome continuous-output adapter: `_pool_motor` → `(mean, log_std)` head via `_policy.py`; preserve strict-mask + fixed gap junctions (forward + post-step projection unchanged).
- [ ] 4.4 Unit tests: tanh-Gaussian log-prob/entropy/Jacobian correctness; bounded sampled actions; connectome strict-mask invariant across output modes.
- [ ] 4.5 Continuous-substrate smoke training: connectome + MLP-PPO on continuous-2D klinotaxis train without NaN/Inf with a learning signal (last-quarter > first-quarter return).

## 5. Transformer architecture + plugin-parity verification (Gate 2 G2.b/G2.c)

- [ ] 5.1 Implement the transformer/attention PPO brain (`brain/arch/transformer*.py`) registered via `@register_brain`; conform to the `Brain` Protocol; discrete head sufficient for the parity test (continuous head opportunistic).
- [ ] 5.2 Wire it through the ≤6-file budget: enum member, package import/export, config union, `_build_infra_kwargs` branch only if non-default, test file.
- [ ] 5.3 Refactor the `scripts/run_simulation.py` CLI-default `match brain_type` block ([`:286-298`](../../../scripts/run_simulation.py#L286)) into a registry-driven default so no per-arch branch remains at the entrypoint. **Ship this as a SEPARATE prior commit** (one-time platform fix) so it is NOT counted in the transformer-addition diff measured by G2.b.
- [ ] 5.4 Plugin-parity test: assert files-touched ≤ 6 on the transformer-addition commit(s) only (via `git diff --name-only`), measured per the [plugin-developer-guide](../../../docs/architecture/plugin-developer-guide.md) enumeration — new module, `dtypes.py` enum, `brain/arch/__init__.py`, `config_loader.py` union, `brain_factory.py` (only if non-default), test (YAML example excluded); plus a code-review verdict of no per-architecture branches in the simulation/training loops. Record engineer-hours (G2.a, documented, not load-bearing). Land the transformer runtime wiring in its OWN commit, isolated from the §5.6 doc-enumeration updates and any YAML config example, so the `git diff --name-only` measurement is uncontaminated.
- [ ] 5.5 Transformer trains on a klinotaxis smoke without collapse (learning signal).
- [ ] 5.6 Update the brain-arch enumerations that live OUTSIDE the ≤6 parity budget (and are not counted in G2.b): `AGENTS.md:33`, `README.md:36`, `CONTRIBUTING.md:140`, `openspec/config.yaml:18` — bump 24→25 and add the transformer entry. These are required for merge but are explicitly not per-architecture parity cost.

## 6. Gate 2 floor check + analysis

- [ ] 6.1 G2.d floor check: connectome + MLP-PPO on continuous-2D klinotaxis (single seed, T4 episode budget) reach mean episode return ≥ 50% of their T4 grid-substrate baseline for the same architecture. Record the numbers.
- [ ] 6.2 Platform-refactor delta analysis: continuous-2D vs discrete-grid movement kinematics; continuous-action vs discrete-action training stability (T5.analysis).

## 7. Logbook, Gate 2 decision, tracker + roadmap updates

- [ ] 7.1 Author continuous-2D scenario YAML(s) under `configs/scenarios/`; existing grid scenarios untouched.
- [ ] 7.1b Register a continuous-2D scenario in the smoke list (`SIMULATION_CONFIGS`, `tests/quantumnematode_tests/test_smoke.py:25`) so CI has a standing guard that the new substrate keeps parsing + running.
- [ ] 7.2 Publish the T5 logbook (`docs/experiments/logbooks/0XX-platform-refactor.md`) with the parity verification + floor-check evidence.
- [ ] 7.3 Record the Gate 2 GO/PIVOT/STOP decision in the T5 logbook against G2.a–G2.d; PIVOT trigger if G2.b OR G2.c fails; STOP trigger if post-pivot the interface is still incompatible with a MUST family.
- [ ] 7.4 Update `openspec/changes/phase6-tracking/tasks.md` T5 rows + the Gate 2 decision link; flip the `docs/roadmap.md` Phase 6 Tranche Tracker T5 row + Gate 2 outcome.
- [ ] 7.5 `openspec validate add-continuous-2d-and-action-heads --strict` clean; targeted `pre-commit run --files <changed>` clean; full `pre-commit run -a` before push.

## 8. SHOULD/MAY continuous heads (opportunistic, NOT gating Gate 2)

- [ ] 8.1 Continuous-output adaptation for SHOULD/MAY families (quantum, spiking, reservoir, hybrid) as opportunity allows; document any that don't adapt cleanly and defer.
- [x] 8.0 **Pull-forward discrete-path migration for the two SHOULD T7 comparison rows** (so their continuous heads build on the shared module). `equivariant_quantum` migrated **byte-exact** (torch path) and `spiking_ppo` migrated **Option B** (numpy sampler verbatim, torch log-prob/entropy/surrogate, ~1e-7). 116 tests green. The broader consolidation of the remaining PPO-family brains (hybrids via `_hybrid_common`, reservoir family via the two reservoir bases, `qsnnppo`/`qsnnreinforce`/`qliflstm`, + REINFORCE brains) is tracked in [GitHub issue #204](https://github.com/SyntheticBrains/nematode/issues/204), NOT this change.
