[project]
name = "quantum-nematode"
version = "1.0.0"
description = "Quantum brain simulation of a nematode (C. elegans) using variational quantum circuits"
license = { text = "Apache-2.0" }
authors = [{ name = "Chris Zaharia" }]
maintainers = [{ name = "Chris Zaharia" }]
requires-python = ">=3.13,<3.14"
keywords = [
    "quantum",
    "machine-learning",
    "nematode",
    "c-elegans",
    "simulation",
    "qiskit",
    "quantum-circuits",
    "variational-quantum-eigensolver",
    "quantum-reinforcement-learning",
    "parameter-shift-rule",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Physics",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Private :: Do Not Upload",
]
dependencies = [
    "qiskit>=2.0.0,<3.0.0",
    "matplotlib>=3.10.1",
    "ipykernel>=6.29.5",
    "numpy>=2.2.4",
    "pydantic>=2.11.4",
    "python-dotenv>=1.1.1",
    "rich>=13.0.0",
    "cma>=4.0.0",
    "optuna>=4.0.0",
    "pandas>=2.2",
    "openpyxl>=3.1",
    "ncps>=1.0.1",
]

[project.urls]
Homepage = "https://github.com/SyntheticBrains/nematode"
Repository = "https://github.com/SyntheticBrains/nematode"
Documentation = "https://github.com/SyntheticBrains/nematode#readme"
Issues = "https://github.com/SyntheticBrains/nematode/issues"

[project.optional-dependencies]
cpu = [
  "qiskit-aer>=0.17.0",
]
gpu = [
  # Upstream stopped publishing the default CUDA-12 `qiskit-aer-gpu` build after
  # 0.15.1 (Sept 2024), whose newest wheel is cp312 — it is the single reason
  # this project could not move past Python 3.12. The CUDA-11 build is still
  # maintained (0.17.2, Sept 2025) and ships cp313 wheels, so the GPU extra now
  # tracks it. NOTE: this moves the bundled CUDA runtime from 12 to 11.
  "qiskit-aer-gpu-cu11>=0.17.2",
]
torch = [
  "torch>=2.7.0",
]
qpu = [
  "qiskit-ibm-runtime>=0.40.1",
  "qiskit-ibm-catalog>=0.9.0",
]
pixel = [
  "pygame>=2.6.1",
]
analysis = [
  "scikit-learn>=1.5.0",
  "scipy>=1.14.0",
]

[tool.uv]
conflicts = [
    [
      { extra = "cpu" },
      { extra = "gpu" },
    ],
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["quantumnematode"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--verbose",
    "--strict-markers",
    "--strict-config",
    "--cov=quantumnematode",
    "--cov-report=term-missing",
    "--cov-report=xml",
    "--cov-report=html",
]
markers = [
    "smoke: Smoke tests that run entry-point scripts (excluded from pre-commit)",
    "nightly: Nightly regression tests against benchmarks (slow)",
    "slow: Heavy integration tests (e.g. real EvolutionLoop runs) — excluded from pre-commit, run before push",
]

[tool.coverage.run]
source = ["quantumnematode"]
branch = true
omit = [
    "*/tests/*",
    "*/test_*.py",
    "*/__pycache__/*",
    "*/site-packages/*",
]

[tool.coverage.report]
precision = 2
show_missing = true
skip_covered = false
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if __name__ == .__main__.:",
    "raise AssertionError",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
    "@abstractmethod",
]

[tool.coverage.xml]
output = "coverage.xml"

[tool.coverage.html]
directory = "htmlcov"
