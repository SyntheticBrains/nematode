"""Tests for top-level ``scripts/`` entry points.

Deliberately not named ``scripts``: a sibling test locates the repository root by walking up to
the first ancestor holding a ``scripts/`` directory, and a package of that name here shadows it.
"""
